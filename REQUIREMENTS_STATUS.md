# Requirements implementation status

Updated: 2026-09-02 (Issue #238 delivery)

This tracked file is the source of truth for the original product requests. Update it in the same Issue/PR that changes a status. `Implemented` means the behavior and automated regression coverage are on `main`; it does not replace the physical release evidence in `VALIDATION_BLOCKERS.md`.

## Implemented on main

| Area | Delivered behavior |
|---|---|
| Batch condition editing | Direct table editing, validated all-or-nothing Ctrl+C/Ctrl+V, dropdown fields, Shift/Ctrl multi-selection, and gradient content preview/edit/assignment. Issue #214: a session-only copy dialog selects any combination of visible condition and gradient elements from exactly one checked row, then applies only those elements to multiple checked rows as one accepted-dialog Undo/Redo step; empty-session paste is non-mutating, unselected values remain unchanged, and the existing condition/gradient preset workflows are retained |
| Project and file workflow | New project in another window, Ctrl+S, current project path/name in the window, ASCII/project drag-and-drop, multi-file and directory import. Issue #175: a batch-imported directory is registered with its label/recursive setting and participates in Undo/Redo and project persistence. Issue #199: the directory-import dialog exposes a default-on work-directory registration checkbox; opting out or importing no file leaves the registered list unchanged, while successful enabled imports retain the existing normalized duplicate prevention, Undo/Redo, and persistence behavior |
| Continuous directory import | Multiple project-owned work directories, labels, explicit reload, new-file detection, SHA-256 duplicate skip, and changed-path hold without silent replacement. Issue #175: batch discovery and reload both prefer same-directory, case-insensitive same-stem GCD over TXT, retain TXT-only and separate-directory matches, and report skipped TXT files. Issue #178: known PACsolution GCD File Property timestamps produce readable Run IDs; unknown formats use strict filename then file-mtime fallback with recorded provenance. New imports infer Y2 only from a ch2-only parent path, start with a blank measurement group, and never rewrite existing project Run ID/Y-axis/group fields |
| Chromatogram list | Run ID and timestamp separated from label, shared label/column/gradient at Run level, group/ungroup controls, column display, drag-and-drop reorder, and delete confirmation. Issue #144: new Run IDs use acquisition date/time, persistent project-local sequence and initial label (unknown-datetime when unavailable); direct whole-Run rename rejects empty/colliding IDs without grouping, supports Undo/Redo and preserves existing IDs on migration. Issue #179: visible columns use the requested default order, support drag reordering persisted as an application setting, keep timestamp/group metadata outside the table, and organize the lower controls into titled Data, Analysis, and Spectrum display groups. Issue #200: one confirmed action removes every selected row, counts them in the prompt, and is a single Undo/Redo step that restores the removed chromatograms in place; drag reordering reports the drop as ignored so the view cannot delete the row it rebuilt, and a drop landing on a row is refused instead of overwriting it; the acquisition timestamp has its own editable column left of the label, an imported label starts blank while Run IDs and legends keep their filename fallback, and the batch condition table shows each row's read-only Run ID. Issue #213: the default home-table order begins with selection, visibility, label, trace color, Run ID and timestamp; source paths move from the home table to a read-only conditions-dialog column; legacy saved orders retain the relative order of every surviving column. Analysis now contains only Measurement / sample conditions input and Display settings, while detail, gradient and Run grouping remain available under Data; the trace-color cell keeps its own background during row selection, and the work-directory recursion header explicitly says that subfolders are included. Issue #214: the Measurement / sample conditions dialog uses an explicit Select column, shows read-only Run ID then timestamp, and routes grouping and ungrouping through the existing MainWindow operations; confirmation lists every source Run ID and the target Run, whose existing timestamp remains authoritative and is never regenerated during grouping |
| Selection and display | Shift range selection, Ctrl multi-selection, Y1/Y2 split-view mode, independent axes, Y-only zoom fix, grid toggle, vertical pointers with Delete, and fraction-collector intervals. Issue #176: both screen backends use panel-aware toolbar coordinates and shared scene bounds for Y-axis full view; the native preview ends pan on every release/error, clears committed trace-drag translations, and keeps pointer motion on a geometry-stable no-refresh path. Issue #179: an independent selection checkbox supports check-only multi-selection and stays synchronized with Shift/Ctrl whole-row selection without changing trace visibility. Issue #180: an explicit mouse-mode selector coordinates normal navigation and all editing tools; vertical pointers support additive selection and bulk deletion; integration-area clicks select the narrowest containing interval and its peak-table row in both renderers; fraction ranges support numeric add/edit while retaining the verified drag path; and a transient, signal-based arbitrary time-range contract is available for Issue #183 without changing project data or schema. Issue #182: one action resets only the selected trace's time shift and vertical offset to zero, recalculates its derived peaks, preserves raw arrays and other traces, and is one Undo/Redo step. Issue #202: the grid draws only the vertical lines its existing on/off setting controls, in both renderers; x / y / w reach the existing full-X, full-Y and full-view actions from either renderer and are disabled while an editor holds focus, so the letters still reach text fields; and a reserved layout column keeps the second Y-axis title clear of the B% axis in single and split views. Issue #201: a native pan also ends when pointer motion arrives with no button held, so a release delivered outside the viewport can no longer leave the view grabbed; trace movement compares panel roles only in split view, so a Y2 trace is dragged in the single panel through its own coordinate system with raw arrays unchanged. Issue #203: the numeric integration-range dialog can hand off directly to the existing mouse range editor, shows the established edit-mode hint, and returns to the prior mouse mode after completion or Escape; PyQtGraph selection changes from either peak table restyle the existing integration items without rebuilding the scene or changing the current view. Issue #216: Y-axis full view derives each axis from finite data inside the current X range and leaves an axis unchanged when that range contains no points; toolbar Home and the full-view action now use the same reset path; native integration, retention-label, gradient, grid, and legend changes preserve the current view without rerendering the scene; every mouse mode retains the same event owner across grid changes; and split Y panels have a session-only draggable divider whose ratio survives scene refreshes. Issue #217: one selection mode now handles integration-area clicks and dragged time ranges; a range selects the current chromatogram's integrations and all vertical markers whose displayed retention times fall inside it, and Delete removes them in one Undo/Redo step. Vertical markers show retention-time labels that follow marker movement in both screen renderers. Fraction ranges are numeric-only; the UI accepts intervals in seconds with a one-second minimum, converts them to the unchanged persisted `interval_min` minutes, and rejects operations that would exceed 5,000 divider lines. Issue #221: Display settings retains one-at-a-time explicit color selection, adds solid/dashed/dotted/dash-dot per-trace styles across Matplotlib, PyQtGraph, PNG/SVG/PDF and reports, and assigns a selected table-order trace set colors from the shared 3D colormap families and dense-end range in one Undo/Redo step |
| Replace the interactive Matplotlib screen renderer | Issue #177: Windows 11 / Qt 6 starts with PyQtGraph as the persisted application-level screen renderer, with backend-neutral `ScreenViewState` as the interactive range authority and explicit automatic fallback reasons. Issue #223: Windows 7 / Qt 5 installs the exact Python 3.8 / NumPy 1.20-compatible PyQtGraph 0.13.3 wheel from the verified offline bundle and exposes the same renderer as an application-level opt-in while retaining Matplotlib as its initial default. Qt5/Qt6 share one scene, event, interaction, navigation, fallback, and PyInstaller path; PNG/SVG/PDF, clipboard/print reconstruction, A4 reports and 3D remain Matplotlib-owned. No project schema, preset, raw array, or scientific calculation changes; making PyQtGraph the Win7 default remains gated on physical Core 2 acceptance |
| Legends and labels | Configurable Run ID/label/timestamp/wavelength legend composition, optional spectrum/trace name in the B% legend, fixed-size text boxes, and retention/integration labels on all selected chromatograms. Issue #140: split-view B% is shown on both panels or hidden on both via the existing toggle; both show the selected trace's same gradient and B% scale, with one legend entry, in Matplotlib and the preview. A hidden/unconfigured selected gradient hides both. Issue #182: the persisted B% trace-name option is edited inside the legend-composer dialog without a schema or field rename, and the legend and axis/label settings buttons share one compact row |
| Presets | Preview/diff before apply, created/used/updated/name sorting, filter, rename/duplicate/delete, and import/export conflict policies. Issue #215: analyte presets are a third application-wide preset kind in `presets.json` format 3, holding only ε214, ε280 and molecular weight as positive finite values or blanks. The details dialog offers them in an editable dropdown whose first entry registers a new one, an explicit choice overwrites all three quantitation constants including blanks, and the preset manager lists, renames, duplicates, deletes, exports and imports them under the existing atomic conflict policies. Older save and load entry points keep their signatures and no longer discard analyte presets, and the extinction-coefficient unit is removed from the form while its persisted field is still read and written unchanged |
| Output and reports | Current view print and clipboard copy; all/visible/selected report scopes; compact peak table; per-export baseline/range/retention-time/B% options. Issue #182: the A4 first-page peak table is constrained to its allotted axis with added row spacing so it stays below the chromatogram X-axis label; headings, conditions and continuation pages remain intact. Issue #219: printing the analysis report opens a preview whose paint request runs the same routine, over the same rendered pages, that sends the job to the printer, so the preview cannot drift from the printed result, and cancelling it prints nothing. The exported PDF is named after the project — its file name when saved, its title otherwise — through the shared filename sanitizer, so an empty title falls back to a fixed default and characters Windows rejects never reach the name. Printing the current view keeps its own dialog |
| 3D chromatogram visualization | Issue #185: two or more table-selected chromatograms can be compared as full-data Matplotlib 3D lines with retention time, table-order series labels and intensity axes. The session-only dialog inherits the current 2D X range and existing axis titles, fonts, display unit, trace colors, trace line width and PNG/SVG/PDF size/DPI settings; it adds only the series-axis title, Z range, view angles, aspect, X/Z tick intervals, trace/gradient colors with dense-scale percentage, and axis line width. Time shifts apply while 2D stacking offsets do not; project schema and raw/scientific data are unchanged. Issue #204: grid lines are switchable and default to off, reaching the preview and the exported image alike, and one action returns the elevation and azimuth to their initial values while leaving every other 3D setting and further rotation untouched |
| Scientific metadata | Run-owned analyte name, stable ID, aliases, source, molecular weight, extinction coefficients and units; persistence, export, and quantitation integration |
| Peak fitting | Non-destructive Gaussian and right-tailing EMG fitting, automatic model selection, RMSE/R²/AIC, and visual overlay. Issue #181: every fit is an explicit parent-linked fitted row outside the integration collection, shown by default and distinguished in the table, Matplotlib/PyQtGraph legends, peak CSV, and PDF report; parent area, retention time, FWHM, and %Area remain unchanged; fitted rows can be recalculated or deleted independently; and a non-modal integration-list window keeps selection and note/range/fit/delete actions synchronized with the main table without owning or discarding data. Issue #218: a fitted row now carries the area, height, width and retention time of its model curve, reversing the Issue #181 decision to leave them blank, because the area of the reconstructed peak is the result the fit exists to produce. Gaussian areas use the closed form and EMG areas a dense trapezoid integration of the same profile; %Area and quantitation stay blank on fitted rows so those columns keep their measured meaning. Saturated peak correction detects the clipped flat top, or takes a range given by hand, excludes those samples and rebuilds the peak from its unsaturated flanks as an ordinary fitted row, recording the span it treated as clipped. It refuses an unsaturated peak, leaves raw arrays and the parent integration untouched, and is one Undo/Redo step. Every surface marks the estimate: the peak table tints the derived cells and names the row, the screen and report legends and the report table say estimated, and the peak CSV adds an area_source column beside a saturation-aware peak_type. Issue #238: the correction's reconstructed EMG height and area were collapsing onto the clipping ceiling, because `emg_profile` normalizes by the maximum of whatever samples it is given and the correction excludes the apex samples by design, so the least-squares amplitude found on the remaining samples described the height at the edge of that exclusion rather than the model's true peak. A one-time post-fit rescale, evaluated once per fit rather than inside the search, now reads `amplitude_uv` as the height of the model curve at its own true apex, matching what `fitted_area_uv_min`/`fitted_apex_min`/`fitted_fwhm_min` already assume; the Gaussian path is analytically exact regardless and is untouched. Measured recovery on a synthetic clipped EMG peak improved from -30%/-50%/-65% height error at 70/50/35% clipping to -5.7%/-0.9%/-0.2%, and an ordinary (unsaturated) EMG fit's height moved by about -1.5%, both compared before and after this change on the same synthetic data |
| Automatic peak detection | Issue #183: detection now opens a cancellable dialog with whole-data or prior mouse-selected range and Low/Medium/High sensitivity choices. All eight thresholds for each level are editable and independently resettable in Preferences, persist as application-wide settings, and the chosen level is copied into the existing project Method fields. Range coordinates account for trace time shift, detection boundaries remain inside the raw-time interval, raw arrays are unchanged, omitted-range behavior is preserved, and threshold application plus candidate addition is one Undo/Redo action |
| Preferences and colors | Persisted Japanese/English choice and default 280 nm blue-family / 214 nm red-family trace colors |
| Windows 7 offline build | Issue #234: the offline kit ships the tracked requirement ledger, both Windows 11 dependency pin files and every `.github` release contract document, because the offline build runs the whole test suite before PyInstaller and those tests read them as source contracts. A regression test fails when a contract file is left out of the kit |
| Update foundations | Nonblocking Stable Release check; canonical asset selection; bounded download; SHA-256 and Authenticode probes; signer allow-list policy; progress/cancel cleanup; installer execution remains disabled |

## Remaining source work

| Priority | Requirement | Current boundary | Next coherent delivery |
|---|---|---|---|
| High | Spectrum-domain selection and retention labels | Chromatogram multi-selection/labels are complete, but there is no separate spectrum data model, importer, view, or shared chromatogram/spectrum selection contract | Specify supported spectrum source format and semantics, then add the persisted model/import/view before enabling common labels |
| Medium | Activate signed updater workflow | Check UI, non-launching verified-download API, background Qt worker, and bilingual progress/cancel/error dialog exist; the application deliberately exposes no installer download or execution. Issue #190 settled the distribution channel the check depends on — public GitHub Releases plus offline media, recorded in `DISTRIBUTION.md` — without changing any application behavior | Configure an approved signer identity and validate signed fixtures before connecting the component to the end-user action; launch still requires an explicit confirmation design and physical validation |

The following are useful extensions, but are not gaps in the original requested minimum: a shared analyte master library beyond the application-wide analyte presets of Issue #215 and the persisted Run snapshot, multi-peak deconvolution/curved baseline fitting beyond the Gaussian/EMG single-peak fitting and saturated-peak correction of Issue #218, and the long-term TraceLab platform split.

## Defects found in shipped behavior

Windows 11 field use on `c5fe4c6` found three defects in rows listed above as implemented. Their root causes are recorded in `WIN11_FEEDBACK_WORKFLOW.md` section 9 and each is confirmed, not suspected. Until they are fixed, the affected rows overstate what the application does. Issue #238 is fixed and removed from this table; its outcome is recorded in the Peak fitting row above instead.

| Issue | Affected row | What is actually wrong |
|---|---|---|
| #236 | Selection and display | Selecting the normal pan/zoom mouse mode does not activate the toolbar tool, so pan and zoom only work while a toolbar button is pressed. Separately, PyQtGraph's axis bounding rectangle expands to cover the whole plot while a grid is drawn, so every pointer press inside the plot resolves to the X-axis region and pans X only. Issue #216 fixed a different mechanism for the same symptom and the symptom survived |
| #237 | Selection and display | Changing the integration selection restyles the lines but not the filled region, because `LinearRegionItem.setBrush` does not schedule a repaint the way `setPen` does on the line items |

Issues #239, #240, #241 and #242 in the same batch are usability and layout requests rather than defects, except that #241 also adds the missing input floor behind a reported crash: axis tick spacing accepts values down to 0.000001 min, which asks both renderers for tens of millions of tick lines. The requested floor is 0.1 min, and the spacing fields stay in minutes.

## Project compatibility notes for Issues #181 and #221

Schema 107 stores explicit fitted rows in each Dataset's new `fitted_peaks` collection while keeping integrations in the existing `peaks` collection. Loading schema 106 or earlier creates a parent-linked fitted row from each legacy parent fit without changing the parent's integration values. The latest fitted result is also mirrored into the parent's existing `fit_*` fields so older v1 applications can still display one fit and continue to read the integrations.

If a schema-107 project is opened and saved again by an older application, that application ignores and therefore removes the new `fitted_peaks` collection. Explicit fitted-row IDs, parent links, fitted-row notes, and all but the last mirrored fit per parent are lost. The parent integrations and the last mirrored `fit_*` result remain; reopening that older-saved project in the current application reconstructs at most one explicit fitted row per parent. Physical Win7-to-Win11 and Win11-to-Win7 project round trips remain an external validation gate.

Schema 108 adds the display-only `Dataset.line_style` field. Schema 107 and earlier projects migrate to `solid`; saved solid, dashed, dotted and dash-dot choices otherwise round-trip without changing raw arrays, integration values, or the project-wide line width. Older v1 applications ignore the new field and therefore display every trace as solid; if such an application re-saves the project, the per-trace line-style choices are discarded and reopen as solid in the current application. Explicit trace colors remain in their established field. Physical Win11-to-Win7 and Win7-to-Win11 schema-108 round trips remain an external validation gate.

## External and physical gates

Code signing identity, signed updater launch, Stable publication approval, Windows 11 installer checks, physical Windows 7 SP1 x86/Core 2 offline checks, cross-OS project/upgrade preservation, physical printing, and published-asset re-download remain open. Their exact required evidence is maintained in `VALIDATION_BLOCKERS.md`; none is considered passed from CI or offscreen tests.

## Maintenance rule

Before opening a new implementation Issue, check this file, `VALIDATION_BLOCKERS.md`, open GitHub Issues/PRs, and current `main` to avoid duplicate work. Merge one independent Issue before starting the next. A PR that implements or materially narrows a row must update this file and cite the Issue/PR in its summary.
