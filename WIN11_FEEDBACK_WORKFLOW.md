# Windows 11 実機フィードバック 対応ワークフロー

Updated: 2026-08-31 (サ行)

Windows 11 実機での動作確認によって提出された修正要求を、Claude と Codex の
どちらが担当しても同じ手順・同じ粒度で実装できるようにするための計画文書です。

この文書は **要求の割り当てと進め方だけ** を定義します。リポジトリ全体の規約
（Windows 7 互換、プロジェクト互換、科学的挙動、Git 運用）は `AGENTS.md` が
引き続き上位にあり、この文書がそれを緩めることはありません。

---

## 0. 読む順序（Claude / Codex 共通）

作業を始める前に、必ずこの順に確認します。

1. `AGENTS.md` — リポジトリ規約（最上位）
2. `REQUIREMENTS_STATUS.md` — 実装済み／残作業の唯一の台帳
3. `VALIDATION_BLOCKERS.md` — 外部・物理検証の未達ゲート
4. **この文書** — 今回のバッチ定義と実施順
5. `CODEX_BRIEF.md` — 着手可能な Issue と手順の要約
6. GitHub の Open Issue / Open PR と最新の `origin/main` — 重複着手の防止

`CLAUDE.md` はこの一覧への入口であり、内容を持ちません。

---

## 1. バッチ方針（Issue を切りすぎない）

Issue #110〜#173 は 1 Issue = 1 関数レベルまで細分化されており、AI の
コンテキスト再構築コストが実装コストを上回っていました。以降は次の方針です。

- **1 Issue = 1 つのまとまった利用者価値**。同じモジュール・同じテストファイルを
  触る要求は 1 つの Issue にまとめる。
- 1 Issue の目安は **「変更ファイル 1〜4、要求項目 2〜5 件」**。要求 1 件だけの
  Issue は、他に同居させられる要求がない場合に限る。
- **1 Issue = 1 ブランチ = 1 PR = 1 マージ**（`AGENTS.md` の既存規約を維持）。
- Issue を分けるのは、**モジュールが違う／依存順が違う／片方が実機確認待ちで
  ブロックされうる** 場合だけ。「作業が多いから」は分割理由にしない。

### バッチ名（かな行）

要求群を五十音の行で呼びます。1 行あたり最大 5 バッチです。

| 行 | バッチ | 状態 |
|---|---|---|
| **ア行** | ア・イ・ウ・エ・オ | 完了（#175, #176, #177, #178, #179） |
| **カ行** | カ・キ・ク・ケ・コ | 完了（#180, #181, #182, #183, #185） |
| **サ行** | サ・シ・ス・セ・ソ | **今回のバッチ** |

GitHub の Issue タイトルは既存リポジトリの慣習に合わせて英語、本文は日本語で
書きます。本文冒頭に `Batch: サ` のように行記号を明記します。

---

## 2. 実施順と依存関係

```
サ(#200)                       （独立。表まわりのみ）
ソ(#204)                       （独立。plot3d.py のみ）
ス(#201) ──► シ(#202) ──► セ(#203)   （PyQtGraph 系。同じファイルを触るため直列）
```

| 記号 | Issue | 内容 | 優先 |
|---|---|---|---|
| サ | #200 | クロマトグラム表の操作と列構成 | 高 |
| ス | #201 | PyQtGraph の入力不具合（パン解放・第 2 縦軸のスペクトル移動） | 高 |
| シ | #202 | PyQtGraph の軸配置・グリッド・キーボードショートカット | 中 |
| セ | #203 | 積分操作の分かりやすさ（マウス選択への切替・ちらつき） | 中 |
| ソ | #204 | 3D 表示のグリッド切替と視点リセット | 低 |

**着手可能なのは サ(#200) / ス(#201) / ソ(#204) の 3 つ**です。相互に依存しません。

- **ス → シ → セ**: 3 つとも `screen_preview.py` を触ります。並行させるとほぼ確実に
  衝突するので直列にします。**ス を先に**着手してください。パン解放は 2 回続けて
  報告されている不具合で、利用者への影響が最も大きいためです。
- **サ・ソ は独立**。サ は表と一括入力ダイアログ、ソ は `plot3d.py` だけを触ります。
  PyQtGraph 系とファイルが重なりません。

ス が実機調査で止まった場合でも、シ・セ を待たせないでください。**先にマージ
された方に他方を rebase**すれば進められます。その場合は PR にその旨を書いてください。

---

## 3. 1 Issue あたりの手順

```
1. git fetch origin && git checkout -B <branch> origin/main
2. この文書の該当バッチ節を読み、受け入れ条件を Issue のチェックボックスに写す
3. 実装（AGENTS.md の「最小で首尾一貫した変更」を維持）
4. テスト追加・更新（受け入れ条件 1 件につき最低 1 アサーション）
5. 検証コマンド（第 4 節）を実行し、実際の出力を PR に貼る
6. REQUIREMENTS_STATUS.md を同じ PR で更新
7. PR を作成（.github/pull_request_template.md を埋める）
8. CI 通過・指摘解消後にマージ。次の Issue はマージ後の main から開始
```

ブランチ名は Claude が `claude/...`、Codex が `codex/...` を使います。

### 実機確認が必要な項目の扱い

今回の要求の多くは **オフスクリーン CI では真偽を確定できません**（パン解放、
ちらつき、軸の間隔、DPI 依存のレイアウト）。次のように扱います。

- 実装 PR には **再現テスト（イベント列レベル・状態レベル）** を必ず入れる。
  合成イベントで回帰は防げます。
- そのうえで Issue に **「実機確認」チェックリスト** を置き、Windows 11 実機で
  確認した結果（日時・確認者・コミット SHA・操作手順・結果）を Issue にコメント
  として記録する。
- 実機確認が未了でも、合成テストが通り退行がなければマージしてよい。ただし
  PR の「Known limitations」に未確認である旨を明記する。
- 実機で再現しないと判断した項目は、Issue を閉じずに「実機で再現せず」を記録し、
  該当受け入れ条件を取り下げる。**CI が通ったことを実機確認の代替にしない。**

**ス(#201) は特にこれが効きます。** パン解放は前回の イ(#176) で修正済みと
判断してマージしましたが、実機では直っていませんでした。合成テストが通ることと
実機で直っていることは別だという実例です。第 5 節に、前回とは異なる原因の
仮説を書いてあります。

---

## 4. 検証コマンド

リポジトリ直下で実行します。

```text
python -m compileall -q hplc_app app.py
python scripts/ci_validate.py --offline-assets auto
set QT_QPA_PLATFORM=offscreen
python -m unittest discover -s tests -v
```

描画・操作系（シ・ス・セ・ソ）を触った場合は追加で:

```text
python scripts/probe_pyqtgraph_parity.py
python scripts/benchmark_integrated_screen.py
```

Windows 7 の固定依存に触れた場合のみ:

```text
python scripts/windows7_import_preflight.py
python scripts/verify_windows7_wheelhouse.py
```

PR の Testing 欄には **実際に走らせたコマンドと結果だけ** を書きます。

---

## 5. 事前調査で判明した技術的事実

再調査の重複を避けるため、計画時にコードを読んで確認した内容を記録します。
行番号は `main` = `d693ee8` 時点のものです。**実装が進むとずれるので、行番号では
なく関数名で探してください。**

### 5.1 パン解放がまだ直らない理由（前回とは別の原因。有力仮説）

イ(#176) / PR #188 で `PyQtGraphNavigationController.handle_event` に `try/finally`
が入り、**解放イベントが届けばボタン種別を問わずパンを終了する**ようになりました。
現在のコードを読む限り、このハンドラ自体は正しく書けています。

にもかかわらず症状が残るということは、**解放イベントがそもそもハンドラに届いて
いない**と考えるのが自然です。有力な経路は次のとおりです。

`PyQtGraphSceneConsumer._dispatch_viewport_event` は、ハンドラが消費すると
`True` を返してイベントフィルタでイベントを止めます（`pyqtgraph_scene.py`）。
押下が消費されると `QGraphicsView` 本体に押下が渡らないため、**Qt の暗黙の
マウスグラブがビューポートに確立されない**可能性があります。その状態でポインタ
がビューポート外に出てから解放すると、解放は別のウィジェットに配送され、
フィルタを通りません。パンは掴まれたまま残ります。

「速く動かして外で放すと残る／中でゆっくり放すと直る」という **判定の甘さ**の
体感と一致します。

対処の候補（実機で確認してから選ぶこと）:

1. パン開始時に明示的に `grabMouse()` し、解放・キャンセル時に `releaseMouse()` する
2. `motion_notify_event` で **ボタンが押されていない**（`event.buttons()` が空）
   ことを検出したらパンを終了する安全網を足す。合成イベントで直接テストできる
3. パン中だけアプリケーション全体のイベントフィルタで解放を拾う

**2 は 1 の有無にかかわらず入れる価値があります。** どの経路で解放を取りこぼしても
次の移動で必ず復帰でき、オフスクリーンで回帰テストが書けるためです。

### 5.2 第 2 縦軸のスペクトル移動が効かない理由（有力仮説）

`ExperimentalScreenPreview._handle_move_event` (`screen_preview.py`) は、まず
`role = "y2" if selected.y_axis == 2 else "y1"` を決め、開始判定の `valid` では
`(not self.consumer.split_y_axes or event.axis_role == role)` と、**分割表示のときだけ**
軸ロールの一致を要求しています。

ところがドラッグ継続中の分岐には、その保護がありません。

```python
if (not valid or event.axis_role != target["role"] or event.button != 1):
    self.cancel_move_drag()
```

**非分割表示ではポインタは常に primary 上にあるため `event.axis_role` は `"y1"`**
です。Y2 のデータセットを選んでいると `target["role"]` は `"y2"` なので、最初の
移動イベントで条件が成立し、ドラッグが即キャンセルされます。これが「第 2 縦軸に
設定したクロマトグラムのスペクトル移動が機能しない」の説明になります。

対処方針: 継続中の軸ロール比較も `split_y_axes` のときだけ行う。非分割では
primary の上で Y2 の座標系（`event.data_for("y2")`）を使って移動量を計算する。
座標自体は `pointer_event` が `("y2", self.secondary)` を含めて渡しているので、
既に利用可能です。

### 5.3 ピーク選択で画面がちらつく理由（確定）

`MainWindow._peak_selection_changed` (`gui.py`) の末尾はこうなっています。

```python
if (self._screen_preview is None and self._is_lightweight_rendering()
        and self._peak_overlay_artists):
    self._apply_peak_selection_styles()      # 軽量な再スタイルのみ
    self._request_canvas_draw(throttled=True)
else:
    self._plot()                             # 図全体を作り直す
```

軽量な再スタイル経路は **`self._screen_preview is None`**、つまり Matplotlib 画面の
ときだけ通ります。ウ(#177) 以降 PyQtGraph が Windows 11 の既定なので、
**ピークを選ぶたびに `_plot()` でシーンを作り直しています**。これがちらつきの
原因です。仮説ではなく、条件式そのものです。

対処方針: PyQtGraph 側にも「積分オーバーレイの見た目だけを更新する」経路を作り、
シーン再構築と表示範囲の再適用を伴わずにハイライトを切り替える。既存の
`_apply_peak_selection_styles` が Matplotlib 側の等価物です。

### 5.4 ドラッグ&ドロップでクロマトグラムが消える理由（有力仮説）

`DatasetTableWidget` (`gui.py`) は `InternalMove` + `MoveAction` を設定し、
`dropEvent` で自前に `rowMoveRequested` を発火してから
`event.acceptProposedAction()` を呼んでいます。

Qt の `QAbstractItemView::startDrag` は、ドラッグの結果が `MoveAction` で
`dragDropMode() == InternalMove` のとき、**ドラッグ元の行をビューから削除します**。
つまり、こちらが `rowMoveRequested` でモデルを並べ替えて表を作り直したあとに、
Qt が「元の行」を削除します。再構築後の表の当該位置は別のクロマトグラムなので、
それが表示から消えます。

対処方針: `dropEvent` で `event.setDropAction(Qt.IgnoreAction)` としてから
`accept()` する。Qt 側の削除処理を走らせず、並べ替えは自前の
`rowMoveRequested` 経路だけが行う形にする。

**「別のクロマトグラムの上に置けないようにする」** については、
`setDragDropOverwriteMode(False)` は設定済みですが `dragMoveEvent` の上書きが
ないため、行の「上」にドロップする位置指示が出ます。`dragMoveEvent` を上書きし、
`dropIndicatorPosition()` が `OnItem` のときは受け付けないでください。

### 5.5 現在 Matplotlib が担当している範囲

ウ(#177) で PyQtGraph が Windows 11 の既定になりました。Matplotlib は次を持ち
続けています。画面まわりを触るときの前提として引き続き有効です。

| # | 担当領域 |
|---|---|
| 1 | **画面状態の真実の源**。軸・表示範囲・オーバービュー窓・履歴は Matplotlib の axes が保持し、PyQtGraph は `refresh()` のたびに `owner._screen_view_state()` を読んで追従する |
| 2 | **図の出力**。PNG / SVG / PDF・クリップボード・印刷は `_full_quality_export_figure()` が Matplotlib アーティストを完全再構築してから出力。A4 レポートと 3D 図も Matplotlib Figure |
| 3 | **フォールバック**。Qt5（Windows 7）ビルド、PyQtGraph 未導入、描画例外時は Matplotlib へ復帰 |
| 4 | **一部の操作計算**。`SpanSelector` を使う従来経路と autoscale |

PyQtGraph 有効時、Matplotlib は**データアーティストを持たない骨組み**として
維持され、出力時だけ完全再構築されます。「画面のちらつき」「表示範囲が戻る」系の
不具合は、たいていこの往復のどこかで起きています。

### 5.6 解決済みの調査記録

以下は ア行・カ行の計画時に記録し、**対応済み**になった項目です。詳細は各 PR を
参照してください。再調査は不要です。

| 内容 | 対応 |
|---|---|
| 一括読み込みが作業ディレクトリに登録されない | #175 / PR #187（#199 に残課題あり） |
| `.gcd` の Run ID が unknown-datetime になる | #178 / PR #192 |
| ピークフィットが別ピークになっていない | #181 / PR #195 |
| 「Y 軸全体」が PyQtGraph で効かない | #176 / PR #188 |
| マウス座標表示が PyQtGraph で未接続 | #176 / PR #188 |
| 縦線ポインターで画面が揺れる | #176 / PR #188 |
| パン解放が認識されない | #176 / PR #188 → **実機で未解決。ス(#201) で再対応** |

---

## 6. バッチ定義

各バッチの「対象ファイル」は着手時の見込みであり、増える場合は PR に理由を書きます。

---

### サ — クロマトグラム表の操作と列構成

**Issue: #200** / **依存なし**

対象要求
- 複数のクロマトグラムを選んでから「削除」したとき、複数同時に削除する
- ドラッグ&ドロップで並べ替えると表示上消えてしまうのを直す
- 行と行の**間**への挿入は並べ替えとして受け付け、行の**上**へのドロップは
  受け付けない（誤操作防止）
- 一括入力画面に Run ID を表示する
- 現在「ラベル」と表記されている欄を「タイムスタンプ」に変更し、新規に「ラベル」欄を
  その右に追加する。新しいラベルは既定で空白

受け入れ条件
- [ ] 複数行を選択して「削除」すると、選択したすべてが 1 回の操作で削除される
- [ ] 確認ダイアログが対象件数を示す。取り消したときは 1 件も削除されない
- [ ] 複数削除が Undo / Redo の 1 ステップとして扱われる
- [ ] ドラッグ&ドロップの並べ替えでクロマトグラムが消えない
- [ ] 行の間へのドロップは並べ替えになる
- [ ] 行の上へのドロップは受け付けられず、視覚的にもそれと分かる
- [ ] 一括入力画面に Run ID 列が表示され、どの行がどの Run か分かる
- [ ] 「タイムスタンプ」欄に取得日時が表示され、編集できる
- [ ] 「ラベル」欄がその右にあり、新規読み込み時は空白
- [ ] 既存 `.hplcproj` を開いても、既存のラベルとタイムスタンプが失われない
- [ ] `REQUIREMENTS_STATUS.md` を更新する

対象ファイル: `hplc_app/gui.py`, `hplc_app/dialogs.py`, `hplc_app/i18n.py`,
`tests/test_gui.py`

注意
- 消える原因と上書き防止の実装方針は **第 5.4 節**にあります。再調査は不要です。
- 削除は現在 `remove_dataset()` が `currentRow()` だけを見ています。オ(#179) で
  追加した**選択チェックボックス**との関係を決めてください。既定案は
  「選択チェックボックスと行選択のどちらでも複数削除の対象になる」です。
- **ラベル／タイムスタンプの実装方針（既定案）**: スキーマを変えない案で進めて
  ください。既に `timestamp` 列（`acquisition_datetime` に接続、既定で非表示）が
  存在します。これを表示して「タイムスタンプ」の位置に置き、既存の `label` 列を
  その右に移し、**新規読み込み時の `dataset.label` の初期値を空文字**にします。
  Run ID 生成は `dataset.label or dataset.original_filename or "unlabeled"` と
  フォールバックするので空でも壊れません。凡例やレポートで空ラベルが問題になる
  場合のみ、新規フィールド追加＋移行を検討し、その判断を Issue にコメントして
  ください。

---

### ス — PyQtGraph の入力不具合

**Issue: #201** / **依存なし** / バッチ「シ」の前提

対象要求
- パン操作でマウスを放したときの判定が甘い問題が解消されていない
- 第 2 縦軸に設定したクロマトグラムの「スペクトル移動」が機能しない（PyQtGraph のみ）

受け入れ条件
- [ ] ポインタをプロット外に出してから放してもパンが終了する
- [ ] 速いドラッグの直後に放してもパンが残らない
- [ ] ボタンを押していない移動イベントが届いたら、パンが必ず終了する
- [ ] 上記を合成イベント列で検証する回帰テストがある
- [ ] 第 2 縦軸のクロマトグラムを、非分割表示でスペクトル移動できる
- [ ] 分割表示での既存のスペクトル移動が退行していない
- [ ] 第 1 縦軸のスペクトル移動が退行していない
- [ ] 生データ配列の SHA-256 が全操作前後で不変
- [ ] `REQUIREMENTS_STATUS.md` を更新する

対象ファイル: `hplc_app/pyqtgraph_navigation.py`, `hplc_app/pyqtgraph_scene.py`,
`hplc_app/screen_preview.py`, `tests/test_gui.py`

注意
- **原因の仮説は第 5.1 節・第 5.2 節にあります。** イ(#176) のときとは違う原因を
  想定しています。`handle_event` の `try/finally` は既に入っているので、そこを
  もう一度直そうとしないでください。
- **前回はここで「修正済み」と判断してマージし、実機で直っていませんでした。**
  合成テストが通ったことを実機の代替にしないでください。実機で確認できるまで、
  PR の Known limitations に未確認である旨を明記すること。
- 5.1 の対処候補 2（ボタンなし移動でパン終了）は、他のどの経路を採っても
  安全網として入れる価値があります。オフスクリーンで直接テストできます。

---

### シ — PyQtGraph の軸配置・グリッド・ショートカット

**Issue: #202** / **依存: #201（ス）**

対象要求
- 第 2 縦軸と第 3 縦軸の軸とラベルの配置が狭く、適切な間隔になっていない
- 「x」で X 軸全体表示、「y」で Y 軸全体表示、「w」で全体表示のショートカット
- グリッドは横軸由来の縦線のみ。横線は不要

受け入れ条件
- [ ] 第 2 縦軸（右）と第 3 縦軸（%B）の軸線・目盛・軸タイトルが重ならない
- [ ] 分割表示でも同様に重ならない
- [ ] 日本語フォントでも軸タイトルが切れない
- [ ] `x` / `y` / `w` キーで、それぞれ X 軸全体・Y 軸全体・全体表示になる
- [ ] ショートカットが Matplotlib 描画でも同じように効く
- [ ] テキスト入力中や表の編集中にショートカットが誤発火しない
- [ ] グリッド表示が縦線のみになる。横線が出ない
- [ ] グリッドの ON/OFF が既存の設定で従来どおり切り替わる
- [ ] `REQUIREMENTS_STATUS.md` を更新する

対象ファイル: `hplc_app/pyqtgraph_scene.py`, `hplc_app/screen_preview.py`,
`hplc_app/gui.py`, `hplc_app/i18n.py`, `tests/test_gui.py`

注意
- グリッドは `screen_preview.py` の `primary.showGrid(x=..., y=...)` が両方に
  同じ値を渡しています。`y=False` に固定するだけで縦線のみになります。分割表示の
  `lower.showGrid` も同じです。
- 軸の間隔は `pyqtgraph_scene.py` の `primary.layout.addItem(self.gradient_axis, 2, 3)`
  と、`screen_preview.py` の `refresh()` にある `axis.setWidth(...)` の計算が
  支配しています。**まず実機のスクリーンショットで、どちらが詰まっているかを
  確認してから**直してください。
- ショートカットは既存の「X軸全体 / Y軸全体 / 全体表示」ボタンと同じ処理に
  つないでください。新しい計算経路を作らないこと。

---

### セ — 積分操作の分かりやすさ

**Issue: #203** / **依存: #202（シ）**

対象要求
- 積分エリアをマウスで選択する方法が分かりにくい。ダイアログで「マウスで選択」を
  選んだら、そこからマウス選択に切り替わってほしい
- 積分リストで別のピークを選ぶと画面が切り替わってちらつく。表示は固定したまま、
  選んだピークだけがハイライトされてほしい

受け入れ条件
- [ ] ダイアログで「マウスで選択」を選ぶと、閉じたあとマウス選択モードに入る
- [ ] 現在マウス選択待ちであることが画面から分かる
- [ ] 選択を完了またはキャンセルすると、元のモードに戻る
- [ ] 積分リストでピークを選び替えても、表示範囲・ズーム・視点が変わらない
- [ ] 選び替えのたびにシーンが作り直されない（再構築回数をテストで固定する）
- [ ] 選んだピークだけがハイライトされる
- [ ] メインウィンドウのピーク表と別ウィンドウの積分リストで同じ挙動になる
- [ ] Matplotlib 描画でも従来どおり動く
- [ ] `REQUIREMENTS_STATUS.md` を更新する

対象ファイル: `hplc_app/gui.py`, `hplc_app/screen_preview.py`,
`hplc_app/pyqtgraph_scene.py`, `hplc_app/dialogs.py`, `tests/test_gui.py`

注意
- **ちらつきの原因は第 5.3 節で確定しています。** `_peak_selection_changed` の
  軽量経路が `self._screen_preview is None` を条件にしているため、PyQtGraph が
  既定になった今は必ず `_plot()` を通ります。PyQtGraph 側にも再スタイル専用の
  経路を用意してください。
- 再構築が起きていないことは、シーンの生成回数やアイテムの同一性で検証できます。
  カ行で `trace_items` の同一性を検証した既存テストが参考になります。
- マウス選択への切替は、カ(#180) で入れた**マウスモード**の仕組みに載せてください。
  新しいモード管理を並行して作らないこと。

---

### ソ — 3D 表示のグリッド切替と視点リセット

**Issue: #204** / **依存なし**

対象要求
- グリッド線表示の ON / OFF を選べるようにする
- 表示を回転させたあと、初期表示に戻せるボタンが欲しい

受け入れ条件
- [ ] 3D 図のグリッド線を ON / OFF で切り替えられる
- [ ] 既定値が決まっており、切り替えが図と画像出力の両方に反映される
- [ ] 視点を初期値に戻すボタンがあり、押すと既定の仰角・方位角に戻る
- [ ] 戻したあとに再度回転できる
- [ ] 既存の 3D 設定（Z 範囲・アスペクト比・配色・目盛間隔）が退行していない
- [ ] `REQUIREMENTS_STATUS.md` を更新する

対象ファイル: `hplc_app/plot3d.py`, `hplc_app/dialogs.py`, `hplc_app/i18n.py`,
`tests/test_core.py`, `tests/test_gui.py`

注意
- 現在 `plot3d.py` の `build_3d_chromatogram_figure` は `axis.grid(False)` を
  ハードコードしています。`ThreeDPlotOptions` に項目を足して切り替えてください。
- 視点の初期値は `ThreeDPlotOptions` の `elevation_deg=20.0` /
  `azimuth_deg=-65.0` です。リセットはこの既定値に戻す実装にしてください。
  利用者の表現は「x, y 軸が垂直な表示状態」なので、**実機で見て、その既定値が
  意図に合うかを確認**してください。合わない場合は既定値の変更を提案し、
  判断を Issue にコメントすること。
- `ThreeDPlotOptions` は frozen dataclass です。項目追加は既定値つきで行い、
  既存の呼び出しを壊さないでください。

---

## 7. 完了条件（全バッチ共通）

各 Issue は次がすべて満たされたときに閉じます。

1. 受け入れ条件のチェックがすべて埋まっている（取り下げたものは理由が書かれている）
2. 第 4 節の検証コマンドが通り、結果が PR に記録されている
3. `REQUIREMENTS_STATUS.md` が更新されている
4. 実機確認項目について、確認済みか「未確認」の明記があるか、どちらかである
5. PR がマージされ、`origin/main` に含まれている

## 8. 未決定事項

| 項目 | 内容 | 判断者 |
|---|---|---|
| 空ラベルの影響範囲 | サ節。`dataset.label` の初期値を空にしたとき、凡例・レポート・Run ID 表示で困らないか | 実装時に既定案（スキーマ変更なし）で進め、問題が出たら Issue にコメント |
| 複数削除の対象 | サ節。選択チェックボックスと行選択のどちらを対象にするか | 実装時は両方を対象として進め、実機確認で調整 |
| 3D 視点の初期値 | ソ節。既定の仰角 20 / 方位角 -65 が「x, y 軸が垂直」の意図に合うか | 実機確認。合わなければ既定値変更を提案 |
| グリッドの既定値 | ソ節。3D グリッドを既定 ON にするか OFF にするか | 実装時は現状維持（OFF）で進め、実機確認で調整 |

### 決定済み

| 項目 | 決定 |
|---|---|
| 環境設定の「描画品質」削除 | **保留**。Windows 7 実機での検証が未了のため、設定は現状のまま残す |
| フィット由来ピークの表示既定 | **既定で表に出す**。切替表示にはしない |
| 個人名の記載 | **書かない**。追跡対象ファイル・コミットメッセージ・Issue・PR のいずれにも研究室メンバーの氏名を残さない（`AGENTS.md`） |
