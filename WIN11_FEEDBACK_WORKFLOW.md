# Windows 11 実機フィードバック 対応ワークフロー

Updated: 2026-08-30 (rev.2)

Windows 11 実機での動作確認によって新たに提出された修正要求を、Claude と Codex の
どちらが担当しても同じ手順・同じ粒度で実装できるようにするための計画文書です。

この文書は **要求の割り当てと進め方だけ** を定義します。リポジトリ全体の規約
（Windows 7 互換、プロジェクト互換、科学的挙動、Git 運用）は `AGENTS.md` が
引き続き上位にあり、この文書がそれを緩めることはありません。

---

## 0. 読む順序（Claude / Codex 共通）

作業を始める前に、必ずこの順に確認します。

1. `AGENTS.md` — リポジトリ規約（最上位）
2. `REQUIREMENTS_STATUS.md` — 実装済み／残作業の唯一の台帳
3. `VALIDATION_BLOCKERS.md` — 外部・物理検証の未達ゲート
4. **この文書** — 今回のバッチ定義と実施順
5. GitHub の Open Issue / Open PR と最新の `origin/main` — 重複着手の防止

`CLAUDE.md` はこの一覧への入口であり、内容を持ちません。

---

## 1. バッチ方針（Issue を切りすぎない）

過去の Issue #110〜#173 は 1 Issue = 1 関数レベルまで細分化されており、AI の
コンテキスト再構築コストが実装コストを上回っていました。今回は次の方針に変えます。

- **1 Issue = 1 つのまとまった利用者価値**。同じモジュール・同じテストファイルを
  触る要求は 1 つの Issue にまとめる。
- 1 Issue の目安は **「変更ファイル 1〜4、要求項目 2〜5 件」**。要求 1 件だけの
  Issue は、他に同居させられる要求がない場合に限る。
- **1 Issue = 1 ブランチ = 1 PR = 1 マージ**（`AGENTS.md` の既存規約を維持）。
- Issue を分けるのは、**モジュールが違う／依存順が違う／片方が実機確認待ちで
  ブロックされうる** 場合だけ。「作業が多いから」は分割理由にしない。

### バッチ名（かな行）

要求群を 2 つのグループに分け、五十音の行で呼びます。

| グループ | Issue | 位置づけ |
|---|---|---|
| **ア行** | ア・イ・ウ・エ・オ | 最優先の 4 件と、それに直結する読み込み・表の基盤 |
| **カ行** | カ・キ・ク・ケ | 積分・表示・設定・レポートの改善 |

GitHub の Issue タイトルは既存リポジトリの慣習に合わせて英語、本文は日本語で
書きます。本文冒頭に `Batch: ア` のように行記号を明記します。

---

## 2. 実施順と依存関係

```
ア(#175) ──► エ(#178) ──► オ(#179)
イ(#176) ──► ウ(#177) ──► カ(#180) ──► キ(#181) ──► ケ(#183) ──► ク(#182)
```

| 記号 | Issue | 内容 | 優先 |
|---|---|---|---|
| ア | #175 | 一括読み込みディレクトリの登録と GCD/TXT 重複解決 | 最高 |
| イ | #176 | PyQtGraph 操作系の実機不具合修正 | 最高 |
| ウ | #177 | PyQtGraph の既定化と Matplotlib 責務の明文化 | 最高 |
| エ | #178 | 読み込み時の初期値整備（Run ID・縦軸・グループ） | 中 |
| オ | #179 | クロマトグラム表と下部ボタンメニューの再構成 | 中 |
| カ | #180 | マウスモードの新設と積分エリアのクリック選択 | 中 |
| キ | #181 | 積分データの明示化と積分リストの別ウィンドウ | 中 |
| ケ | #183 | 自動ピーク検出の範囲選択・感度 3 段階・閾値編集 | 中 |
| ク | #182 | 表示・レポートの小改修 | 低 |

**次に着手するのは ア(#175) と イ(#176) です。** どちらも `origin/main` から
直接分岐でき、相互に依存しません。

- **ア → エ**: どちらも `_import_chromatogram_paths` 周辺を触るため、直列化して
  コンフリクトを避ける。
- **エ → オ**: エが列の初期値を決め、オが列構成そのものを変える。逆順にすると
  オのテストをエで書き直すことになる。
- **イ → ウ**: 操作系のバグが残ったまま既定描画を切り替えると、実機で退行する。
- **ウ → カ**: 「マウスモード」は既定描画が確定してから設計する。
- **カ → キ**: 積分エリアのクリック選択（カ）が、積分リストの選択同期（キ）の前提。
- **カ → ケ**: 自動検出の「マウスで選んだ範囲」は、マウスモード（カ）が決まってから
  設計する。先に作ると同じ範囲選択 UI を二度作ることになる。
- **ク** は最後。ウ（凡例設定）・カ（トレース移動）・ケ（環境設定の再構成）の結果に
  依存する。

ア系列とイ系列は独立しているため、担当を分ければ並行して進められます。並行する
場合も `origin/main` から各々分岐し、片方をマージしてからもう片方を rebase します。

---

## 3. 1 Issue あたりの手順

```
1. git fetch origin && git checkout -B <branch> origin/main
2. この文書の該当バッチ節を読み、受け入れ条件を Issue のチェックボックスに写す
3. 実装（AGENTS.md の「最小で首尾一貫した変更」を維持）
4. テスト追加・更新（受け入れ条件 1 件につき最低 1 アサーション）
5. 検証コマンド（第 4 節）を実行し、実際の出力を PR に貼る
6. REQUIREMENTS_STATUS.md を同じ PR で更新
7. PR を作成（.github/pull_request_template.md を埋める）
8. CI 通過・指摘解消後にマージ。次の Issue はマージ後の main から開始
```

ブランチ名は Claude が `claude/...`、Codex が `codex/...` を使います。

### 実機確認が必要な項目の扱い

今回の要求の多くは **オフスクリーン CI では真偽を確定できません**（パン解放、
画面の揺れ、座標表示、DPI 依存のレイアウト）。次のように扱います。

- 実装 PR には **再現テスト（イベント列レベル）** を必ず入れる。合成イベントで
  回帰は防げます。
- そのうえで Issue に **「実機確認」チェックリスト** を置き、Windows 11 実機で
  確認した結果（日時・確認者・コミット SHA・操作手順・結果）を Issue にコメント
  として記録する。
- 実機確認が未了でも、合成テストが通り退行がなければマージしてよい。ただし
  PR の「Known limitations」に未確認である旨を明記する。
- 実機で再現しないと判断した項目は、Issue を閉じずに「実機で再現せず」を記録し、
  該当受け入れ条件を取り下げる。**CI が通ったことを実機確認の代替にしない。**

---

## 4. 検証コマンド

リポジトリ直下で実行します。

```text
python -m compileall -q hplc_app app.py
python scripts/ci_validate.py --offline-assets auto
set QT_QPA_PLATFORM=offscreen
python -m unittest discover -s tests -v
```

描画・操作系（イ・ウ・カ）を触った場合は追加で:

```text
python scripts/probe_pyqtgraph_parity.py
python scripts/benchmark_integrated_screen.py
```

Windows 7 の固定依存に触れた場合のみ:

```text
python scripts/windows7_import_preflight.py
python scripts/verify_windows7_wheelhouse.py
```

PR の Testing 欄には **実際に走らせたコマンドと結果だけ** を書きます。

---

## 5. 事前調査で判明した技術的事実

再調査の重複を避けるため、計画時点で確認できた実装状況を記録します。行番号は
`main` = `7a939ce` 時点のものです（`hplc_app/` は `7ba0db3` から変更なし）。
実装が進むとずれるので、行番号ではなく関数名で探してください。

### 5.1 「現状、何が Matplotlib 担当なのか？」

PyQtGraph はすでに画面の**見た目と入力**をほぼ担当できていますが、Matplotlib は
次の 5 つを持ち続けています。**既定化とは (1) を反転し、(2) の所有権を移す作業**です。

| # | 担当領域 | 実装位置 |
|---|---|---|
| 1 | **画面描画の既定** | `plot_stack` の既定ウィジェットは `MatplotlibScreenRenderSurface` の `FigureCanvasQTAgg`（`screen_renderer.py`）。PyQtGraph はチェックボックスによる**その回限りのオプトイン**で、設定に永続化されない（`gui.py:1120`, `_toggle_screen_preview` `gui.py:741`） |
| 2 | **画面状態の真実の源** | 軸・表示範囲・オーバービュー窓・履歴は Matplotlib の `self.axes` / `axes_right` / `axes_overview` / `axes_gradient` が保持。PyQtGraph 側は `refresh()` のたびに `owner._screen_view_state()` を読み取って追従するだけ（`screen_preview.py:166`） |
| 3 | **図の出力** | PNG / SVG / PDF・クリップボード・印刷は `_full_quality_export_figure()` が Matplotlib アーティストを完全再構築してから出力（`gui.py:929`）。A4 解析レポートは `report.py` の独立した Figure |
| 4 | **フォールバック** | Qt5（Windows 7）ビルド、PyQtGraph 未導入、描画例外時は Matplotlib へ復帰（`_stop_screen_preview` `gui.py:767`） |
| 5 | **一部の操作計算** | `SpanSelector` を使う積分／範囲編集／フラクションの従来経路と、`_reset_y_view` などの autoscale は Matplotlib の軸計算に依存（`gui.py:4343`） |

PyQtGraph 有効時、Matplotlib は**データアーティストを持たない骨組み**として維持され
（`_matplotlib_screen_complete = False`、`gui.py:2716`）、出力時だけ完全再構築されます。

**Windows 7 への含意**: PyQtGraph は Qt6 / PySide6 専用です。Windows 7 は PySide2 固定
（`AGENTS.md` の変更禁止事項）なので、**既定化は Qt6 ビルド限定**であり、Windows 7 では
Matplotlib が画面描画の既定として残ります。(3)(4)(5) はどちらの環境でも残ります。

### 5.2 「Y軸全体」が PyQtGraph で効かない理由（有力仮説）

`_reset_y_view()` (`gui.py:4343`) は `_plot(preserve_view=False)` を呼んで
Matplotlib の autoscale に Y 範囲を計算させます。しかし PyQtGraph 有効時の
Matplotlib figure は 5.1 のとおり**データ線を持たない骨組み**なので、autoscale の
入力が存在せず、既定範囲に落ちます。その結果が `_screen_view_state()` 経由で
PyQtGraph に適用されます。同じ理由で `_reset_view` も影響を受ける可能性があります。

対処方針: Y の全体範囲を Matplotlib の autoscale ではなく、`_screen_scene` の
トレースデータから背景非依存に算出する。

### 5.3 パン解放が認識されない理由（有力仮説 2 つ）

`PyQtGraphNavigationController.handle_event` (`pyqtgraph_navigation.py`) で、

- **仮説 A**: 解放イベントのボタンが 1 以外だと `self._pan` を解除せずに `return True`
  する分岐があります。ドラッグ中に別ボタンを押下・解放した、あるいは Qt から
  `NoButton` の解放が届いた場合、パンが掴まれたまま残ります。Matplotlib の
  `drag_pan` は解放ボタンを問わず終了するため、**この非対称性が「Matplotlib より
  多い気がする」という体感に一致**します。
- **仮説 B**: パン適用中（`self._apply(state)`）に例外が発生すると、
  `_dispatch_viewport_event` 経由で `eventFilter` から例外が抜け、`self._pan` が
  未解除のまま残ります。以後の移動で動き続け、次のクリックの解放でようやく
  解除される — 報告された症状と一致します。

対処方針: 解放は**ボタン種別を問わず**パンを終了させる。適用処理を
`try/finally` で囲み、例外時も `self._pan` を必ず解除する。合成イベント列
（press → move → 別ボタン release → move）で回帰テストを書く。

### 5.4 マウス座標表示が働いていない理由

`gui.py` の座標表示は Matplotlib の `motion_notify_event` 接続（`gui.py:1330`
`_on_canvas_motion`）に紐づいています。PyQtGraph 有効時はイベントが
`PyQtGraphSceneConsumer._dispatch_viewport_event` → `ExperimentalScreenPreview.handle_event`
へ流れ、Matplotlib のコールバックには到達しません。座標表示だけが未接続です。

対処方針: 座標表示を `ScreenPointerEvent`（`screen_events.py` の背景非依存契約）から
更新するようにし、両描画で同じ経路にする。

### 5.5 縦線ポインター設置時に画面が揺れる理由（要計測）

ポインター移動のたびに `_request_canvas_draw` → `_refresh_screen_preview` →
`ExperimentalScreenPreview.refresh()` が走り、`refresh()` は毎回、軸ペン・
ティックフォント・軸幅（`QFontMetricsF` による再計算）・軸ラベル・表示範囲を
すべて再適用します（`screen_preview.py:96-171`）。左軸幅やレイアウトが 1px 単位で
揺れるとプロット領域が動きます。

対処方針: ポインター更新はシーン再構築を伴わない軽量経路に分離する
（`set_pointer_cursor` だけを呼び、`refresh()` 全体を走らせない）。まず実機で
どの再適用が揺れの原因かを計測してから修正する。

### 5.6 「一括読み込み」が「作業ディレクトリ」に出ない理由（確定）

`import_directory()` (`gui.py:4458`) は選択ディレクトリを
`LAST_IMPORT_DIRECTORY` 設定に書くだけで、`project.work_directories` に
一切追加していません。`WorkDirectoriesDialog` はそのリストしか表示しないため、
一括読み込みしたディレクトリは管理画面に現れません。**仮説ではなく実装どおりの挙動**です。

### 5.7 .gcd の Run ID が unknown-datetime になる理由（確定）

`run_id_timestamp()` (`timestamps.py`) は `acquisition_timestamp()` の結果を使い、
それは `Sample Information.Acquisition Date` メタデータ → ファイル名の厳密パターン、
の順で解決します。一方 `parse_gcd_streams()` (`gcd_parser.py:292`) が生成する
metadata には取得日時のキーが**まったく含まれていません**。よって .gcd は常に
ファイル名フォールバックに落ち、そこも一致しなければ `unknown-datetime` になります。

対処方針: GCD の複合ファイルストリーム（`File Property` / `System` / その他）から
取得日時を読み出す。読み出せない形式では、既存の厳密なファイル名パターン、
最後にファイル更新時刻の順で解決し、**どの由来かをメタデータに記録**して
「日時を捏造していない」ことを保てるようにする。

### 5.8 ピークフィットは別ピークになっていない（確定）

`fit_peak()` の結果は既存 `PeakRegion` の `fit_model` / `fit_parameters` /
`fit_retention_time_min` / `fit_rmse_uv` / `fit_r_squared` / `fit_aic`
（`models.py:276-281`）に**上書き保存**されます。積分結果とフィット結果が同一行に
同居するため、利用者からは区別がつきません。要求どおり別ピークとして扱うには
スキーマ変更（親ピーク参照を持つ派生ピーク）と `project_io.py` の移行が必要です。

---

## 6. バッチ定義

各バッチの「対象ファイル」は着手時の見込みであり、増える場合は PR に理由を書きます。

---

### ア — 一括読み込みディレクトリの登録と GCD/TXT 重複解決

**Issue: #175** / **優先度: 最高**（優先事項 1 件を含む）

対象要求
- 「ディレクトリを一括読み込み」で読み込んだディレクトリが「作業ディレクトリを管理」から見えない
- 同一ディレクトリに `.txt` と `.gcd` が両方あるとき、`.gcd` を優先し `.txt` を無視する
- `.txt` しかないときは `.txt` を読み込む
- その仕様が利用者に分かるよう明記する

受け入れ条件
- [ ] 一括読み込みで選んだディレクトリが `project.work_directories` に登録され、
      「作業ディレクトリを管理」に表示される（ラベル初期値はフォルダ名）
- [ ] 同じディレクトリを再度一括読み込みしても重複登録されない
- [ ] 登録は Undo / Redo とプロジェクト保存・再読込をまたいで保持される
- [ ] 同一ディレクトリで拡張子だけが異なる同名ファイルは `.gcd` のみ取り込まれ、
      `.txt` は取り込まれない（比較は拡張子を除いたファイル名、大文字小文字非依存）
- [ ] `.txt` のみのファイルは従来どおり取り込まれる
- [ ] 再帰読み込み時は**同一ディレクトリ内でのみ**重複判定する（別フォルダの同名は別物）
- [ ] 読み込み結果ダイアログに「.gcd を優先し、同名 .txt を N 件スキップ」を表示
- [ ] 一括読み込みダイアログと `README.md` にこの優先規則を記載

対象ファイル: `hplc_app/import_batch.py`, `hplc_app/gui.py`, `hplc_app/dialogs.py`,
`README.md`, `tests/test_import_batch.py`, `tests/test_gui.py`

注意
- `discover_reload_candidates` は「作業ディレクトリ再読み込み」と共有されるため、
  優先規則は**両方の経路で同じ**にする。片方だけ変えない。
- SHA-256 による重複スキップは既存の別機構。今回の拡張子優先はその**手前**で行う。

---

### イ — PyQtGraph 操作系の実機不具合修正

**Issue: #176** / **優先度: 最高**（優先事項 2 件を含む）

対象要求
- マウス位置の座標を読み込む機能が働いていない
- パン操作でクリックを放しても認識されず、再度クリックしないと動き続ける
- 縦線ポインターを置いたときに画面が揺れる
- 「y軸全体」表示がうまくいかない
- スペクトル移動後に時間シフトと縦オフセットを 0 にしても元の位置に完全に戻らない

受け入れ条件
- [ ] PyQtGraph 表示中もマウス位置の座標が Matplotlib と同じ場所・同じ書式で更新される
- [ ] 分割表示・オーバービューでも、ポインタのあるパネルの座標系で表示される
- [ ] 左ボタン解放でパンが必ず終了する。**解放時のボタン種別に依存しない**
- [ ] パン適用中に例外が起きても掴んだ状態が残らない（`try/finally`）
- [ ] 合成イベント列（press → move → 別ボタン release → move）でパンが継続しない回帰テスト
- [ ] 縦線ポインターの移動でプロット領域の位置・サイズが変化しない
- [ ] 「Y軸全体」が PyQtGraph でも X 範囲を保ったまま Y 全体に合わせる
- [ ] 時間シフトと縦オフセットを 0 に戻すと、移動前とトレースが完全一致する
      （表示座標配列の比較で検証）
- [ ] 生データ配列の SHA-256 が全操作前後で不変

対象ファイル: `hplc_app/pyqtgraph_navigation.py`, `hplc_app/pyqtgraph_scene.py`,
`hplc_app/screen_preview.py`, `hplc_app/gui.py`, `tests/test_gui.py`

実機確認
- [ ] パンを 30 秒以上連続操作して掴まれ残りが起きない
- [ ] 縦線ポインターを左右に振って揺れがない
- [ ] 座標表示が実際のポインタ位置と一致する

---

### ウ — PyQtGraph を既定描画にし、Matplotlib の責務を明文化する

**Issue: #177** / **優先度: 最高**（優先事項 1 件を含む） / **依存: #176（イ）**

対象要求
- pyqtgraph 表示をデフォルトにしたい
- 現状、何が matplotlib 担当なのかを明確にしたい
- ~~環境設定から「描画品質」を削除してよい~~ → **保留**（第 8 節を参照）

受け入れ条件
- [ ] Qt6（Windows 11）ビルドでは PyQtGraph が既定の画面描画になる
- [ ] 選択は設定として永続化され、再起動後も保たれる
- [ ] PyQtGraph が使えない場合（未導入・初期化失敗・Qt5）は自動的に Matplotlib へ
      フォールバックし、その理由が画面に表示される
- [ ] Windows 7 / PySide2 ビルドは従来どおり Matplotlib が既定（**退行させない**）
- [ ] チェックボックスの文言から「実験的・今回のみ」を外し、描画方式の切替として整理
- [ ] `README.md` と `REQUIREMENTS_STATUS.md` に Matplotlib の残担当
      （出力・レポート・印刷・フォールバック・Win7 画面）を明記
- [ ] プロジェクトファイルのスキーマと数値計算は不変

対象ファイル: `hplc_app/gui.py`, `hplc_app/dialogs.py`, `hplc_app/settings_store.py`,
`hplc_app/screen_preview.py`, `README.md`, `REQUIREMENTS_STATUS.md`, `tests/test_gui.py`

> **「描画品質」の削除は保留（利用者判断、2026-08-30）**
> 「描画品質（高品質／軽量）」の軽量モードは、**PyQtGraph が動かない Windows 7 /
> Core 2 機のために存在する**画面専用の間引きです（`_is_lightweight_rendering`,
> `gui.py:2219`）。Windows 7 実機での検証が済んでいないため、この設定は**現状のまま
> 残します**。本バッチで削除・自動化のいずれも行わないでください。
> Windows 7 実機検証の結果が出たあとに、別 Issue として再検討します。

実機確認
- [ ] 起動直後から PyQtGraph で表示される
- [ ] PNG / SVG / PDF / A4 レポート / 印刷が従来品質のまま出力される

---

### エ — 読み込み時の初期値整備（Run ID・縦軸・グループ）

**Issue: #178** / **依存: #175（ア）**

対象要求
- `.gcd` の Run ID 初期値が `unknown datetime` になる。`.txt` と同様に適切にラベルする
- ディレクトリパスに `ch1` があれば第 1 縦軸、`ch2` があれば第 2 縦軸に割り当てる
- グループの初期値は空白でよい

受け入れ条件
- [ ] `.gcd` から取得日時を読み出し、`.txt` と同じ書式の Run ID を生成する
- [ ] 読み出せない場合は「厳密なファイル名パターン → ファイル更新時刻」の順で解決し、
      **由来をメタデータに記録**する。どこからも得られないときのみ `unknown-datetime`
- [ ] 日時を推測で捏造しない（`AGENTS.md` の科学的挙動規約）
- [ ] 元ファイルのパスに `ch1` を含む場合は Y1、`ch2` を含む場合は Y2 を初期値にする
      （大文字小文字非依存、両方含む・どちらも含まない場合は従来どおり Y1）
- [ ] 割り当ては読み込み時の初期値のみ。既存プロジェクトの再読込では変更しない
- [ ] 新規データセットのグループ初期値が空文字になる
- [ ] 既存 `.hplcproj` を読み込んでも Run ID・縦軸・グループが書き換わらない

対象ファイル: `hplc_app/gcd_parser.py`, `hplc_app/timestamps.py`, `hplc_app/models.py`,
`hplc_app/gui.py`, `hplc_app/project_migrations.py`, `tests/test_core.py`, `tests/test_gui.py`

注意
- GCD の取得日時ストリームは仕様が公開されていません。`scripts/inspect_gcd.py` で
  実ファイルのストリームを調べ、**確実に読める場合のみ**採用してください。
  不確実な解釈で日時を作らないこと。

---

### オ — クロマトグラム表と下部ボタンメニューの再構成

**Issue: #179** / **依存: #178（エ）**

対象要求
- 各要素をマウスドラッグで並び替えられるようにする
- 既定の列順: Run ID, ラベル, 波長, 縦軸, AU/V, 色, カラム, 時間シフト, 縦オフセット, 元ファイル
- タイムスタンプ列は不要
- 表示のチェックボックスに加えて選択のチェックボックスも付ける
- 下部ボタンを見出し付きで整理する
  - 「データ」: 読み込み・削除
  - 「解析」: 詳細・定量条件・グラジエント・一括入力・スペクトル・同一runへ・runから分離（この順）
  - 「スペクトル表示」: すべて表示・非表示・上へ・下へ

受け入れ条件
- [ ] 列ヘッダーのドラッグで列順を変更できる
- [ ] 変更した列順が設定として永続化され、再起動後も保たれる
- [ ] 既定の列順が指定どおりになる
- [ ] タイムスタンプ列が表から削除される（**データは保持**し、詳細情報や凡例からは
      引き続き参照できること）
- [ ] 表示用チェックボックスとは独立した選択用チェックボックスがあり、
      複数選択がチェックだけで完結する
- [ ] 選択チェックボックスと既存の行選択（Shift / Ctrl）が相互に同期する
- [ ] 下部ボタンが 3 つの見出し付きグループに分かれ、「解析」は指定順に並ぶ
- [ ] 既存のドラッグによる**行**並び替え（`rowMoveRequested`）が壊れていない

対象ファイル: `hplc_app/gui.py`, `hplc_app/settings_store.py`, `hplc_app/i18n.py`,
`tests/test_gui.py`

注意
- 列インデックス定数（`DATASET_*_COLUMN`, `gui.py:182-195`）を直接参照している箇所が
  多数あります。定数の付け替えではなく、**論理列 ID → 表示位置** の対応表を導入して
  参照側を壊さない形にしてください。

---

### カ — 「マウスモード」の新設と積分エリアのクリック選択

**Issue: #180** / **依存: #177（ウ）**

対象要求
- 「マウスモード」を新設し、縦線ポインターの複数選択や積分エリアのクリック認識などを割り当てる
- クロマトグラム上の積分エリアをクリックすると積分区間がセレクトされる
- フラクション範囲はマウスではなく数値入力で指定する

受け入れ条件
- [ ] マウス操作の役割を選ぶ「マウスモード」UI があり、現在のモードが明示される
- [ ] 少なくとも「通常（パン／ズーム）」「縦線ポインター」「積分エリア選択」を選べる
- [ ] 縦線ポインターモードで複数のポインターを選択でき、まとめて削除できる
- [ ] 積分エリア選択モードで積分済み領域をクリックすると、その積分区間が選択され、
      ピーク表の該当行も選択される
- [ ] 重なった積分エリアをクリックした場合の選択規則が決まっている（最前面・最小面積など）
- [ ] フラクション範囲が数値入力（開始・終了）で追加・編集でき、マウスドラッグ経路は
      整理される
- [ ] モード切替で既存のツール（積分・範囲編集・分割）と競合しない
- [ ] Matplotlib 描画でも同じモードが機能する（できない項目は明示的に無効化し理由を表示）

対象ファイル: `hplc_app/gui.py`, `hplc_app/screen_preview.py`, `hplc_app/dialogs.py`,
`hplc_app/i18n.py`, `tests/test_gui.py`

注意
- `AGENTS.md`「既存のキーボード／マウス機能を、新しい操作の実装過程で削除しない」。
  フラクションのマウス経路を**削除**する前に、数値入力が同等以上であることを確認する。

---

### キ — 積分データの明示化と積分リストの別ウィンドウ表示

**Issue: #181** / **依存: #180（カ）**

対象要求
- ピークフィットされたものを明示的に別ピークとして扱う
- 積分リストを別ウィンドウで表示する機能

受け入れ条件
- [ ] フィット結果が元の積分ピークとは別の行として**既定で表示され**、由来（親ピーク）が分かる
- [ ] 元の積分ピークの数値（面積・保持時間・FWHM・%Area）はフィットによって変化しない
- [ ] フィット由来ピークであることが表・凡例・エクスポートで区別できる
- [ ] フィット由来ピークを個別に削除・再計算できる
- [ ] 積分リストを別ウィンドウで開ける。メインウィンドウの表と選択・編集が双方向に同期する
- [ ] 別ウィンドウを閉じてもデータが失われない
- [ ] スキーマ変更に対して `project_io.py` / `project_migrations.py` に移行を追加し、
      **既存 `.hplcproj` が引き続き読み込める**
- [ ] 旧バージョンで保存し直した場合に何が失われるかを `REQUIREMENTS_STATUS.md` に記録

対象ファイル: `hplc_app/models.py`, `hplc_app/peak_fitting.py`, `hplc_app/analysis.py`,
`hplc_app/project_io.py`, `hplc_app/project_migrations.py`, `hplc_app/gui.py`,
`hplc_app/dialogs.py`, `hplc_app/exporters.py`, `tests/test_core.py`, `tests/test_gui.py`

注意
- **今回の要求で唯一スキーマに触るバッチ**です。`AGENTS.md` のプロジェクト互換規約
  （フィールドを黙って改名・削除しない、移行を明示する）を厳守してください。
- **表示既定は決定済み（利用者判断、2026-08-30）**: フィット由来ピークは既定で表に
  出します。切替表示にはしません。フィット由来であることは列で示してください。

---

### ケ — 自動ピーク検出の範囲選択と感度 3 段階

**Issue: #183** / **依存: #180（カ）** / バッチ「ク」の前提

対象要求
- 「自動ピーク検出」を押した後にダイアログを表示し、全範囲を対象にするか、
  マウスで選んだ範囲を対象にするかを選ばせる
- 検出感度をその場で 3 段階（低・中・高感度）から選べるようにする
- その 3 段階の閾値を環境設定から確認・編集できるようにする
- それぞれについて「デフォルトに戻す」ボタンを保持する

受け入れ条件
- [ ] 「自動ピーク検出」を押すとダイアログが出て、検出せずにキャンセルできる
- [ ] 検出対象範囲を「全範囲」「マウスで選んだ範囲」から選べる
- [ ] 範囲が未選択のまま「マウスで選んだ範囲」を選ぶことはできない（無効化または警告）
- [ ] 感度を「低・中・高」から選べ、選んだ段階の閾値で検出される
- [ ] 環境設定で 3 段階それぞれの閾値（既存 8 項目）を確認・編集できる
- [ ] 3 段階それぞれに「デフォルトに戻す」があり、その段階の既定値に戻る
      （押しただけでは適用されず、OK で確定する）
- [ ] 検出範囲を限定したとき、範囲外の既存ピークが削除・変更されない
- [ ] 検出結果が Undo / Redo の 1 ステップとして扱われる
- [ ] 生データ配列が全操作前後で不変

対象ファイル: `hplc_app/gui.py`, `hplc_app/dialogs.py`, `hplc_app/analysis.py`,
`hplc_app/models.py`, `hplc_app/preset_store.py`, `hplc_app/i18n.py`,
`tests/test_core.py`, `tests/test_gui.py`

注意
- **これは科学的挙動の変更です**（`AGENTS.md`）。既存の検出アルゴリズムは変えず、
  入力パラメータの選び方と適用範囲だけを変えてください。同じ閾値・同じ範囲なら
  結果がこれまでと一致することをテストで示すこと。
- `detect_peaks(dataset, method)` (`analysis.py:402`) には時間範囲の引数がありません。
  範囲を任意引数として追加し、**省略時は現在と同じ全範囲**にしてください。
- 3 段階の閾値は**アプリ全体の設定**として保存します（`AGENTS.md`「永続的な全体設定を
  個別プロジェクトに入れない」）。既存の `auto_peak_*` 8 項目は `Method`
  (`models.py:484-491`) にあり、プロジェクトに保存されつつ環境設定から編集される
  という既存の不整合があります。**この不整合を今回まとめて直そうとしないこと。**
  3 段階のプリセットを新たにアプリ設定として持ち、選択結果を従来どおり `Method` に
  適用する形が、最も影響範囲の小さい既定案です。
- 「デフォルトに戻す」の既定値は単一の出所（`Method` のデータクラス既定値、または
  そこから導いたアプリ既定）から取り、ダイアログ側にハードコードしないでください。

---

### ク — 表示・レポートの小改修

**Issue: #182** / **依存: #177（ウ）, #180（カ）, #183（ケ）** / 最終バッチ

対象要求
- スペクトル移動後に元に戻す「移動を元に戻す」ボタンを追加
- 「%Bの凡例にクロマトグラム名を表示」を「凡例設定」に移動
- 「凡例設定」と「軸・ラベル設定」のボタンを少し小さくして 1 行に横並びにする
- 解析レポートで、クロマトグラムの X 軸が積分リストにかぶっている配置を調整する

受け入れ条件
- [ ] 「移動を元に戻す」で、選択トレースの時間シフトと縦オフセットが 0 に戻る
- [ ] 元に戻した結果が Undo / Redo の 1 ステップとして扱われる
- [ ] `gradient_legend_include_name` が「凡例設定」ダイアログ内に移り、
      元の場所からは削除される（設定値は移行され、既存プロジェクトで値が変わらない）
- [ ] 「凡例設定」「軸・ラベル設定」ボタンが 1 行に横並びで収まる
- [ ] A4 解析レポートでクロマトグラムの X 軸ラベルと積分リストが重ならない
- [ ] レポートの他要素（見出し・条件表・継続ページ）のレイアウトが崩れない

対象ファイル: `hplc_app/gui.py`, `hplc_app/dialogs.py`, `hplc_app/report.py`,
`hplc_app/i18n.py`, `tests/test_gui.py`, `tests/test_core.py`

注意
- 「自動ピーク検出のデフォルト値に戻すボタン」は当初この節にありましたが、同じ
  ウィジェット群を全面的に作り直すバッチ「ケ」(#183) に移しました。本バッチでは
  環境設定の自動ピーク検出グループに触れないでください。
- レポートは `report.py:341-343` の `add_gridspec(4, 1, height_ratios=(0.42, 0.95, 3.8, 2.65))`
  と `subplots_adjust(hspace=0.3)` が支配しています。クロマトグラム行の下余白を
  増やす方向で調整し、A4 1 ページに収まることを出力で確認してください。

---

## 7. 完了条件（全バッチ共通）

各 Issue は次がすべて満たされたときに閉じます。

1. 受け入れ条件のチェックがすべて埋まっている（取り下げたものは理由が書かれている）
2. 第 4 節の検証コマンドが通り、結果が PR に記録されている
3. `REQUIREMENTS_STATUS.md` が更新されている
4. 実機確認項目について、確認済みか「未確認」の明記があるか、どちらかである
5. PR がマージされ、`origin/main` に含まれている

## 8. 未決定事項

| 項目 | 内容 | 判断者 |
|---|---|---|
| マウスモードの初期モード | カ節。起動時の既定を「通常」とするかどうか | 実装時に既定「通常」で進め、実機確認で調整 |
| 感度 3 段階の具体的な閾値 | ケ節。低・中・高それぞれの 8 項目の値 | 実装時に現行既定を「中」として提案し、実機の検出結果で調整 |

### 決定済み（2026-08-30）

| 項目 | 決定 |
|---|---|
| 環境設定の「描画品質」削除 | **保留**。Windows 7 実機での検証が未了のため、設定は現状のまま残す。バッチ「ウ」の範囲から外した |
| フィット由来ピークの表示既定 | **既定で表に出す**。切替表示にはしない（バッチ「キ」） |
