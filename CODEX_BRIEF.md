# Codex 指示書 — Windows 11 実機フィードバック対応

Updated: 2026-09-10（2 巡目コ・サ #313, #314 を追加発注／#307-#311 と同時進行）rev.25

この文書は**今回の作業の発注書**です。規約は持ちません。規約は `AGENTS.md`、
バッチの詳細は `WIN11_FEEDBACK_WORKFLOW.md` にあります。

---

## 1. 最初にやること

```bash
git fetch origin main
git log --oneline -1 origin/main
```

そのうえで、この順に読んでください。

1. `AGENTS.md` — リポジトリ規約。**最上位。この指示書がこれを緩めることはありません**
2. `REQUIREMENTS_STATUS.md` — 実装済み／残作業の台帳
3. `VALIDATION_BLOCKERS.md` — 外部・物理検証の未達ゲート
4. `WIN11_FEEDBACK_WORKFLOW.md` — **バッチ定義・受け入れ条件・事前調査結果**
5. 担当する Issue の本文
6. GitHub の Open Issue / Open PR — 重複着手の防止

`WIN11_FEEDBACK_WORKFLOW.md` に、計画時にコードを読んで特定した根本原因が
書いてあります。**すべて確定で、必要なものは実際に計測した数値も載せてあります。
同じ調査を最初からやり直さないでください。**

| 回 | 事前調査 | バッチ定義 | 未決定事項 |
|---|---|---|---|
| 2026-09-02（#236-#242） | 第 9 節（9.1〜9.18） | 第 10 節 | 第 11 節 |
| 2026-09-03（#249-#252） | 第 12 節（12.0〜12.10） | 第 13 節 | 第 14 節 |
| 2026-09-06（#267-#271） | 第 15 節（15.1〜15.14） | 第 16 節 | 第 17 節 |
| 2026-09-08（#280-#282） | 第 18 節（18.0〜18.8） | 第 19 節 | 第 20 節 |
| 2026-09-09 朝（#290-#293） | 第 21 節（21.1〜21.9） | 第 22 節 | 第 23 節 |
| 2026-09-09 夕（#301, #302） | 第 24 節（24.1〜24.5） | 第 25 節 | 第 26 節 |
| **2026-09-10 朝（#307-#311）** | **第 27 節（27.1〜27.13）** | **第 28 節** | **第 29 節** |
| **2026-09-10 夕（#313, #314）** | **第 30 節（30.1〜30.9）** | **第 31 節** | **第 32 節** |
| **2026-09-09（#301-#302）** | **第 24 節（24.1〜24.5）** | **第 25 節** | **第 26 節** |

第 5 節から第 8 節は完了したタ行・ナ行の記録で、今回の作業には使いません
（ただし第 3 節の手順と第 4 節の検証コマンドはどの回もそのまま使います）。

**覆された決定が 4 つあります。** 2026-09-02 のズームの決定（第 12.0 節）、
#218 の「フィット行の %Area と定量値は空欄」（第 15.12 節）、#218 の
「フィット面積はモデル曲線の全面積」（第 18.4 節）、そして今回の **#218 の
「飽和していないピークには警告を出すか実行しない」**（第 21.5 節・ン #291）です。

**かなの記号が一巡しました。** ア行からワ行までを使い切ったので、**ヲ・ン**
に続けて **2 巡目のア行（ア²・イ²）**、その次の **ウ²・エ²** まで使いました。
次のバッチは **オ²** から続けてください。

### Windows 11 実機ゲートが 5 件合格しました（2026-09-08）

`.github/WIN11_ACCEPTANCE_TEST.md` のセッション 0〜4 が実機で実行され、5 ゲートすべて
合格と報告されました。`VALIDATION_BLOCKERS.md` の該当行は更新済みです。

**今回の 7 件の要求はゲートの失敗ではなく、そこから出た改善要求です。**
証跡（`.github/WIN11_ACCEPTANCE_EVIDENCE.md`）には実施者しか持たない値の `要記入`
欄が残っており、Stable リリースの記録として使う前に埋める必要があります。
**この欄を推測で埋めないでください**（`AGENTS.md`）。

---

## 2. 担当と順序

**#175 から #302 までのバッチはすべて完了済みです。**

**今回のバッチは 2 巡目オ〜ケ（#307-#311）です。** 13 件の実機フィードバックを
5 つの Issue にまとめてあります。

| 記号 | Issue | 内容 | 優先 | 依存 |
|---|---|---|---|---|
| オ² | #307 | 「全体+拡大」の仕上げ（ズーム基点・スクロールバー位置・X 目盛・余白） | 高 | なし |
| カ² | #308 | 凡例の外側配置と枠線・塗りつぶし、保持時間ラベルの縦書き | 高 | なし |
| キ² | #309 | ダブルクリックの多段戻りと面積の桁区切り | 中 | なし |
| ク² | #310 | 画面表示設定（アスペクト比・dpi）と積分欄の整理 | 中 | なし |
| ケ² | #311 | 元ファイル表示の作り直しとプリセット内容の編集 | 中 | なし |

さらに **2 巡目コ・サ（#313, #314）**が追加されました。8 件の要求です。

| 記号 | Issue | 内容 | 優先 | 依存 |
|---|---|---|---|---|
| サ² | #313 | フラクション一覧ダイアログ、操作欄の整理、フラコレ領域の表示切り替え | 高 | なし |
| コ² | #314 | レポートの目盛設定の反映とフラクション回収範囲の出力 | 中 | #313 |

**今すぐ着手できるのは #307-#311 と #313 です。** #314 は #313 のマージ後に
始めてください。

### ⚠️ #313 はスキーマに触ります

「フラコレ領域を表示」のチェックボックスを足すため、**`AnalysisMethod` に永続
フィールドを 1 つ**加えます（第 30.8 節）。`project_io.py` /
`project_migrations.py` に移行を入れ、**既存 `.hplcproj` が「表示する」として
読める**ことをテストしてください（現在は常に描かれているので、既定 `True` が
現状維持です）。**#314 はスキーマを触りません。**

### ⚠️ #313 の「表示」欄は 2 列にすると欄が広がります。省略には仕掛けが要ります

**「表示」欄のチェックを 2 列に並べると、欄の最小幅が 264 px → 409 px、窓の
最小幅が 1486 px → 1631 px に広がります**（第 30.9 節の実測）。**利用者判断で、
収まらないときは右端を `…` で省略します**（2026-09-10）。

**素直にやっても省略されません。** `QCheckBox` に省略の機能はなく、
**描画だけ省略しても欄は縮みません** — 横の `QSizePolicy` が `Minimum` で、
レイアウトが `sizeHint()` を下限として扱うためです。**`Preferred` へ変える
ところまでが 1 組**です（実測で欄の最小幅が 409 px → 277 px）。

**下限を「`…` 1 個ぶん」にしないでください。** 幅が余っていても「積…」まで
縮みます。**一番短いラベルが収まる幅**を既定案にしています。

**英語 UI はもっと厳しい**です（`Show integration ranges / baselines` は
日本語の 2.5 倍の 420 px）。**言語で分けないこと。**
**`LeftElideDelegate` は左を省略する別物です。使い回さないでください。**

### ⚠️ #310 と #313 は同じ `integration_controls` を触ります

**#310**（自動ピーク検出・ピークフィット・飽和ピーク補正を横一列）と
**#313**（積分リスト窓とフラクション一覧のボタンを横一列、欄名の変更）は、
**同じレイアウトの別の行**を触ります。**同時に走らせないでください。**
後にマージする方が rebase し、**最終的な行構成を PR に書いてください。**

### ⚠️ カ²(#308) は今回唯一スキーマに触ります

`AnalysisMethod` に**凡例の枠線と塗りつぶしの永続フィールドを 2 つ**足します。
`project_io.py` / `project_migrations.py` に移行を入れ、**既存 `.hplcproj` が
枠無し・塗りつぶしなしとして読める**ことをテストしてください。
`AGENTS.md` のプロジェクト互換規約を厳守すること。

**他の 4 バッチはスキーマを触りません。** とくに ク²(#310) のアスペクト比と dpi は
既存の `figure_width_mm` / `figure_height_mm` / `dpi` に落とせます（第 27.11 節）。

### 重なるファイル

**並行数の目安は 2 つまで。**

| ファイル | 触る Issue |
|---|---|
| `pyqtgraph_scene.py` / `screen_preview.py` | #307, #308 |
| `gui.py` | #307, #308, #309, #310 |
| `dialogs.py` | #308, #310, #311 |
| `models.py` / `project_io.py` / `project_migrations.py` | #308 |

**#307 と #308 を同時に走らせないでください。** どちらも同じ描画経路を触ります。
**#310 と #311 は別クラスなので並行できます**が、後にマージする方を rebase して
ください。**#307 と #309**、あるいは **#308 と #311** の組み合わせが安全です。

### 実機で先に確認が要る項目が 2 つあります

**#307 の「余白が残る」**（第 27.4 節）: エ²(#302) で「行の取り分を 0 にする」修正が
入ったのに、実機ではまだ残っています。行 0 / 仕切り行 / スクロールバーの帯の
3 つが候補です。**スクロールバーを外へ出す修正を先に入れて、それでも残るかを
確認するのが最短です。**

**#309 の「1 段しか戻らない」**（第 27.5 節）: `ScreenViewHistory.navigate("back")` は
素直に読めば繰り返し戻れる作りです。**履歴の記録側が疑わしい**ので、
実機で `history.count` と `history.position` の遷移を記録してから直してください。

### 3 回目の修正が 1 つあります

**#311 の「元ファイル」の省略は、ヘ(#239) / リ(#270) に続く 3 回目です。**
オフスクリーンで 9 通りの幅を実測したところ、**デリゲートが返す文字列はどの幅でも
正しく**、2 度目の右省略も「収まっていれば無害」でした。壊れるのは
**デリゲートが測った矩形と `drawControl` が使う矩形が食い違う幅のときだけ**です。

**2 つの矩形が一致することに頼るのをやめてください。** 背景と選択状態だけ style に
描かせ、**文字は自分で `painter.drawText` してください。** 安全余白を引くだけの
対処は当て推量になるので勧めません。**今度は幅に依存しない形にしてください。**

前回までのバッチから残っているのは、実機確認（#224 ほか）と
`VALIDATION_BLOCKERS.md` の物理ゲートです。

---

## 3. 1 Issue の進め方

```bash
git fetch origin main
git checkout -B codex/<短い説明> origin/main
```

1. `WIN11_FEEDBACK_WORKFLOW.md` の該当バッチ節と Issue 本文を読む
2. 受け入れ条件を作業チェックリストに写す
3. 実装する。`AGENTS.md` の「最小で首尾一貫した変更」を守る
4. テストを追加・更新する。**受け入れ条件 1 件につき最低 1 アサーション**
5. 検証コマンド（第 4 節）を走らせる
6. `REQUIREMENTS_STATUS.md` を同じ PR で更新する
7. `.github/pull_request_template.md` を埋めて PR を作る
8. CI 通過・指摘解消後にマージする。次の Issue はマージ後の `main` から始める

ブランチ名は `codex/...` を使ってください（Claude は `claude/...` を使います）。

**1 Issue = 1 ブランチ = 1 PR = 1 マージ。** 複数の Issue を 1 つの PR にまとめないで
ください。逆に、1 つの Issue を複数 PR に割らないでください。

---

## 4. 検証

リポジトリ直下で実行します。

```bash
python -m compileall -q hplc_app app.py
python scripts/ci_validate.py --offline-assets auto
QT_QPA_PLATFORM=offscreen python -m unittest discover -s tests -v
```

描画・操作系（レ #280 / ハ #236 / ヒ #237 / マ #241）を触った場合は追加で:

```bash
python scripts/probe_pyqtgraph_parity.py
python scripts/benchmark_integrated_screen.py
```

レポートの体裁（ロ #281）と 3D の出力（ワ #282）は、**実際に書き出したファイルを
開いて確認してください。** テストが通ることと A4 1 ページに収まることは別です。
ロ(#281) はピークが 1 件・数件・多数の 3 通りで見ること。

Windows 7 の固定依存に触れた場合のみ:

```bash
python scripts/windows7_import_preflight.py
python scripts/verify_windows7_wheelhouse.py
```

**PR の Testing 欄には、実際に走らせたコマンドと結果だけを書いてください。**
走らせていないチェックに印を付けないこと（`AGENTS.md`）。

### 実機確認が必要な項目

ちらつき・マウスモードの効き・分割バーの操作感・印刷プレビューは、
**オフスクリーン CI では真偽が確定しません**。

- 合成イベント列（press → move → release など）の回帰テストは**必須**です
- そのうえで、実機確認は Issue のチェックリストに残し、結果を Issue にコメントする
- 実機確認が未了でもマージしてよいが、PR の「Known limitations」に明記する
- **CI が通ったことを実機確認の代替にしないでください**

**これは実例のある注意です。** パン解放は イ(#176) / PR #188 で修正済みと判断して
マージしましたが実機では直っておらず、ス(#201) で作り直しました。合成テストが
通ることと実機で直っていることは別です。

「ちらつかない」は人間の目だけで確認せず、**シーン再構築回数かアイテム同一性**で
固定してください。セ(#203) とカ行の既存テストが参考になります。

### CI の Windows ジョブがまれにハングします

**`GuiTests.test_preview_peak_range_target_and_cancellation_guards` が無言で
止まり、ジョブが 25 分の `timeout-minutes` で cancelled になる事象を #283 で
観測しました。** 詳細と観測値は `WIN11_FEEDBACK_WORKFLOW.md` 第 18.8 節にあります。

- **落ちるのではなく、出力が止まったまま戻ってきません。** テスト失敗とは
  見た目が違います
- 同一コミットの再実行で解消しました。通常の所要は 11〜13 分です
- **止まっているのは レ(#280) が触る `screen_preview` の span-drag 経路です。**
  #280 の作業中に同じ症状が出たら、**まず自分の変更を疑ってください。**
  第 18.8 節の記録は「無関係だと決めつけてよい」という意味ではありません
- **再実行は 1 回だけ。** 2 回目も同じ位置で止まったら実在するハングです。
  再実行を重ねて緑を引き当てないこと（`AGENTS.md`）

---

## 5. 特に注意してほしいこと

### 変えてはいけないもの

- **Windows 7 の固定依存**（Python 3.8.10 x86 / NumPy 1.20.3 / PySide2 5.15.2.1 /
  Matplotlib 3.7.5 ほか）。明示的な依頼なしに追加・更新・緩和しないこと。
  NumPy 1.24.4 は実機で `0xc000001d` 起動失敗の実績があります
- **オフラインビルド可能性**
- **数値計算**。積分・保持時間・FWHM・%Area・単位換算の結果が変わったら、それは
  意図した変更でない限りバグです
- **既存の `.hplcproj` の読み込み**。永続フィールドを黙って改名・削除しないこと

### 判断が要るときは Issue にコメントする

各 Issue に「要判断」節がある場合、**既定の実装方針を書いてあります**。それで進めて
構いません。異なる判断が必要だと思ったら、進める前に Issue にコメントしてください。

### 今回スキーマに触るのは カ²(#308) だけ

**カ²(#308) は `AnalysisMethod` に凡例の枠線と塗りつぶしの永続フィールドを 2 つ
足します。** `project_io.py` / `project_migrations.py` に移行を入れ、**既存
`.hplcproj` が枠無し・塗りつぶしなしとして読める**ことをテストしてください。
`AGENTS.md` のプロジェクト互換規約を厳守すること。

**他の 4 バッチは触りません。** とくに ク²(#310) のアスペクト比と dpi は、既存の
`figure_width_mm` / `figure_height_mm` / `dpi` に落とせます（第 27.11 節）。

### （記録）これまでのバッチはスキーマに触りませんでした

**#280-#282 の 3 バッチはいずれも `.hplcproj` の形式を変えません。**
既定案でスキーマを触らずに済むよう設計してあります。

- レ(#280) の上部の表示範囲は既存の `_overview_full_x` を動かすだけ。
  `ScreenOverviewState` にフィールドを足さない
- ロ(#281) は面積の**計算方法**を変えるのであって、保存する項目は変わらない。
  レポートの表の高さは描画時の値で、永続化しない
- ワ(#282) の軸表示 ON/OFF は `ThreeDPlotOptions`（セッション限り）に足す。
  ミ(#242) の決定どおり `.hplcproj` には保存しない

（記録）#236-#242・#249-#252・#267-#271 の 16 バッチもスキーマを変えていません。

- ユ(#267) の上下比は保存しない。Y2 のオーバービュー矩形は既存の
  `overview_secondary` に載せた
- リ(#270) の単独表示は表示上の一時状態。`dataset.visible` も書き換えない
- ル(#271) の `area_percent` / `amount_nmol` / `amount_ug` は `PeakRegion` に既にある

- モ(#251) の既定表示モードは、保存済みプロジェクトが自分の値を持ち続けるので
  移行が要らない（第 12.7 節）
- モ(#251) のチャンネル配色は既存の `dataset.y_axis` を使う。新しいフィールドを
  足さない（第 12.6 節）
- ム(#249) のオーバービュー矩形は `ScreenOverviewState` に y を足さずに描ける
  （第 12.4 節）
- ヤ(#252) の量の 2 列は `PeakRegion.amount_nmol` / `amount_ug` が既にある

- マ(#241) の線の太さは、既にある `Method.line_width` に UI を付けるだけ
- マ(#241) の目盛の下限は **0.1 分**。入力欄の単位も永続フィールド
  （`x_major_tick_min` / `x_minor_tick_min`）の単位も分のままで、秒に変えない
- ミ(#242) の 3D グリッドは `ThreeDPlotOptions`（セッション限り）のまま
- ホ(#240) のトレースごとの太さは**やりません**（線種と違い、スキーマ変更になる）
- フ(#238) は保存済みの `fit_parameters` を移行しない（再計算で更新されると明記する）

スキーマを変える必要が出てきたら、それは設計を見直す合図です。**進める前に Issue に
コメントしてください。**

### 数値が変わるのは ロ(#281) だけ

ロ(#281) は通常のフィット行の面積を、**親の積分区間の中だけ**で数えるように変える
ものです。`AGENTS.md` の科学的挙動の規約が正面から効きます。

- ベースラインは**既に親と同じ**。書き換えないこと（第 18.4 節）
- 変えるのは積分区間。現在は EMG で `center ± 8σ + 20τ` の広いグリッドまで数えている
- 親の積分ピークの**生の値は変えない**
- **推定であることの表示は維持する**（表・凡例・エクスポート・レポート）
- 飽和ピーク補正（#271）の面積定義は変えない。今回の変更で通常のフィット行が
  そちらに揃う

**数値が変わるので、テストを先に書いて固定してください。** 代表的なピークで変更
前後の値を測って PR に載せ、#218 の判断を覆す理由も書くこと。

レポートの表の上詰め（第 18.5 節）は見た目だけの変更で、数値は変わりません。
**A4 1 ページに収まることを実出力で確認してください。**

### （記録）数値が変わったのは ル(#271)

ル(#271) は飽和ピーク補正の結果を **%Area と定量値まで届かせる**変更でした。
面積の定義は **「測定値＋飽和部の埋め戻し」**（積分範囲の中だけを数える）で確定して
います。精度の限界を隠さないこと。非飽和のフランクが足りないと 20% 近く外す実測が
あります（第 15.12 節）。

### （記録）表示単位が変わったのは ヤ(#252)

ヤ(#252) はレポートの面積を mAU·sec から **µV·sec** に変えます。**値そのものは
既存フィールド（`raw_area_uv_sec`）をそのまま出すので、計算は変わりません。**
`%Area` が動かないことをテストで固定してください。

**ヤ(#252) はレポートの表から「種別／親」列を消します。** そこは #218 の成果である
「推定値が測定値と区別できる」がレポートの表で成立している唯一の場所です。
**列は消してよいが、推定であることの表示は表のどこかに必ず残してください。**

### 数値が変わったのは フ(#238)

フ(#238) は飽和ピーク補正の**復元値そのものを直す**変更です。`AGENTS.md` の
科学的挙動の規約が正面から効きます。

- 生の取り込み値は変更しない
- 推定値を測定値と同じ見た目で出さない（ナ #218 で入れた区別を退行させない）
- **数値が変わるので、テストを先に書いて固定する**
- 通常（飽和していない）フィットの値がどれだけ動くかを測り、PR に書く

原因は `WIN11_FEEDBACK_WORKFLOW.md` の第 9.5 節に確定しています。**EMG プロファイルが
渡された `x` の上の最大値で正規化しているため、頂点を除外して当てはめると
`amplitude_uv` が頭打ちの値になります。** 現状と修正後の実測値も載せてあります。

**探索の中で密なグリッドを毎回作らないでください。実測で 2 分を超えました。**

**名称は「飽和ピーク補正」のままです**（`Saturated peak correction`）。
他のバッチは表示・操作・体裁の変更で、測定値も導出値も変わりません。

### 保留中の項目

**「環境設定から描画品質を削除する」は保留です。** Windows 7 実機での検証が
済んでいないため、この設定は現状のまま残します。ウ(#177) の範囲から外してあるので、
`_render_quality` 周辺と環境設定の該当グループには手を触れないでください。

### 個人名を書かない

公開を想定したリポジトリです。**追跡対象ファイル・コミットメッセージ・Issue・PR の
いずれにも、研究室メンバーの氏名を書かないでください**（`AGENTS.md`）。

3D 表示（`hplc_app/plot3d.py`）には、研究室内で使われていた Google Colab の
ノートブックという原典がありますが、由来に触れる必要があるときは「Google Colab の
ノートブックで作っていた図」のように**作業内容で説明**してください。作成者を指す
記述は入れないこと。原典のノートブックと試験データもリポジトリには追加しません。

`THIRD_PARTY_NOTICES.txt` は第三者ソフトウェアのライセンス表記専用です。ここに人名を
足さないでください。

### 3D 表示に Plotly を持ち込まない

3D 表示（`hplc_app/plot3d.py`）は Matplotlib の `mpl_toolkits.mplot3d` で実装
されています。Plotly / kaleido は**導入しないでください**。Windows 7 の固定依存に
触れますし、既存のエクスポート経路が Matplotlib Figure 前提です。

### 既にあるものを作り直さない

今回の要求はほとんどが既存機能の改善です。新しい仕組みを並行して作らないで
ください。

**今回のバッチ（#280-#282）:**

- 上部画面のパン（レ #280）は、下部と**同じ `axis_pan_view` 経路**を使う。
  上部専用のパン計算を別に書かない（第 18.2 節）
- スクロールバー（レ #280）は `_overview_scrollbar_changed` を**繋ぎ替える**。
  いま `"detail"` アクションを発行しているところを `_set_overview_x` にする。
  つまみを別に作らない（第 18.1 節）
- 上部の操作割り当ては、ユ(#267) で入れたクリック／範囲ジェスチャと衝突します。
  **どのボタン・どのモードで何が起きるかを表にして PR に書いてください**（第 18.3 節）
- レポートの表（ロ #281）は `_add_peak_table` の `bbox` を直す。表の描画を作り直さ
  ない。**続きページ側の呼び出しにも同じ扱いが要ります**（第 18.5 節）
- 3D の軸表示 ON/OFF（ワ #282）は、既存の `ThreeDPlotOptions` に**既定値つきで項目を
  足す**。既定は現在の見た目（すべて表示）
- 画面と出力の一致（ワ #282）は、**プレビューを出力と同じ縦横比・同じ実効サイズで
  描く**。`figure_width_mm` / `figure_height_mm` から figure サイズを決め、canvas 側を
  縮小表示にする。**フォントサイズを倍率で補正する案は採りません**（第 18.7 節）。
  補正係数が増えるほど一致の保証が弱くなり、`bbox_inches="tight"` の切り詰めまでは
  補正できません
- `_save_static_figure` は **2D の図と共有**です。触る場合は **2D の出力が退行しない
  ことを確認**してください

**前回まで（記録）:**

- マウスモード（ハ #236）は、カ(#180) の仕組みに上部バーとの連動を足すのであって、
  新しいモード管理を作るのではない。`_mouse_mode_changed` のモード ID →
  コントロール対応表が、そのまま上部バーのアイコン一覧になる
- 上部バーのアイコン（ハ #236）は**3 つ目の操作経路**であって、置き換えではない。
  グループ欄のモード用ボタンは残し、プルダウン・アイコン・ボタンの 3 者を同期させる
- 矩形ズーム（ハ #236）は、`screen_preview` に素性の良い実装が既にある。
  **アイコンを消しても機能を消さないこと。** `removeAction()` しても
  `toolbar._actions["zoom"]` の `QAction` は辞書に残るので、門番の判定は変えずに済む
- 積分ハイライト（ヒ #237）は、セ(#203) の「作り直さずに restyle する」経路を直す。
  **`_plot()` を呼ぶ解決にしない**
- Run ID の改名（ヘ #239）は、`Project.rename_run` を**ダイアログから呼ぶ**。
  検証の並行実装を作らない
- 元ファイルの省略表示（ヘ #239）は、`gui.py` の `LeftElideDelegate` を
  `dialogs.py` へ移して共有する。**同じデリゲートを 2 つ書かない**
- 選択チェックの同期（ホ #240）は、オ(#179) の仕組みを使う
- 配色の解決順（ホ #240）は `dataset_display_color` の「明示色 → 波長既定 →
  パレット」を変えない
- 線の太さ（マ #241）は、既にある `Method.line_width` に UI を付けるだけ
- 目盛の下限と本数制限（マ #241）は、ト(#217) がフラクションで入れた
  「下限 + 本数の上限 + 事前警告」の形に揃える。**下限は 0.1 分**（利用者判断）
- 列インデックス定数（ホ #240）は振り直さない。オ(#179) の論理列 ID → 表示位置の
  対応表を使う

### 機能を勝手に消さない

ホ(#240) で「データ」欄から外す「詳細・定量条件」「グラジエント」は**移動であって
削除ではありません**。行き先は チ(#214) が「測定・試料条件の入力」ダイアログに入れた
「選択行の詳細設定」「選択行のグラジエント」です（第 9.13 節で確認済み）。
**どこから使えるようにしたかを PR に書いてください。**

ホ(#240) の「すべて表示」「すべて非表示」ボタンも残してください。複数行への一括
操作を足すのであって、既存のボタンを置き換えるのではありません。

---

## 6. 止まってよいとき

次の場合は、次の Issue に進まずに報告してください（`AGENTS.md`）。

- テストが落ちている
- PR がマージできない
- レビュー指摘が未解決
- 科学的・移行・セキュリティ・利用者データに関わるリスクが未解決

**問題を積み残したまま次の Issue を重ねないでください。**
