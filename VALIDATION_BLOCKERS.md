# External blockers and deferred physical validation

Updated: 2026-08-31

This file records work that source changes and automated CI cannot complete alone. An unavailable physical or external gate is `BLOCKED — evidence required`; it is never treated as passed or silently marked not applicable. Release execution remains governed by `.github/RELEASE_PROCESS.md` and `.github/RELEASE_CHECKLIST.md`.

Version context: latest published Stable is `1.2.4`; current main development line is `1.3.0-dev.1`. No `1.3.0` tag or Release exists yet.

## Current hard blockers

| Area | Status | Why source work cannot finish it | Evidence or decision required |
|---|---|---|---|
| Public code-signing identity | BLOCKED — external account and identity required | Authenticode can be probed in code, but the project has no approved production certificate, subject, thumbprint, or timestamp service | Select Microsoft Artifact Signing where available, otherwise a public-CA OV Authenticode certificate; complete organization/identity verification; record expected signer subject/thumbprint and timestamp policy |
| Updater installer launch | BLOCKED — signing identity and signed fixture required | Download, SHA-256, and Authenticode foundations exist, but launch is intentionally always disabled | Verify a signed installer and manifest produced by the release pipeline; pin the approved signer identity; approve the user-confirmed launch workflow |
| Stable GitHub Release publication | BLOCKED — human release approval required | Tags and Releases are external publication actions and require completed evidence | Human approval of the exact verified commit, annotated tag, final assets, checksums, release notes, and signing status |

## Mandatory physical release gates not yet satisfied for the current candidate

| Gate | Current status | Required evidence |
|---|---|---|
| Windows 11 x64 installer | BLOCKED — evidence required | Build the installer from the candidate commit; clean install, launch, import, save, figure/report output, upgrade, uninstall, reinstall, and protected-data byte comparison |
| Windows 7 SP1 x86/Core 2 offline build | BLOCKED — physical hardware required | Build without network access on physical Windows 7 SP1 32-bit/Core 2; dependency isolation, normal/debug startup smoke tests, installer, import, save, and figure output |
| Installer upgrade/data preservation on both OS targets | BLOCKED — installers and machines required | Complete `.github/INSTALLER_UPGRADE_TEST.md` and one `.github/INSTALLER_UPGRADE_EVIDENCE.md` record per OS |
| Win11 ↔ Win7 project portability | BLOCKED — both OS builds required | Open/save representative projects in both directions and record schema/migration/display results |
| Physical print path | BLOCKED — printer evidence required | Print current view and A4 analysis report through a real Windows printer driver; confirm the chromatogram X-axis label remains separated from the peak table and record page layout and clipping results |
| Windows 11 PyQtGraph default desktop view | BLOCKED — native desktop evidence required | Validate default startup, persisted renderer switching, Japanese fonts, DPI scaling, pointer devices, both Y axes/overview/split panels, fallback reasons, clipboard and physical print on Windows 11. Confirm the compact legend/axis button row and trace-position reset controls remain readable at native DPI; offscreen font metrics and captures do not replace this evidence |
| Automatic peak detection on laboratory chromatograms | BLOCKED — real-data and physical UI evidence required | On representative quiet, noisy, and crowded chromatograms, confirm Low/Medium/High produce meaningfully different candidate counts, mouse-selected detection stays inside the intended displayed range after time shift, existing outside integrations remain intact, and the three Preferences tabs/reset actions are usable on Windows 11 and Windows 7 |
| Final Release asset re-download | BLOCKED — published draft assets required | Download installer/offline kit/`SHA256SUMS.txt` into a clean directory and run the documented checksum verification |

## Automated or offscreen evidence already available

These results reduce implementation risk but do not replace the physical gates above.

- Issue #182 passed all 315 tests (174 Windows/PySide6 offscreen GUI and 141 non-GUI), syntax compilation, local source contracts, the Windows 7 offline manifest/wheelhouse check, and the 11-feature PyQtGraph parity probe. Coverage verifies selected-trace-only time-shift/offset reset, exact display restoration, one-step Undo/Redo, unchanged raw arrays and other traces, the existing persisted B% legend-name field inside the legend-composer dialog, compact one-row legend/axis and movement controls, and a rendered A4 first-page gap between the chromatogram X label and a full 20-row peak table. A rendered 45-peak report retained its title, conditions, 20-row first page and 25-row continuation page. The synthetic integrated benchmark reduced 800,000 source points to 25,824, retained trace/static identities and equal before/after raw SHA-256, and measured 0.129 s native versus 0.128 s legacy median. No project schema, numerical algorithm, automatic-peak Preferences setting, or dependency pin changed. Physical Japanese/DPI presentation and printer-driver output remain required.

- Issue #183 passed all 313 tests (173 Windows/PySide6 offscreen GUI and 140 non-GUI), syntax compilation, local source contracts, and the Windows 7 offline manifest/wheelhouse check. Coverage verifies cancel-without-detection, unavailable-range disabling, reversed selected-range normalization, display-to-raw time-shift conversion, Low/Medium/High threshold application, three independently editable/resettable eight-field presets, application-wide persistence, one-step Undo/Redo, preservation of existing outside peak definitions, identical omitted/explicit-full numerical results, constrained boundaries, and unchanged raw arrays. Deterministic synthetic data produced 1/2/3 candidates for the default Low/Medium/High presets. No project schema or dependency pin changed. Meaningful separation on representative laboratory chromatograms and physical Win11/Win7 dialog presentation remain required.

- Issue #181 passed 169 Windows/PySide6 offscreen GUI tests and 138 non-GUI tests, syntax compilation, local offline source contracts, the 11-feature PyQtGraph parity probe, and the integrated 800,000-point benchmark with equal before/after raw-array SHA-256 (0.128 s native versus 0.130 s legacy median). Schema 107 keeps integrations in `peaks`, stores explicit parent-linked fit results in `fitted_peaks`, migrates legacy parent `fit_*` results without changing area/retention/FWHM/%Area, and mirrors the latest child for older v1 readers. Coverage includes default rows, parent identity, denominator exclusion, independent recalculate/delete, parent-split cleanup, main/detached list synchronization and close safety, save/reload, old-schema migration, Matplotlib/PyQtGraph legends, CSV, and PDF report output. No dependency pin changed and the offline Win7 asset validation passed. Physical PySide2 dialog presentation and Win11↔Win7 schema-107 round trips remain required; an older application re-save discards explicit child IDs/links/notes and all but the last mirrored fit per parent.

- Issue #178 passed all 292 tests (157 Windows/PySide6 offscreen GUI and 135 non-GUI). Known PACsolution `File Property` version 2.32.00 reads its confirmed Windows FILETIME field; unknown versions are not interpreted and instead use strict filename then file-modification-time fallback. Every chosen path records its provenance, and the unambiguous FILETIME UTC value remains in source metadata while display/Run ID uses the importing OS timezone. New imports assign Y2 only when parent directories contain `ch2` without `ch1`, keep the measurement group blank independently of the work-directory label, and preserve existing project Run ID/Y-axis/group values without a schema change. The local read-only verifier matched acquisition timestamps for all 6 untracked GCD/TXT pairs (101,750 GCD points total), while retention times, rounded intensities and 231 vendor peak records retained their prior equality. Physical Windows 11 import/Run-ID display with the laboratory timezone, real `ch1`/`ch2` directory layout, and Win11↔Win7 project portability remain required.
- Issue #177 passed all 290 tests (157 Windows/PySide6 offscreen GUI and 133 non-GUI), syntax compilation and local offline source contracts. Qt6 now starts with the PyQtGraph screen renderer by default, persists the application-level PyQtGraph/Matplotlib selection across restarts, keeps backend-neutral view state authoritative, and falls back to Matplotlib with visible reasons for missing/failed PyQtGraph or Qt5/Windows 7. Matplotlib continues to own PNG/SVG/PDF, clipboard, print, report rendering, Win11 fallback and the Win7/PySide2 screen. The Win11 package check found Python 3.11.9 x64, PySide6 6.8.3, Matplotlib 3.10.1, NumPy 2.2.3 and PyQtGraph 0.13.7. The parity probe reports all 11 features supported. The synthetic integrated benchmark reduced 800,000 source points to 26,256, retained trace/static identities and equal before/after raw SHA-256, and measured 0.130 s native versus 0.130 s legacy (1.001x legacy/native ratio). Project schema, numerical calculations, Windows 7 dependency pins and rendering-quality controls are unchanged. Physical default-startup, renderer-switch, DPI/font/device, output and print evidence remains required.
- Issue #176 passed all 288 tests (155 Windows/PySide6 offscreen GUI and 133 non-GUI), syntax compilation and local offline source contracts. Coverage verifies panel-correct toolbar coordinates for Y1/Y2/overview and the shared Matplotlib event path, repeated native pointer motion without preview refresh or plot-geometry change, press/move/other-button-release/move pan termination, pan cleanup after apply failure, scene-derived native Y-full bounds with X/B% preservation, and exact detail/overview array restoration after trace shift/offset return to zero. Raw time/intensity SHA-256 remains unchanged. The parity probe reports all 11 features supported. The synthetic integrated benchmark reduced 800,000 source points to 26,256, retained trace/static identities and equal before/after raw SHA-256, and measured 0.129 s native versus 0.296 s legacy (2.30x legacy/native ratio). Physical 30-second pan, pointer-device/DPI jitter, coordinate readability and Y-full checks remain required.
- Issue #168 extends the integrated MainWindow benchmark to production file/directory discovery and GCD/TXT parsing, deterministic de-duplication, explicit per-file parse errors, actual trace/point/suffix counts and raw-array digest invariance. Decorated tracked ASCII data (3 traces, 162,004 points) measured 0.078 s native versus 0.138 s legacy (1.76x). Local user-owned paired GCD/TXT data (6 GCD + 6 corresponding TXT traces, 203,500 points) measured 0.118 s versus 0.199 s (1.68x); six non-chromatogram instrument sidecar TXT files were reported and skipped. Both retained trace/static reuse and equal before/after raw SHA-256. No raw files or generated result JSON are tracked. It passed 151 Windows/PySide6 offscreen GUI tests and 131 non-GUI tests, and local offline source contracts; physical desktop/device/print evidence remains open.
- Issue #166 passed 150 Windows/PySide6 offscreen GUI tests and 130 non-GUI tests. It extends native trace reuse across gradients, peak overlays, markers, fraction regions and text annotations. Semantically identical static scenes retain their graphics identities; changed static content is replaced independently, removals clear stale item/hit-target maps, and trace topology changes still perform a full safe rebuild. The decorated Windows/Python 3.11/Qt 6 offscreen benchmark at 8 traces × 100,000 points retained both trace/static identities, reduced 800,000 source points to 26,256 display points, preserved raw SHA-256 and measured 0.391 s native versus 0.364 s legacy (legacy/native ratio 0.931). This improved the pre-static-reuse native median of 0.476 s by about 18%, but remains about 7% slower than legacy. Coverage includes unchanged/changed/removed overlays, stale-hit cleanup, identity stability, topology rebuild and raw-array invariance; syntax compilation and local offline source contracts passed. Representative real-data and physical performance evidence remain required.
- Issue #164 passed 150 Windows/PySide6 offscreen GUI tests and 130 non-GUI tests. It adds native screen-only peak-preserving min/max reduction and stable detail/overview trace-item reuse for compatible trace-only refreshes, with safe reconstruction on added/removed/reassigned traces or overlay topology. Its integrated Windows/Python 3.11/Qt 6 offscreen benchmark at 8 traces × 100,000 points reduced 800,000 source points to 26,256 rendered points, reused trace identities, retained equal before/after raw SHA-256, and measured 0.147 s native versus 0.299 s legacy (legacy/native ratio 2.04). This is about 13.5x faster than Issue #162's 1.989 s native median. Coverage verifies identity stability, updated values/style/labels, topology rebuild, no duplicates, peak preservation and raw-array invariance; syntax compilation and local offline source contracts also passed. Physical desktop and overlay-rich real-data performance remain open.
- Issue #162 passed 149 Windows/PySide6 offscreen GUI tests and 130 non-GUI tests. It removes hidden Matplotlib data/overlay/annotation/legend construction during the opt-in preview while retaining axes/view state. Complete high-quality Matplotlib artists are rebuilt only for PNG/SVG/PDF export and for disable/failure fallback, then the active preview returns to its skeleton. Coverage includes zero hidden lines, native scene evidence, export reconstruction, fallback restoration and unchanged raw arrays; syntax compilation and local offline source contracts also passed. The integrated Windows/Python 3.11/Qt 6 offscreen benchmark at 8 traces × 100,000 points recorded zero Matplotlib data lines and equal before/after SHA-256, but native full-scene recreation was slower: 1.989 s median versus 0.301 s for the legacy path (legacy/native ratio 0.151). Native item reuse/view-aware reduction remains source work; no default-renderer or physical acceptance claim is made.
- Issue #160 passed 149 Windows/PySide6 offscreen GUI tests and 130 non-GUI tests. Native axis-title and tick-label family/size/color, manual/automatic X major/minor spacing, all six legend locations and legend family/size/color are covered in single and split views. Outside-right uses an unclipped layout column, and the existing `primary.legend` consumer contract remains available. Syntax compilation and local offline source contracts passed. Schema, dependencies, scientific calculations, Win7 behavior and defaults remain unchanged; native-desktop DPI/font/print acceptance remains open.
- Issue #158 passed 148 Windows/PySide6 offscreen GUI tests and 130 non-GUI tests. Both Matplotlib toolbar configuration entries now reuse the persisted axes/label-style dialog without leaving the opt-in preview; accept, cancel, Undo, dirty state, native refresh and unchanged legacy-toolbar routing are covered. Syntax compilation and local offline source contracts passed. Schema, dependencies, scientific calculations, Win7 behavior and defaults remain unchanged; exact native-desktop presentation and release gates remain open.
- Issue #156 passed 147 Windows/PySide6 offscreen GUI tests and 130 non-GUI tests. Native rectangle zoom supports X, Y, X+Y and automatic modes, split-panel Y1/Y2 ownership, shared Home/Back/Forward history, click-only rejection and Escape/focus/view/backend cancellation while the opt-in preview remains active. Syntax compilation and local offline source contracts passed. Schema, scientific calculations, dependencies, Win7 behavior and defaults remain unchanged; physical DPI/font/pointer/printing and cross-OS gates remain open.
- Issue #154 passed 146 Windows/PySide6 offscreen GUI tests and 130 non-GUI tests. Native Y1/Y2 text labels support fixed-size rendering, item-bound hit testing, drag movement, click creation, double-click edit/delete and marker-hit precedence while reusing the existing dialog, model, Undo/Redo and persistence. Coverage includes real Qt drag input, Escape/focus/view/backend and renderer-failure rollback, project round trip, SVG export and unchanged raw arrays. Syntax compilation and local offline source contracts passed. Schema, dependencies and defaults remain unchanged; physical DPI/font/pointer/printing and cross-OS gates remain open.
- Issue #152 passed 144 Windows/PySide6 offscreen GUI tests and 130 non-GUI tests. Native trace movement matches the existing X shift/Y offset callback for X/Y/both directions in single/overview/split layouts, with live detail/overview feedback and selected-trace/panel guards. Tests cover wrong buttons/targets, markers, click-only gestures, Escape/focus/outside/selection/direction/mode/view/backend cancellation, renderer and peak-recalculation failure rollback in both renderers, one-step Undo/Redo, table refresh, project round trip, SVG export and unchanged raw arrays. Syntax compilation and local offline source contracts passed. Numerical algorithms, schema, dependencies and defaults remain unchanged; physical desktop pointer/DPI, cross-OS and printing gates remain open.
- Issue #150 passed 141 Windows/PySide6 offscreen GUI tests and 130 non-GUI tests. Native peak splitting matches the existing nearest-valid-sample calculation across all five baseline modes, time shifts/display offsets, repeated child splits and single/overview/split layouts. Tests cover parent baseline/group/notes/source preservation, wrong panels, hidden/deselected/replaced/removed targets, navigation/double-click suppression, invalid/narrow ranges and atomic rollback after recalculation failure in both renderers, one-step Undo/Redo, project round trip, SVG export and unchanged raw arrays. Syntax compilation and local offline source contracts passed. Numerical algorithms, schema, dependencies and defaults remain unchanged; physical desktop, cross-OS and printing gates remain open.
- Issue #148 passed 138 Windows/PySide6 offscreen GUI tests and 130 non-GUI tests. Native existing-peak range editing matches existing recalculation for all five baseline modes with time shifts/display offsets, forward/reverse drags and single/overview/split layouts. Tests cover peak ID/settings/notes preservation, changed/removed/deselected targets, wrong panels, cancellation, invalid-range rollback, one-step Undo/Redo, project round trip, SVG export and unchanged raw time/intensity arrays. Syntax compilation and local offline source contracts passed; Meiryo offscreen drag/committed images were visually checked. Numerical algorithms, schema, dependencies and defaults remain unchanged. Physical desktop, cross-OS and printing gates remain open.
- Issue #146 passed 135 Windows/PySide6 offscreen GUI tests and 130 non-GUI tests. Native manual integration matches the existing calculation exactly for all five baseline modes with time shifts/display offsets, forward/reverse drags and single/overview/split layouts. Coverage includes selected-trace/panel guards, cancelled target changes, insufficient-point warnings without edits, one-step Undo/Redo, project save/reload, SVG output, unchanged raw arrays and retained fraction behavior. Syntax compilation and offline source contracts passed; Meiryo offscreen drag/committed captures were visually checked. Numerical algorithms, schema and dependencies are unchanged; physical desktop, cross-OS and print gates remain open.
- Issue #144 passed all 262 tests (132 Windows/PySide6 offscreen GUI and 130 non-GUI). Coverage includes readable/unknown-time IDs, project-local uniqueness, whole-Run rename without implicit grouping, no-op/error paths in Japanese/English, label/time/reorder stability, numbering after delete/group/split/Undo/Redo/save/reload, legacy-ID migration, unchanged raw data/peaks and SVG legends. Python 3.8 grammar, pinned dependencies and local Windows 7 offline manifest/wheelhouse checks passed. Schema 106 adds only the project numbering counter; existing IDs are retained. The installed standalone Python 3.8 x86 interpreter lacks NumPy, so no legacy-runtime or physical cross-OS round trip is claimed. These remain release gates; older applications may discard the new counter when re-saving.
- Issue #142 passed 130 Windows/PySide6 offscreen GUI tests and 124 non-GUI tests. Native fraction selection covers real Qt forward/reverse drags in single/overview/split layouts, transient feedback without model mutation, one-step Undo/Redo, interval preservation, clear/save/reload/SVG output and unchanged raw arrays/peaks. Cancellation covers Escape, focus/viewport loss, resize, invalid/click-only gestures, navigation/tool/selection/layout changes and renderer failure. SVG export is also checked for swallowed callback errors after disconnecting hidden Matplotlib fraction selectors. Syntax compilation and offline source contracts passed; Meiryo offscreen drag/committed captures were visually inspected. These do not satisfy physical desktop/device or print gates.
- Issue #140 passed 128 Windows/PySide6 offscreen GUI tests and 124 non-GUI tests. Both renderers now test B% on/off for both split panels together, selected-gradient changes/missing data, shared B% scale, one legend entry, save/reload, SVG output, raw-array invariance and Matplotlib overlay pointer ownership. Syntax compilation and `scripts/ci_validate.py --offline-assets required` passed. A Meiryo offscreen capture confirmed B% curves/scales on both panels with aligned time axes. Physical desktop/printing and the existing lower-control sizing gate remain open.
- The non-GUI suite passed 124 tests for the Issue #138 candidate from the repository root, including B% range preservation during shared axis pan, the root-layout and workflow contracts, normalized backend-neutral overview full/detail-window state, bounded home/back/forward history, axis-pan state/calculations, immutable pointer coordinates and hit targets, optional-renderer isolation/pinning, backend-neutral scene contracts, update metadata, project migrations, numerical invariance, import, presets, and release-source contracts.
- The Windows/PySide6 offscreen GUI suite passed 126 tests for Issue #138. New split-panel coverage exercises native Y2 wheel/drag with Y1 and B% ranges preserved, aligned shared X, B% host migration, pane-specific vertical-marker hover/place/hit testing, Undo/Redo, raw-array invariance, layout switching/history, font changes, repeated refresh/resize, hidden traces and renderer replacement failure cleanup. Prior native-marker save/reload, MainWindow snapshot/export, fallback and default-Matplotlib regressions also pass. This is not physical mouse/device validation.
- Issue #138 passed syntax compilation and `scripts/ci_validate.py --offline-assets required`, including the locally supplied Windows 7 manifest/wheelhouse. A split-view MainWindow PNG with B% on Y2 was visually inspected with local Meiryo explicitly registered; a deferred-layout axis-width mismatch was corrected and the aligned result rechecked. Native desktop fonts/DPI, lower-control sizing and physical printing remain open as recorded above. No installer or physical OS/device validation was performed.
- The Win11 isolated renderer benchmark measured about 2.25x improvement for the high-density PyQtGraph case; the parity probe reported all 11 representative features supported, including independent Y2 and B% axes and a 1000x700 snapshot.
- Windows 7 dependency pins, offline-build scripts, installer definitions, manifests when present, and source contracts are automatically checked. GitHub CI does not contain the ignored third-party offline payload and does not produce a Windows 7 executable.
- Historical Windows 7/Core 2 evidence established that NumPy 1.24.4 fails with `0xc000001d` and NumPy 1.20.3 starts. This supports the existing pin but is not evidence for the current candidate installer.

## Work that can continue without these blockers

- Release asset discoveryとfail-closed signer-policy基盤は実装済み。production signer thumbprintの設定はidentity取得後に継続し、installer launchはそれまで無効のまま維持する。
- Updater download progress/cancel/error presentation is implemented with a background Qt worker and synthetic fixtures, without offering execution. Connecting it to the end-user action remains blocked on the approved signer identity and signed release fixture.
- The PyQtGraph MainWindow renderer is now the Qt6/Windows 11 default, with an application-level persisted switch to Matplotlib and explicit automatic-fallback reasons. Backend-neutral view state owns screen navigation; Matplotlib artists are still created on demand for high-quality figure/clipboard/print/report output and fallback, while Qt5/Windows 7 remains Matplotlib-only. Native trace/static reuse and peak-preserving screen reduction have synthetic, tracked ASCII and local paired GCD/TXT integrated evidence. Native desktop default-startup, compatibility, long-session interaction, output and physical print evidence are the remaining renderer gates.
- Application-level features, tests, documentation, and project migrations that preserve the pinned Windows 7 dependency set.
- Release checklist dry runs that create no tag or public Release; `scripts/release_evidence_preflight.py` now distinguishes malformed records from explicit dry-run blockers and rejects both for Stable publication.

## Evidence handoff

When evidence becomes available, record the date/time, verifier, OS edition/architecture, hardware class, commit SHA, application/dependency versions, artifact filename and SHA-256, exact procedure, and result. Link the evidence Issue or Release draft here and change only the corresponding row; do not infer completion from a related automated test.
