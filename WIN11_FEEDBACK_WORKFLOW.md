# Windows 11 実機フィードバック 対応ワークフロー

Updated: 2026-09-05 (マ行後半・ヤ行) rev.9 — #236-#242 実装完了

Windows 11 実機での動作確認によって提出された修正要求を、Claude と Codex の
どちらが担当しても同じ手順・同じ粒度で実装できるようにするための計画文書です。

この文書は **要求の割り当てと進め方だけ** を定義します。リポジトリ全体の規約
（Windows 7 互換、プロジェクト互換、科学的挙動、Git 運用）は `AGENTS.md` が
引き続き上位にあり、この文書がそれを緩めることはありません。

---

## 0. 読む順序（Claude / Codex 共通）

作業を始める前に、必ずこの順に確認します。

1. `AGENTS.md` — リポジトリ規約（最上位）
2. `REQUIREMENTS_STATUS.md` — 実装済み／残作業の唯一の台帳
3. `VALIDATION_BLOCKERS.md` — 外部・物理検証の未達ゲート
4. **この文書** — 今回のバッチ定義と実施順
5. `CODEX_BRIEF.md` — 着手可能な Issue と手順の要約
6. GitHub の Open Issue / Open PR と最新の `origin/main` — 重複着手の防止

`CLAUDE.md` はこの一覧への入口であり、内容を持ちません。

---

## 1. バッチ方針（Issue を切りすぎない）

Issue #110〜#173 は 1 Issue = 1 関数レベルまで細分化されており、AI の
コンテキスト再構築コストが実装コストを上回っていました。以降は次の方針です。

- **1 Issue = 1 つのまとまった利用者価値**。同じモジュール・同じテストファイルを
  触る要求は 1 つの Issue にまとめる。
- 1 Issue の目安は **「変更ファイル 1〜4、要求項目 2〜6 件」**。要求 1 件だけの
  Issue は、他に同居させられる要求がない場合に限る。
- **1 Issue = 1 ブランチ = 1 PR = 1 マージ**（`AGENTS.md` の既存規約を維持）。
- Issue を分けるのは、**モジュールが違う／依存順が違う／片方が実機確認待ちで
  ブロックされうる** 場合だけ。「作業が多いから」は分割理由にしない。

### バッチ名（かな行）

要求群を五十音の行で呼びます。1 行あたり最大 5 バッチです。

| 行 | バッチ | 状態 |
|---|---|---|
| **ア行** | ア・イ・ウ・エ・オ | 完了（#175, #176, #177, #178, #179） |
| **カ行** | カ・キ・ク・ケ・コ | 完了（#180, #181, #182, #183, #185） |
| **サ行** | サ・シ・ス・セ・ソ | 完了（#200, #201, #202, #203, #204） |
| **タ行** | タ・チ・ツ・テ・ト | 完了（#213, #214, #215, #216, #217） |
| **ナ行** | ナ・ニ・ヌ | 完了（#218, #219, #221） |
| **ハ行** | ハ・ヒ・フ・ヘ・ホ | 進行中（#236, #237, #239, #240）／フ(#238) は完了 |
| **マ行** | マ・ミ | 進行中（#241, #242） |
| **マ行**（後半） | ム・メ・モ | **今回のバッチ**（#249, #250, #251） |
| **ヤ行** | ヤ | **今回のバッチ**（#252。マ行に収まらない分） |

GitHub の Issue タイトルは既存リポジトリの慣習に合わせて英語、本文は日本語で
書きます。本文冒頭に `Batch: タ` のように行記号を明記します。

---

## 2. 実施順と依存関係

> **今回（2026-09-03、ム・メ・モ・ヤ）のバッチ定義は第 13 節、事前調査は
> 第 12 節、未決定事項は第 14 節にあります。**
>
> ハ行・マ行（#236-#242、2026-09-02）は第 9〜11 節、完了したタ行・ナ行は
> 第 2〜8 節の記録です。手順（第 3 節）と検証コマンド（第 4 節）はどの回も
> そのまま使います。
>
> **第 11 節の「上部バーと Pan / Zoom → 1 アイコンに統合」は 2026-09-03 に
> 覆されました。第 12.0 節を先に読んでください。**

タ行・ナ行では 28 件の要求を 8 バッチにまとめました。

```
タ(#213) ──┬─► チ(#214)        （表 → 同じダイアログの拡張）
           └─► ヌ(#221)        （表 → 「表示設定」ダイアログの中身）
ツ(#215)                       （独立）
テ(#216) ──► ト(#217)          （画面系。同じファイルを触るため直列）
ナ(#218)                       （独立）
ニ(#219)                       （独立）
```

| 記号 | Issue | 内容 | 優先 |
|---|---|---|---|
| タ | #213 | ホーム画面の表と操作ボタンの整理 | 高 |
| チ | #214 | 「測定・試料条件の入力」ダイアログの拡張 | 高 |
| ツ | #215 | 分析対象物プリセット | 中 |
| テ | #216 | 表示の安定化と表示リセットの意味 | 高 |
| ト | #217 | 選択モードの統合と縦線・フラクションの整理 | 中 |
| ナ | #218 | フィット結果の面積表示と飽和ピーク補正 | 中 |
| ヌ | #221 | 「表示設定」ダイアログ（線種とグラジエント配色） | 中 |
| ニ | #219 | 解析レポートの印刷プレビューと出力名 | 低 |

**タ行・ナ行の実装はすべてマージ済み**です。残るのは実機確認と物理ゲートだけです。

- **タ → チ**: タ が「一括入力」を「測定・試料条件の入力」に改名し、「元ファイル」を
  そのダイアログに移します。チ はその同じダイアログを拡張します。逆順にすると
  改名分をやり直すことになります。
- **タ → ヌ**: タ が「スペクトル」を「表示設定」に改名します。ヌ はその
  ダイアログの中身（線種・配色）を作ります。チ と ヌ は別のダイアログなので、
  タ のマージ後は並行できます。
- **テ → ト**: どちらも `screen_preview.py` と `gui.py` の画面系を触ります。
  並行させると衝突します。**テ を先に**。表示の安定化は利用者への影響が大きく、
  ト の選択モードもその上に乗るためです。
- **ツ・ナ・ニ は独立**。ツ は試料情報ダイアログとプリセット、ナ は
  `peak_fitting.py`、ニ は `report.py` が中心です。

> **並行数の目安は 2 つまで。** `gui.py` はほぼすべてのバッチが触ります。3 つ以上を
> 同時に走らせると rebase が連鎖します。先にマージされた方に他方を rebase して
> ください。その旨は PR に書くこと。

---

## 3. 1 Issue あたりの手順

```
1. git fetch origin && git checkout -B <branch> origin/main
2. この文書の該当バッチ節を読み、受け入れ条件を Issue のチェックボックスに写す
3. 実装（AGENTS.md の「最小で首尾一貫した変更」を維持）
4. テスト追加・更新（受け入れ条件 1 件につき最低 1 アサーション）
5. 検証コマンド（第 4 節）を実行し、実際の出力を PR に貼る
6. REQUIREMENTS_STATUS.md を同じ PR で更新
7. PR を作成（.github/pull_request_template.md を埋める）
8. CI 通過・指摘解消後にマージ。次の Issue はマージ後の main から開始
```

ブランチ名は Claude が `claude/...`、Codex が `codex/...` を使います。

### 実機確認が必要な項目の扱い

今回の要求の多くは **オフスクリーン CI では真偽を確定できません**（ちらつき、
マウスモードの効き、分割バーの操作感、印刷プレビュー）。次のように扱います。

- 実装 PR には **再現テスト（イベント列レベル・状態レベル）** を必ず入れる。
  「ちらつかない」はシーン再構築回数やアイテム同一性で固定できます。
- そのうえで Issue に **「実機確認」チェックリスト** を置き、Windows 11 実機で
  確認した結果（日時・確認者・コミット SHA・操作手順・結果）を Issue にコメント
  として記録する。
- 実機確認が未了でも、合成テストが通り退行がなければマージしてよい。ただし
  PR の「Known limitations」に未確認である旨を明記する。
- 実機で再現しないと判断した項目は、Issue を閉じずに「実機で再現せず」を記録し、
  該当受け入れ条件を取り下げる。**CI が通ったことを実機確認の代替にしない。**

**この注意には実例があります。** パン解放は イ(#176) で修正済みと判断して
マージしましたが実機では直っておらず、ス(#201) で作り直しました。合成テストが
通ることと実機で直っていることは別です。

---

## 4. 検証コマンド

リポジトリ直下で実行します。

```text
python -m compileall -q hplc_app app.py
python scripts/ci_validate.py --offline-assets auto
set QT_QPA_PLATFORM=offscreen
python -m unittest discover -s tests -v
```

描画・操作系（テ・ト）を触った場合は追加で:

```text
python scripts/probe_pyqtgraph_parity.py
python scripts/benchmark_integrated_screen.py
```

Windows 7 の固定依存に触れた場合のみ:

```text
python scripts/windows7_import_preflight.py
python scripts/verify_windows7_wheelhouse.py
```

PR の Testing 欄には **実際に走らせたコマンドと結果だけ** を書きます。

---

## 5. 事前調査で判明した技術的事実（タ行・ナ行／記録）

再調査の重複を避けるため、計画時にコードを読んで確認した内容を記録します。
`main` = `edbb06a` 時点です。**行番号ではなく関数名で探してください。**

### 5.1 「Y 軸全体」が表示範囲を見ていない理由（確定）

`MainWindow._reset_y_view` は `_scene_y_limits(self._screen_scene, "y1")` を呼び、
`_scene_y_limits` は **トレース全体の `y_values` を走査**しています。X 範囲での
絞り込みがどこにもありません。

```python
for trace in scene.traces:
    finite = np.asarray(trace.y_values, dtype=float)   # 全点
    ...
```

対処方針: 現在の X 表示範囲に入る点だけで最小・最大を求める。X 範囲は
`_screen_view_state().x` から取れます。範囲内に点がない場合の扱い（従来どおり
全範囲にする、変更しない、など）を決めてテストで固定してください。

### 5.2 「表示」チェックを押すとちらつく理由（確定）

`MainWindow._method_controls_changed` は、単位・ベースライン・積分範囲表示・
保持時間ラベル・%B・グリッド・凡例位置のどれが変わっても、最後に無条件で
`self._plot()` を呼びます。PyQtGraph が既定になった今、これは**毎回シーンの
作り直し**です。

セ(#203) がピーク選択について同じ問題を直しました。**その解決の形をここにも
広げるのが筋のよい直し方**です。オーバーレイの表示・非表示だけで済む変更に
`_plot()` を通さないでください。

### 5.3 「reset original view」の挙動

Matplotlib ツールバーの Home 相当が「元の表示」に戻る動作になっています。
利用者の期待は「全体表示」です。既存の「全体表示」ボタンと**同じ処理につないで**
ください。新しい計算経路を作らないこと。

### 5.4 フラクション間隔の単位（確定）

- `models.py` の `FractionRegion.interval_min` は **分** 単位で、既定 1.0。
- `gui.py` の `fraction_interval_spin` は `setRange(0.01, 1000.0)` /
  `setDecimals(2)` / `setSuffix(" min")`。**0.01 分 = 0.6 秒**まで指定できます。
- `dialogs.py` の数値入力ダイアログ側は `setMinimum(0.00001)` と、さらに細かい。

要求は「秒単位・最低 1 秒」です。**表示と入力を秒に変え、下限を 1 秒にして
ください。** 永続フィールド `interval_min` の意味（分）は変えないこと。UI で
秒を受け取り、保存時に分へ変換する形が、スキーマを触らずに済みます。

> 「細かく設定でき過ぎると誤操作で点数を増やしすぎてアプリが落ちる」という
> 報告です。下限だけでなく、**生成される区切り線の本数に上限**を設け、超える
> 場合は実行前に警告してください。落ちる原因が本数なのかは実機で確認すること。

### 5.5 分析対象物とプリセットの現状

- `Measurement` / `Run` に `analyte_name` / `analyte_id` / `analyte_aliases` /
  `analyte_source` / `molar_absorptivity_214` / `molar_absorptivity_280` /
  `molecular_weight_g_mol` / `extinction_coefficient_unit` があります。
- `extinction_coefficient_unit` は既定 `"M^-1 cm^-1"` で、`dialogs.py` の
  試料情報フォームに「吸光係数の単位」として出ています。**削除対象**です。
- `preset_store.py` の `PRESET_KINDS = ("conditions", "gradients")` に
  **3 つ目の種類を足す**のが、分析対象物プリセットの置き場所になります。

**これは今回唯一スキーマに触るバッチです。** 永続フィールドの削除と新しい
プリセット種別の追加が入るため、`project_io.py` / `project_migrations.py` に
移行を入れ、既存 `.hplcproj` が読めることをテストしてください。

### 5.6 レポートの印刷と PDF 名（確定）

- 印刷は `QtPrintSupport.QPrintDialog` を使っています。プレビューには
  `QPrintPreviewDialog` があり、`paintRequested` シグナルで既存の描画処理を
  そのまま呼べます。
- PDF の既定名は `self._default_save_path("analysis_report.pdf")` と
  **固定文字列**です。プロジェクト名が入っていません。`naming.py` に
  Windows で安全なファイル名を作る `sanitize_filename_component` が既にあります。

### 5.7 フィット由来ピークに面積が出ない理由（確定）

`peak_fitting.apply_fit_result` は、フィット由来ピークの面積・高さ・FWHM・
%Area・定量値を**明示的に `None` にしています**。

```python
# Fitted rows are descriptive results, not additional integrations.
fitted_peak.raw_height_uv = None
fitted_peak.raw_area_uv_min = None
fitted_peak.area_mau_min = None
fitted_peak.area_percent = None
fitted_peak.fwhm_min = None
```

キ(#181) での意図的な設計です。「フィット行は説明的な結果であって、追加の積分では
ない」という考え方でした。**今回の「フィッティングした面積も表示したい」という
要求は、この判断を覆すものです。**

対処方針: フィット曲線から面積を算出して保持する。Gaussian は解析的に
`amplitude * sigma * sqrt(2π)`、EMG は `evaluate_fit_profile` を数値積分すれば
求まります（既存関数で足ります）。

**注意点が 2 つあります。**

1. **%Area の分母に二重計上しないこと。** 親ピークとフィット由来ピークの両方を
   合計に入れると、%Area が壊れます。どう扱うかを決めてテストで固定してください。
2. **測定由来の面積と推定由来の面積が同じ列に並びます。** 区別が付く形にして
   ください。これは飽和ピーク補正と同じ問題なので、同じ扱いに揃えること。

### 5.8 トレースごとの線種を持つ場所がない（確定）

`Dataset` に線種のフィールドは**ありません**。あるのは `Method.line_width`
（プロジェクト全体で 1 つ）だけです。描画側もトレースを実線固定で描いています。

- Matplotlib 側は `gui.py` でトレースを `linestyle="-"` で描画
- PyQtGraph 側は既定のペン（実線）

つまり「トレースごとに実線・破線を選べるようにする」には、**`Dataset` に永続
フィールドを 1 つ足す必要があります**。ツ(#215) と並んで、今回スキーマに触る
2 つ目のバッチになります。移行と後方読み込みを書いてください。

配色は `dataset_display_color`（明示色 → 波長既定 → パレット）で決まります。
グラジエント配色は**この解決順を壊さずに**、明示色をまとめて設定する手段として
実装するのが素直です。

### 5.9 マウスモードが PyQtGraph で効かない件（未特定・要計測）

「マウスモードの選択肢のほとんどが PyQtGraph で効かない。**主目盛グリッドを
表示している時のみ**起きるようだ」という報告です。

`_mouse_mode_changed` はモード ID を対応するトグルボタンへ委譲する作りで、
グリッド設定を参照していません。一方 `_method_controls_changed`（グリッドの
チェックを含む）は最後に `_plot()` を呼び、PyQtGraph 側ではシーンが作り直され
ます。**その再構築がモードに紐づくハンドラや span selector を落としている**
可能性がありますが、計画時点では確認できていません。

**まず実機で条件を絞り込んでください。** グリッドの ON/OFF でどのモードが
どう効かなくなるかを記録してから直すこと。5.2 の修正でシーン再構築が減ると、
この症状も一緒に消える可能性があります。**テ(#216) の中で 5.2 を先に直し、
その状態で再現するかを確認するのが最短です。**

### 5.10 現在 Matplotlib が担当している範囲

ウ(#177) で PyQtGraph が Windows 11 の既定になりました。Matplotlib は次を持ち
続けています。画面まわりを触るときの前提として引き続き有効です。

| # | 担当領域 |
|---|---|
| 1 | **画面状態の真実の源**。軸・表示範囲・オーバービュー窓・履歴は Matplotlib の axes が保持し、PyQtGraph は `refresh()` のたびに `owner._screen_view_state()` を読んで追従する |
| 2 | **図の出力**。PNG / SVG / PDF・クリップボード・印刷は Matplotlib アーティストを完全再構築してから出力。A4 レポートと 3D 図も Matplotlib Figure |
| 3 | **フォールバック**。Qt5（Windows 7）ビルド、PyQtGraph 未導入、描画例外時は Matplotlib へ復帰 |
| 4 | **一部の操作計算**。`SpanSelector` を使う従来経路 |

「ちらつく」「表示が戻る」系の不具合は、たいていこの往復のどこかで起きています。

---

## 6. バッチ定義（タ行・ナ行／記録）

各バッチの「対象ファイル」は着手時の見込みであり、増える場合は PR に理由を書きます。

---

### タ — ホーム画面の表と操作ボタンの整理

**Issue: #213** / **依存なし** / バッチ「チ」の前提

方針: **ホーム画面の表は見るための場所にし、メタデータの編集はダイアログに寄せる。**

対象要求
- 表の既定の列順を、左から **選択・表示・ラベル・色・Run ID・タイムスタンプ・以下同順** に変更
- 「元ファイル」列は表から外し、「測定・試料条件の入力」ダイアログから見られるようにする
- ボタン「スペクトル」を「表示設定」に改名（中身の拡張は ヌ(#221)）
- ボタン「一括入力」を「測定・試料条件の入力」に改名
- ホーム画面の「解析」欄には、上の 2 つだけを残す
- 表の選択行で「色」セルが反転表示されて見づらい。選択時も非選択時も同じ見え方にする
- 「作業ディレクトリを管理」の「再帰」を分かりやすい表記にするか、注釈を付ける

受け入れ条件
- [ ] 既定の列順が指定どおりになる（設定を初期化した状態で確認する）
- [ ] 「元ファイル」列が表に出ない
- [ ] 「測定・試料条件の入力」ダイアログで元ファイルのパスが確認できる
- [ ] 「解析」欄のボタンが「測定・試料条件の入力」と「表示設定」の 2 つだけになる
- [ ] 解析欄から外したボタンの機能が、どこかから引き続き使える（消さない）
- [ ] 選択行の「色」セルが、非選択時と同じ色で見える
- [ ] 「再帰」の意味が、表記か注釈で分かる
- [ ] 既存の列ドラッグ並び替えと、その永続化が退行していない
- [ ] `REQUIREMENTS_STATUS.md` を更新する

対象ファイル: `hplc_app/gui.py`, `hplc_app/settings_store.py`,
`hplc_app/dialogs.py`, `hplc_app/i18n.py`, `tests/test_gui.py`

注意
- 既定の列順は `settings_store.py` の `DEFAULT_DATASET_COLUMN_ORDER` です。
  非表示は `gui.py` の `DATASET_HIDDEN_COLUMN_IDS` が扱います。**列インデックス
  定数を振り直さないでください。** オ(#179) で入れた論理列 ID → 表示位置の
  対応表を使うこと。
- 既に列順を保存している利用者の設定は、そのまま残ります。既定を変えても
  保存済みの順序は上書きされません。**それでよいか**を Issue で確認するか、
  移行を入れるかを決めて PR に書いてください。
- 「解析」欄から外すボタンは**削除ではなく移動**です。`AGENTS.md` の
  「既存の機能を新しい操作の実装過程で削除しない」に該当します。
- 色セルの反転は、選択時のスタイルを上書きするデリゲートで対処するのが素直です。

---

### チ — 「測定・試料条件の入力」ダイアログの拡張

**Issue: #214** / **依存: #213（タ）**

対象要求
- 現在のプリセット機能はそのまま残したうえで「選択行から条件をコピー」を追加。
  押すとダイアログが出て、**何の要素をコピーするか**をチェックボックスで選ぶ。
  OK でコピー完了。その後、複数行を選択して「ペースト」ボタンで反映する
- **コピー時に複数行を選択している場合は使えない**（エラーメッセージを出す）
- 「対象」列が分かりにくいので「選択」に変更し、選択中の行にチェックが付くようにする
- このダイアログからも Run 統合・分離ができるようにする
- タイムスタンプを Run ID の右に表示する
- 「選択を同一 Run へ」を実行したとき、タイムスタンプは元の値を保持する
- 「選択を同一 Run へ」の確認ダイアログで、統合される Run ID を**両方**表示し、
  さらに統合先の Run も表示する。「正本 Run」という名称を「統合先 Run」に変更

受け入れ条件
- [ ] 「選択行から条件をコピー」で、コピーする要素をチェックボックスで選べる
- [ ] コピー時に 2 行以上を選んでいると、エラーメッセージが出てコピーされない
- [ ] コピー後、複数行を選択して「ペースト」すると、選んだ要素だけが反映される
- [ ] コピーしていない状態でペーストしようとしたときの挙動が決まっている
- [ ] ペーストが Undo / Redo の 1 ステップとして扱われる
- [ ] 既存のプリセット機能が退行していない
- [ ] 「対象」が「選択」になり、選択中の行にチェックが付く
- [ ] ダイアログから Run 統合・分離ができる
- [ ] タイムスタンプ列が Run ID の右にある
- [ ] Run 統合後もタイムスタンプが元の値のまま
- [ ] 統合の確認ダイアログに、統合される Run ID が両方と、統合先の Run が出る
- [ ] 「正本 Run」の表記が「統合先 Run」に変わっている（UI・メッセージ・履歴ラベル）
- [ ] `REQUIREMENTS_STATUS.md` を更新する

対象ファイル: `hplc_app/dialogs.py`, `hplc_app/gui.py`, `hplc_app/i18n.py`,
`tests/test_gui.py`

注意
- Run 統合・分離は `gui.py` に既存の実装（`group_selected_runs` /
  `ungroup_selected_runs`）があります。**ダイアログから同じ処理を呼んでください。**
  並行実装を作らないこと。
- 「タイムスタンプを保持」は、統合時に Run 側の値でデータセットの
  `acquisition_datetime` を上書きしていないかを確認してください。エ(#178) で
  Run ID が取得日時由来になっているので、ここを変えると Run ID 表示と食い違う
  可能性があります。**食い違う場合はどちらを正とするかを Issue にコメント**
  してから進めてください。
- コピーの保持先はセッション内で構いません。プロジェクトに保存する必要は
  ありません（スキーマを触らない）。

---

### ツ — 分析対象物プリセット

**Issue: #215** / **依存なし**（`dialogs.py` でチ と重なるので、後にマージする方を rebase）

対象要求
- 分析対象物の欄をクリックすると、プルダウンでプリセットが選べる
- 一番上に「新しく登録」があり、押すとダイアログで ε214・ε280・分子量を入力して登録できる
- このプリセットを、設定 > プリセット管理 から管理できるようにする
- 「吸光係数の単位」は不要なので削除する

受け入れ条件
- [x] 分析対象物の欄がプルダウンになり、登録済みプリセットから選べる
- [x] 選ぶと ε214・ε280・分子量が一緒に入る
- [x] 一番上の「新しく登録」からダイアログが出て、名前・ε214・ε280・分子量を登録できる
- [x] 登録したプリセットが次回起動後も残る（アプリ全体の設定として保存される）
- [x] 設定 > プリセット管理 から、分析対象物プリセットの一覧・改名・複製・削除ができる
- [x] 既存のプリセット管理（条件・グラジエント）が退行していない
- [x] 「吸光係数の単位」の入力欄が消えている
- [x] 既存 `.hplcproj` が引き続き読める。単位フィールドを持つ古いファイルでも壊れない
- [x] 定量計算の結果が、この変更で変わらない
- [x] `REQUIREMENTS_STATUS.md` を更新する

対象ファイル: `hplc_app/models.py`, `hplc_app/preset_store.py`,
`hplc_app/dialogs.py`, `hplc_app/project_io.py`, `hplc_app/project_migrations.py`,
`hplc_app/i18n.py`, `tests/test_core.py`, `tests/test_gui.py`

注意
- **今回唯一スキーマに触るバッチです。** `AGENTS.md` のプロジェクト互換規約
  （永続フィールドを黙って改名・削除しない、移行を明示する）を厳守してください。
- `extinction_coefficient_unit` は `Measurement` と `Run` の両方にあります。
  UI から消すのは確定ですが、**永続フィールドまで消すかは別の判断**です。
  **既定案は「UI からは消し、フィールドは残して読み書きを維持する」**。既存
  ファイルとの互換が最も安全で、移行も不要です。フィールドごと削除したい場合は、
  移行と後方読み込みを書いたうえで Issue にコメントしてください。
- プリセットは `preset_store.py` の `PRESET_KINDS` に 3 つ目を足す形が素直です。
  **アプリ全体の設定**として保存すること（`AGENTS.md`「永続的な全体設定を個別
  プロジェクトに入れない」）。
- ε と分子量は定量計算に使われます。**プリセットを選んだときに既存の値を
  上書きする条件**（空のときだけか、常にか）を決めて、テストで固定してください。

---

### テ — 表示の安定化と表示リセットの意味

**Issue: #216** / **依存なし** / バッチ「ト」の前提

対象要求
- 「Y 軸全体」を、表示中の X 範囲での最大ピークに合わせる（現在は全範囲基準）
- 「reset original view」を「全体表示」の挙動にする
- 「表示」のチェックリスト（積分範囲・ベースライン、保持時間ラベルなど）を
  押したときのちらつきをなくす（PyQtGraph のみ）
- マウスモードの選択肢のほとんどが PyQtGraph で効かない。
  「主目盛グリッドを表示」している時のみ起きるようだ
- Y1/Y2 を上下分割している時、上下の間にバーを置いて大きさの比率を変えられるようにする

受け入れ条件
- [ ] 「Y 軸全体」が、現在の X 表示範囲に入るデータだけで縦方向にスケールする
- [ ] 範囲内にデータがない場合の挙動が決まっており、テストで固定されている
- [ ] 「reset original view」が「全体表示」と同じ結果になる
- [ ] 「表示」のチェックを切り替えても、表示範囲・ズームが変わらない
- [ ] 切り替えのたびにシーンが作り直されない（**再構築回数をテストで固定する**）
- [ ] グリッド表示の ON / OFF にかかわらず、すべてのマウスモードが PyQtGraph で効く
- [ ] Y1/Y2 分割表示で、上下の境界をドラッグして比率を変えられる
- [ ] 変えた比率が、その表示の間は保たれる
- [ ] Matplotlib 描画が退行していない
- [ ] `REQUIREMENTS_STATUS.md` を更新する

対象ファイル: `hplc_app/gui.py`, `hplc_app/screen_preview.py`,
`hplc_app/pyqtgraph_scene.py`, `tests/test_gui.py`

注意
- **原因は第 5.1・5.2・5.3 節にあります。** 5.1〜5.3 は確定なので再調査不要です。
- **5.9（マウスモード）は未特定です。** 実機でグリッド ON/OFF とモードの対応を
  記録してから直してください。**5.2 のちらつき修正を先に入れて、その状態で
  再現するかを確認するのが最短です。** シーン再構築がハンドラを落としている
  可能性があります。
- 分割バーは `QSplitter` を使えるかをまず検討してください。PyQtGraph の
  `GraphicsLayoutWidget` は 1 つのウィジェットなので、行の伸縮比
  （`setRowStretchFactor` 相当）で実現する必要があるかもしれません。
  **選んだ方法と理由を PR に書いてください。**
- 比率をプロジェクトに保存するかは決めていません。**既定案は「保存しない
  （表示の間だけ）」**です。スキーマを触らずに済みます。

---

### ト — 選択モードの統合と縦線・フラクションの整理

**Issue: #217** / **依存: #216（テ）**

対象要求
- 「積分エリア選択」と「時間範囲選択」を統合して「選択モード」にする
- 「時間範囲選択」で選択したとき、範囲内の積分や縦線も選択される。
  選択された状態で Delete を押すと消せる
- 縦線ポインターで置いた縦線に、保持時間を表示する
- マウスでフラクション範囲を指定するモードは不要。積分コーナーの
  「フラクション範囲」ボタンを削除し、マウスモードからも外す。
  時間間隔の指定は「範囲を数値入力・編集」のダイアログのみから行う
- フラクション範囲の時間間隔を秒単位にし、**最低 1 秒**から選べるようにする

受け入れ条件
- [ ] マウスモードに「選択モード」があり、積分エリアと時間範囲の両方を選べる
- [ ] 「積分エリア選択」「時間範囲選択」が別々のモードとして残っていない
- [ ] 時間範囲で選択すると、範囲内の積分ピークと縦線がまとめて選択される
- [ ] 選択した状態で Delete を押すと、選択されたものが削除される
- [ ] 削除が Undo / Redo の 1 ステップとして扱われる
- [ ] 縦線に保持時間が表示される。移動すると追従する
- [ ] 保持時間の表示が Matplotlib 描画でも出る
- [ ] 「フラクション範囲」ボタンとマウスモードの該当項目がなくなっている
- [ ] フラクション範囲の追加・編集が「範囲を数値入力・編集」から従来どおりできる
- [ ] フラクションの時間間隔が秒単位で入力でき、下限が 1 秒
- [ ] 生成される区切り線の本数に上限があり、超える場合は実行前に警告が出る
- [ ] 既存プロジェクトのフラクション範囲が、読み込み後も同じ値のまま
- [ ] `REQUIREMENTS_STATUS.md` を更新する

対象ファイル: `hplc_app/gui.py`, `hplc_app/screen_preview.py`,
`hplc_app/dialogs.py`, `hplc_app/i18n.py`, `tests/test_gui.py`

注意
- **単位の扱いは第 5.4 節にあります。** 永続フィールド `interval_min`（分）の
  意味は変えず、UI で秒を受け取って保存時に変換してください。スキーマを触らずに
  済みます。
- 「アプリが落ちる」報告があります。**下限を上げるだけで済ませないでください。**
  区切り線の本数に上限を設け、実機で本数が原因かを確認すること。
- マウスモードは カ(#180) で入れた仕組みです。**モードを統合するのであって、
  新しいモード管理を作るのではありません。**
- フラクションのマウス経路の削除は `AGENTS.md` の「既存のマウス機能を削除しない」
  に見えますが、**利用者が明示的に削除を要求している**ので実施してよい変更です。
  数値入力が同等以上に使えることを先に確認してください。

---

### ナ — フィット結果の面積表示と飽和ピーク補正

**Issue: #218** / **依存なし**

対象要求
- **フィッティングしたピークの面積も表示する**（現在は空欄）
- サンプルロード量が多いと検出器が飽和し、ピーク上部が切り取られた形になる。
  底部のピーク形状をもとに、切り取られた上部を再生するフィッティングを実装する
- 名称は**「飽和ピーク補正」に決定**（当初の仮称は「頭打ち補正」）

この 2 件は**同じ計算を共有**します。飽和補正の価値は「本来の面積が分かること」
なので、フィット曲線から面積を出す仕組みがその土台になります。別々に実装しないで
ください。

**名称は「飽和ピーク補正」に決定しました**（英語 `Saturated peak correction`、
2026-09-01 利用者判断）。UI・履歴ラベル・エクスポート・レポートで一貫させて
ください。

受け入れ条件
- [x] Gaussian / EMG のフィット結果に、フィット曲線から求めた面積が表示される
- [x] 面積の算出方法（解析式か数値積分か）が明示され、テストで数値が固定されている
- [x] %Area の合計が、フィット由来ピークの追加によって壊れない
- [x] 飽和して頭が平らになったピークに対して、非飽和部分から本来の形状を推定できる
- [x] 補正後の面積・ピーク高さ・保持時間が確認できる
- [x] **推定値が測定値と明確に区別できる。表・凡例・エクスポート・レポートで区別が付く**
- [x] 元の積分ピークの数値が、この操作で変化しない
- [x] 飽和していないピークに適用しようとしたとき、警告が出るか実行されない
- [x] どの点を飽和とみなしたかが確認できる
- [x] 推定を取り消せる
- [x] 既存の Gaussian / EMG フィッティングが退行していない
- [x] `REQUIREMENTS_STATUS.md` を更新する

対象ファイル: `hplc_app/peak_fitting.py`, `hplc_app/analysis.py`,
`hplc_app/gui.py`, `hplc_app/dialogs.py`, `hplc_app/i18n.py`,
`tests/test_core.py`, `tests/test_gui.py`

注意
- **これは測定していない値を作る機能です。** `AGENTS.md` の科学的挙動の規約が
  正面から効きます。
  - 生の取り込み値は変更しない
  - 生データと導出データを区別する
  - 隠れた平滑化・補間をしない
  - 推定であることを利用者から見える形にする
  **推定値を測定値と同じ見た目で出さないでください。**
- キ(#181) で**フィット由来ピークを別ピークとして扱う仕組み**が入っています
  （`PeakRegion.is_fitted`、`fitted_peak_from_result`）。飽和補正もその仕組みに
  載せてください。新しい種類のピークを別途作らないこと。
- **面積が空欄になっている理由は第 5.7 節にあります。** `apply_fit_result` が
  意図的に `None` にしています。キ(#181) の設計判断を覆す変更なので、
  なぜ変えるのかを PR に書いてください。
- 飽和の判定条件（連続する同値点の数、検出器の上限値の扱い）を明示し、テストで
  固定してください。**自動判定が外れる場合に備えて、利用者が範囲を指定できる
  経路も用意する**のが安全です。
- 当てはめには既存の `_fit_model` / `evaluate_fit_profile` が使えます。
  **飽和した点を除外して当てはめる**のが基本の考え方です。

---

### ヌ — 「表示設定」ダイアログ（線種とグラジエント配色）

**Issue: #221** / **依存: #213（タ）**

対象要求
- 「色の変更」を「**表示設定**」にし、色だけでなく実線・破線などの線種も選べるようにする
- 色は現在のモードに加えて「**グラジエントモード**」を設け、同系列のグラジエント
  カラーを効率よく選べるようにする

受け入れ条件
- [ ] 「表示設定」ダイアログで、選択したトレースの線種を選べる
- [ ] 少なくとも実線・破線・点線・一点鎖線が選べる
- [ ] 線種が Matplotlib と PyQtGraph の両方で反映される
- [ ] 線種が図の出力（PNG / SVG / PDF）とレポートにも反映される
- [ ] 線種がプロジェクトに保存され、再読込後も保たれる
- [ ] 既存 `.hplcproj` を開いたとき、線種は実線として扱われる
- [ ] 色を 1 つずつ選ぶ従来のモードが従来どおり使える
- [ ] グラジエントモードで、選択した複数トレースに同系列の色をまとめて割り当てられる
- [ ] 割り当ての順序が、表の並び順に沿っている
- [ ] グラジエントの系統（色相）と濃淡の範囲を選べる
- [ ] 適用が Undo / Redo の 1 ステップとして扱われる
- [ ] `REQUIREMENTS_STATUS.md` を更新する

対象ファイル: `hplc_app/models.py`, `hplc_app/dialogs.py`, `hplc_app/gui.py`,
`hplc_app/rendering.py`, `hplc_app/pyqtgraph_scene.py`, `hplc_app/project_io.py`,
`hplc_app/project_migrations.py`, `hplc_app/i18n.py`,
`tests/test_core.py`, `tests/test_gui.py`

注意
- **これはツ(#215) と並んでスキーマに触るバッチです。** 第 5.8 節のとおり、
  `Dataset` に線種のフィールドがありません。永続フィールドを 1 つ足し、
  `project_io.py` / `project_migrations.py` に移行を入れてください。
  既存ファイルは実線として読めること。
- **描画は 3 経路あります。** Matplotlib 画面（`linestyle=`）、PyQtGraph 画面
  （Qt の `PenStyle`）、図の出力とレポート。**3 つすべてに反映してください。**
  片方だけだと画面と出力が食い違います。
- グラジエント配色は、コ(#185) の 3D 表示が既に同じことをしています
  （`plot3d.gradient_colors`、Plotly 配色名 → Matplotlib colormap の対応表、
  濃度による切り落とし）。**その実装を再利用するか、少なくとも同じ考え方に
  揃えてください。** 別の配色ロジックを 2 つ持たないこと。
- 配色の解決順（`dataset_display_color`: 明示色 → 波長既定 → パレット）は
  変えないでください。グラジエントモードは**明示色をまとめて設定する手段**として
  実装するのが素直です。
- 線種は色覚に依存しない区別手段でもあります。既定は実線のままにし、
  利用者が選んだときだけ変わるようにしてください。

---

### ニ — 解析レポートの印刷プレビューと出力名

**Issue: #219** / **依存なし**

対象要求
- 解析レポートの印刷時に、印刷プレビューを表示する
- PDF 出力の既定ファイル名を `analysis_report_<プロジェクト名>` にする

受け入れ条件
- [x] 解析レポートの印刷でプレビューが出る
- [x] プレビューから印刷・キャンセルができる
- [x] プレビューの内容が、実際に印刷される内容と一致する
- [x] PDF 出力の既定ファイル名にプロジェクト名が入る
- [x] プロジェクト名が未設定・空のときのファイル名が決まっている
- [x] プロジェクト名に Windows で使えない文字が入っていても、正しいファイル名になる
- [x] 現在の表示の印刷（レポートではない方）が退行していない
- [x] `REQUIREMENTS_STATUS.md` を更新する

対象ファイル: `hplc_app/gui.py`, `hplc_app/report.py`, `hplc_app/i18n.py`,
`tests/test_gui.py`

注意
- 詳細は第 5.6 節にあります。`QPrintPreviewDialog` の `paintRequested` から
  既存の描画処理を呼べば、**印刷経路を二重に持たずに済みます**。
- ファイル名の無害化には `naming.py` の `sanitize_filename_component` が既に
  あります。新しく書かないでください。
- 印刷そのものは `VALIDATION_BLOCKERS.md` の物理ゲートです。**プレビューが
  出ることと、実際のプリンタで正しく出ることは別**です。実機のプリンタでの
  確認が済むまで、そのゲートを解決済みにしないでください。

---

## 7. 完了条件（全バッチ共通）

各 Issue は次がすべて満たされたときに閉じます。

1. 受け入れ条件のチェックがすべて埋まっている（取り下げたものは理由が書かれている）
2. 第 4 節の検証コマンドが通り、結果が PR に記録されている
3. `REQUIREMENTS_STATUS.md` が更新されている
4. 実機確認項目について、確認済みか「未確認」の明記があるか、どちらかである
5. PR がマージされ、`origin/main` に含まれている

## 8. 未決定事項（タ行・ナ行／記録）

| 項目 | 内容 | 判断者 |
|---|---|---|
| 保存済み列順の扱い | タ節。既定の列順を変えても、既に保存された順序は上書きされない。移行するか放置するか | 実装時に Issue へコメントして確認 |
| 統合時のタイムスタンプと Run ID | チ節。タイムスタンプを保持すると、取得日時由来の Run ID と食い違う可能性がある | 食い違う場合は Issue にコメントしてから進める |
| `extinction_coefficient_unit` の削除範囲 | ツ節。UI だけ消すか、永続フィールドごと消すか | 既定案は UI のみ。フィールド削除は移行つきで別途判断 |
| フィット面積と %Area | ナ節。フィット由来ピークを %Area の合計に入れるか | 既定案は「入れない」。実装時に決めてテストで固定 |
| グラジエント配色の粒度 | ヌ節。系統と濃淡をどこまで選ばせるか | 実装時に 3D 表示の配色一覧に揃えて提案し、実機確認で調整 |
| 分割比率の保存 | テ節。上下の比率をプロジェクトに保存するか | 既定案は保存しない |

### 決定済み

| 項目 | 決定 |
|---|---|
| 環境設定の「描画品質」削除 | **保留**。Windows 7 実機での検証が未了のため、設定は現状のまま残す |
| フィット由来ピークの表示既定 | **既定で表に出す**。切替表示にはしない |
| 個人名の記載 | **書かない**。追跡対象ファイル・コミットメッセージ・Issue・PR のいずれにも研究室メンバーの氏名を残さない（`AGENTS.md`） |
| フラクションのマウス指定 | **削除する**。利用者が明示的に要求。数値入力のみに一本化 |
| 飽和ピーク補正の名称 | **「飽和ピーク補正」**（`Saturated peak correction`）に決定 |

---

# ハ行・マ行（2026-09-02 の実機フィードバック）

第 9 節から第 11 節までが今回のバッチです。読む順序（第 0 節）、1 Issue あたりの
手順（第 3 節）、検証コマンド（第 4 節）、完了条件（第 7 節）は変わりません。

---

## 9. 事前調査で判明した技術的事実（ハ行・マ行）

再調査の重複を避けるため、計画時にコードを読み、必要なものは実際に計測して確認した
内容を記録します。`main` = `c5fe4c6` 時点です。**行番号ではなく関数名で探して
ください。**

計測は Windows 11 の `.venv-win11-x64`（Python 3.11.9 / Matplotlib 3.10.1 /
PyQtGraph 0.13.7 / NumPy 2.2.3）で行いました。Windows 7 側（Matplotlib 3.7.5 /
PyQtGraph 0.13.3）でも同じ API を使うため結論は変わりませんが、**Win7 実機での
確認は別途必要**です。

### 9.1 「通常（パン・ズーム）」が上部バーと連動しない（確定）

`MainWindow._mouse_mode_changed` は、モード ID を対応するトグルボタンへ委譲します。
`normal` のときだけ、次のように**何も有効化せずに戻ります**。

```python
        for control in controls.values():
            if control.isChecked():
                control.setChecked(False)
        self._clear_span_selector()
        self._hide_interaction_cursor()
        if mode == "normal":
            self.statusBar().clearMessage()
            return
```

一方 `_toolbar_navigation_triggered` は、上部バーの pan / zoom が押されたときに
**マウスモードを `normal` へ戻します**。つまり連動が片側だけで、

- 上部バーの交差矢印を押す → マウスモードが「通常」になる（動く）
- マウスモードで「通常」を選ぶ → 上部バーは押されないまま（動かない）

という非対称になっています。報告の「上部バーのボタンを押している時しかできない」
「上部アイコンの交差矢印と同じであってほしい」は、どちらもこの 1 か所です。

対処方針: `normal` を選んだときに、pan も zoom も無効なら pan を有効化します
（`self.toolbar.pan()`）。既に zoom が有効ならそのままにするのが素直です。
`_toolbar_navigation_triggered` との**相互呼び出しでループしない**ことをテストで
固定してください。`_deactivate_toolbar_navigation` が `toolbar.mode` を見て
トグルする既存の作りをそのまま使えます。

### 9.2 グリッド ON で PyQtGraph のパンが x だけになる（確定・実測あり）

パンの方向は `screen_navigation.axis_pan_view` が `session.target`（=
`ScreenPointerEvent.hit_region`）だけで決めます。`"x"` なら X しか動きません。
その `hit_region` を決めているのが `PyQtGraphSceneConsumer._pointer_region` で、
**軸の矩形をプロット面より先に判定**しています。

```python
        for axis, role, region in axis_regions:
            if self._point_in_rect(scene_position, axis.sceneBoundingRect(), 2.0):
                return role, region
        if self._point_in_rect(scene_position, self.primary.vb.sceneBoundingRect()):
            return "y1", "plot"
```

PyQtGraph の `AxisItem.boundingRect()` は、**グリッドが有効なとき、軸の矩形を
ViewBox 全体との和に広げます**（グリッド線を軸自身が描くため）。

```python
        if linkedView is None or self.grid is False:
            ...
        else:
            return self.mapRectFromParent(self.geometry()) | linkedView.mapRectToItem(self, linkedView.boundingRect())
```

`PlotItem.showGrid(x=True)` は `updateGrid()` 経由で `getAxis('bottom').setGrid(...)`
を呼ぶので、**下側 X 軸の矩形がプロット面全体を覆います**。結果、プロット面の
どこを押しても `hit_region` が `"x"` になり、パンが X 方向だけになります。

実測（`GraphicsLayoutWidget` 700x420、offscreen）:

```text
grid OFF  bottom axis rect: [45.0, 385.0, 690.0, 410.0]   plot centre inside? False
grid ON   bottom axis rect: [51.0,  10.0, 690.5, 410.0]   plot centre inside? True
```

**これは テ(#216) で直したつもりの症状の、本当の原因です。** 第 5.9 節では
「シーン再構築がハンドラを落としている可能性」と書き、#216 はイベント所有者が
グリッド変更をまたいで保たれることをテストで固定しました。その修正自体は正しい
のですが、症状はこの当たり判定側に残っています。**同じ場所をもう一度疑わないで
ください。**

対処方針: プロット面（ViewBox）を先に判定し、軸の帯は ViewBox の外側だけで
判定する。あるいは `sceneBoundingRect()` ではなく軸のジオメトリ
（`axis.mapRectToScene(axis.mapRectFromParent(axis.geometry()))`）を使う。
**分割表示側（`if self.split_y_axes:` の分岐）にも同じ判定があります。両方直して
ください。** 軸の帯をドラッグしたときの単軸パン（X だけ／Y だけ）は退行させないこと。

### 9.3 上部バーのツールチップは Matplotlib の定数（確定）

ツールチップは `NavigationToolbar2.toolitems` の英語定数です。

```text
('Home', 'Reset original view', 'home')
('Back', 'Back to previous view', 'back')
('Forward', 'Forward to next view', 'forward')
('Pan', 'Left button pans, Right button zooms / x/y fixes axis, CTRL fixes aspect', 'move')
('Zoom', 'Zoom to rectangle / x/y fixes axis', 'zoom_to_rect')
('Subplots', 'Configure subplots', 'subplots')
('Save', 'Save the figure', 'filesave')
```

`gui.py` は既に `self.toolbar._actions["pan"]` という形でアクション辞書を使って
います（`_wire_toolbar_navigation_actions` / `_toolbar_navigation_triggered`）。
同じ辞書のキー（`home` / `back` / `forward` / `pan` / `zoom` / `subplots` /
`save`）に対して `setToolTip()` を掛けるのが最短です。文言は `i18n.py` に置くこと。

**注意が 2 つあります。**

1. **Home の文言は現在の挙動と食い違っています。** テ(#216) で Home は「全体表示」
   につながりました。英語の "Reset original view" は古い意味です。日本語では
   「全体表示」と書いてください。英語側も直すかは Issue で決めてください。
2. `Subplots` は、PyQtGraph 表示のとき `edit_axis_labels()` を開くように
   `AxisAwareNavigationToolbar.configure_subplots` で差し替えられています。
   ツールチップも実際に開くもの（軸・ラベル・書式設定）に合わせてください。

### 9.4 積分の背景ハイライトだけ切り替わらない（確定）

`PyQtGraphSceneConsumer.set_peak_selection` は、選択が変わったときに既存アイテムの
見た目だけを差し替えます（セ #203）。背景の塗りにも `region.setBrush(...)` を
呼んでいるので、**値は正しく更新されています**。それでも画面が変わりません。

原因は PyQtGraph 側の非対称です。

- `InfiniteLine.setPen()` は最後に `self.update()` を呼ぶ
- `PlotCurveItem.setPen()` も `self.update()` を呼ぶ
- **`LinearRegionItem.setBrush()` は `self.brush` と `self.currentBrush` を
  差し替えるだけで `update()` を呼びません**（`setMouseHover()` は呼びます）

そのため線・ベースライン・境界線だけが再描画され、塗りは前のままになります。
報告の「線の付近だけハイライトが切り替わる」と正確に一致します。

対処方針: `setBrush` の後に `region.update()` を呼ぶ。`set_peak_selection` の
末尾にある `self.widget.update()` は `QGraphicsView` 本体の更新であり、
シーンアイテムの再描画を保証しません。**アイテム側で更新してください。**
シーンを作り直す（`_plot()` を呼ぶ）解決にしないこと。#203 の「作り直さずに
restyle する」という設計を壊します。

### 9.5 飽和ピーク補正が本来の高さを復元しない（確定・実測あり）

添付画像の「青」の状態です。**原因は EMG プロファイルの正規化**にあります。

```python
def emg_profile(x, center, sigma, tau):
    ...
    maximum = float(np.max(profile)) if profile.size else 0.0
    return profile / maximum if maximum > 0 else np.zeros_like(x)
```

**渡された `x` の上での最大値で割っています。** 飽和補正は
`fit_peak(..., exclude_range=...)` で頂上のサンプルを除外して当てはめるため、
`x` に本来の頂点が含まれません。その結果 `amplitude_uv` の意味が
「モデル曲線の高さ」ではなく「除外区間の縁での高さ」になり、**再構成された高さは
飽和の頭打ち値そのものになります。**

一方 `fitted_area_uv_min` / `fitted_apex_min` / `fitted_fwhm_min` は
`_model_grid`（頂点を必ず含む密なグリッド）の上で正規化した曲線を使います。
**同じ `amplitude_uv` がモジュール内で 2 つの意味で使われています。**

合成 EMG ピーク（真の高さ 100000、sigma=0.25、tau=0.35）を頭打ちにして現状の
`fit_saturated_peak` に掛けた実測です。形状パラメータはほぼ正確に復元されており
（center 3.996 / sigma 0.244 / tau 0.354、R^2≈1.0）、**外しているのは高さと面積だけ**です。

```text
頭打ち率   復元された高さ    高さ誤差   復元された面積   面積誤差
   90%           88379       -11.6%           81288      -9.5%
   70%           70011       -30.0%           62880     -30.0%
   50%           49725       -50.3%           44269     -50.7%
   35%           34898       -65.1%           31392     -65.1%
```

`gaussian_profile` は `exp(-0.5*((x-c)/sigma)^2)` で解析的に単位高さなので、
**Gaussian 経路には影響がありません。** そして既存テスト
`test_saturated_peak_correction_recovers_the_clipped_area` は
`fit_saturated_peak(dataset, parent, "gaussian")` と**モデルを明示して**います。
実データのテーリングピークが選ぶのは EMG です。**EMG 経路のテストが無いまま
出荷されたのが、この不具合が残った理由です。**

対処方針: `amplitude_uv` の意味を「モデル曲線そのものの高さ」に統一します。
実装は 2 通りあり、どちらでも結果は同じです。

1. `emg_profile` を、渡された `x` に依存しない最大値で正規化する
2. 探索は現状のまま行い、**当てはめ後に 1 回だけ**
   `amplitude_uv *= (モデルの真の最大値) / (当てはめに使った x 上での最大値)`
   と直す

**探索の中で毎回 `_model_grid`（最大 200001 点、`math.erfc` は Python ループ）で
正規化する実装にしないでください。実測で 2 分を超えて完了しませんでした。**
形状の順位付け（RMSE / R^2 / AIC）は振幅を最小二乗で決めるため正規化の定数倍に
不変です。したがって 2 の後処理は 1 と数学的に等価で、探索コストを増やしません。
最大値を取るなら、頂点を含む狭い局所グリッド（例 `center-2sigma` から
`center+8tau` を 2001 点）で十分です。

上の方法 2 を試した実測です。

```text
頭打ち率   復元された高さ   高さ誤差   復元された面積   面積誤差
   90%           96872      -3.13%           89099     -0.83%
   70%          100054      +0.05%           89862     +0.02%
   50%          101850      +1.85%           90675     +0.93%
   35%           99703      -0.30%           89684     -0.17%
```

**注意が 3 つあります。**

1. **通常（飽和していない）フィットにも同じ関数が使われます。** その場合は `x` に
   頂点が含まれるので差はごく小さいはずですが、**ゼロではありません**。既存の
   フィット面積・高さがどれだけ動くかを測り、PR に書いてください。
2. `PeakRegion.fit_parameters` は**プロジェクトに保存されます**。修正前に保存された
   飽和補正行の `amplitude_uv` は古い意味のままです。読み込み時に作り直すのか、
   利用者に再計算を促すのかを決めてください。**既定案は「移行しない。再計算で
   更新されると Issue に明記する」**。スキーマは触りません。
3. これは `AGENTS.md` の科学的挙動に当たる変更です。**数値が変わる変更なので、
   テストで固定してから直してください。**

### 9.6 「元ファイル」が省略表示される（確定）

`BatchMetadataDialog` は元ファイル列にフルパスを入れています
（`dataset.original_path or dataset.original_filename`、ツールチップにも同じ値）。
見えないのは表示側の都合です。

```python
        self.table.resizeColumnsToContents()
        self.table.setColumnWidth(self.SOURCE_COLUMN, 320)
```

固定 320px で、Qt の既定は**右側を省略**します。だから `G:\...` と頭だけが残ります。
ホーム画面の表は同じ問題を `gui.py` の `LeftElideDelegate`（左側を省略してファイル名
側を残す）で解決済みです。タ(#213) で列をダイアログへ移したときに、デリゲートが
一緒に移動しませんでした。

対処方針: `LeftElideDelegate` を `dialogs.py` へ移し、`gui.py` はそこから import
します（`gui.py` は既に `dialogs.py` を import しているので循環参照になりません）。
**同じデリゲートを 2 つ書かないでください。** 幅も、内容に合わせるか利用者が
広げられるようにするかを決めてください。

### 9.7 Run ID セルが明示的に読み取り専用だった（原因確定）

```python
            run_id = QtWidgets.QTableWidgetItem(dataset.run_id)
            run_id.setFlags(run_id.flags() & ~ITEM_IS_EDITABLE)
```

チ(#214) で、Run ID は読み取り専用として入れました。編集可能にするだけなら
フラグを外すだけですが、**Run ID の改名には既存の実装があります。**
`Project.rename_run` で、`gui.py` の `_dataset_item_changed` が
`DATASET_RUN_ID_COLUMN` で呼んでいます（空・重複を拒否し、Run 全体に効き、
Undo / Redo の 1 ステップになる）。

対処方針: **ダイアログからも `rename_run` を呼んでください。** 検証の並行実装を
作らないこと。同じ Run に属する他の行の表示も合わせて更新する必要があります。
OK 時の一括適用と、その場での改名のどちらにするかを決めてテストで固定してください。

### 9.8 3D のグリッドは平面ごとに切り替えられない（確定）

`plot3d.build_3d_chromatogram_figure` は `axis.grid(bool(options.show_grid))` の
一括切り替えです（コ #185 / #204）。Matplotlib 側に平面ごとの API はありません。

```python
    def grid(self, visible=True, **kwargs):
        # TODO: Operate on each axes separately
        self._draw_grid = visible
```

さらに `Axis3D.draw_grid` は、目盛ごとに**2 つの平面をまたぐ折れ線**を引きます
（平面の交線を通って両側の端まで）。つまり軸単位でグリッドを消しても「保持時間 x
系列 平面だけ」にはなりません。

対処方針: `axis.grid(False)` のままにして、**選ばれた平面のグリッドを自分で描く**。
`mpl_toolkits.mplot3d.art3d.Line3DCollection` に、各目盛位置の線分を入れて
`add_collection3d` するだけで済みます。X 目盛なら
`(x, y_min, z_min)` から `(x, y_max, z_min)`、Y 目盛なら
`(x_min, y, z_min)` から `(x_max, y, z_min)` が「保持時間 x 系列」平面です。
Matplotlib 3.7.5（Windows 7）にも同じ API があります。**私用 API
（`_axinfo` など）は使わないでください。**

`ThreeDPlotOptions` はセッション限りの設定で、プロジェクトには保存されません
（コ #185）。**今回のバッチにスキーマ変更はありません。**

### 9.9 目盛間隔に下限がなく、点数の上限もない（確定）

`AxisLabelsDialog` の主目盛・副目盛は**分**単位で、下限が実質ありません。

```python
        for widget in (self.x_major_tick, self.x_minor_tick):
            widget.setRange(0.000001, 1000000.0)
            widget.setDecimals(6)
```

その値は両方の描画経路にそのまま渡ります。

- Matplotlib: `MainWindow._set_dynamic_x_ticks` から `MultipleLocator(major_tick)`
- PyQtGraph: `screen_preview` から `axis.setTickSpacing(major, minor)`

0.000001 分の指定は 70 分の範囲で 7000 万本になります。**「誤操作で点数を
増やしすぎたときにアプリが落ちる」報告の説明はこれで付きます。**

対処方針: **入力欄は分のまま、下限を 0.1 分**にしてください（2026-09-02 利用者判断）。
`setRange(0.1, ...)` と `setDecimals(1)` で足ります。軸ラベルも既定値（5 分）も分
なので、単位の変換は要りません。**秒に変えないでください。**

0.1 分の下限なら、70 分の範囲でも 700 本です。**下限だけで落ちる原因はほぼ消えます。**
そのうえで、生成される目盛の本数に上限を設け、超える場合は適用前に警告してください。
表示範囲は利用者が広げられるので、下限だけでは上限が保証されないためです。
ト(#217) がフラクションの区切り線で同じ形（下限 + 5000 本の上限 + 事前警告）を
入れています。**その形に揃えてください。**

永続フィールド `x_major_tick_min` / `x_minor_tick_min` の単位（分）は変えないこと。
既定値（主 5.0 / 副 1.0）はどちらも下限より上なので、**既存プロジェクトの値が
下限で切り上がることはありません。** 0.1 分未満の値を保存している古いプロジェクトを
読んだときの扱い（そのまま描くか、下限に丸めるか）だけ決めてテストで固定してください。

### 9.10 レポート続きページの余白（確定）

```python
        remaining = display_peaks[20:]
        for offset in range(0, len(remaining), 40):
            continuation = Figure(figsize=A4_SIZE_INCHES, dpi=REPORT_DPI)
            continuation.subplots_adjust(left=0.06, right=0.94, top=0.93, bottom=0.06)
```

続きページは 1 ページ 40 行、上下に 6〜7% ずつ空いています。1 ページ目
（`top=0.95, bottom=0.055`）はクロマトグラム図があるので事情が違いますが、
**続きページは表だけ**です。上下を詰めれば行数を増やせます。

対処方針: 続きページの `top` / `bottom` と、タイトルの `pad=12` を詰め、
**1 ページあたりの行数を余白に合わせて増やす**。行が重ならないことと、
ページ番号・日時のフッター（`continuation.text(0.94, 0.025, ...)`）が
表に食い込まないことをテストで確認してください。1 ページ目は触らないこと
（ニ #219 / #182 で調整済みです）。

### 9.11 線の太さは既に Method にある（確定）

`AnalysisMethod.line_width`（既定 1.2）がプロジェクト全体の線幅で、画面
（Matplotlib / PyQtGraph）、図の出力、レポート、3D のすべてが読んでいます。
**永続フィールドは既にあり、UI がないだけです。**

対処方針: `AxisLabelsDialog` に数値入力を 1 つ足して `line_width` を編集
できるようにします。**スキーマは触りません。**

> **要判断。** トレースごとの太さにはしません。ヌ(#221) で線種は `Dataset` の
> 永続フィールドになりましたが、太さを個別化するとスキーマ変更になります。
> 利用者の要求は「軸・ラベル設定に追加」なので、**そのダイアログが編集する
> Method 全体の設定**にするのが素直です。個別化が必要なら別 Issue にしてください。

### 9.12 ホーム画面の「選択」「表示」チェックの現状（確定）

- 表そのものは `SelectRows` + `ExtendedSelection` で、**Shift / Ctrl の行選択は
  既に効きます**。`_sync_dataset_selection_checkboxes` が「選択」列のチェックと
  双方向に同期します（オ #179）。
- 足りないのは 2 点です。
  1. 「選択」列のチェックボックスを**直接クリック**したとき、
     `_apply_dataset_checkbox_selection` が全チェック行から選択を作り直すため、
     Shift / Ctrl の修飾が効きません。
  2. 「表示」列のチェックは `_dataset_item_changed` で**その行だけ**を変えます。
     複数行を選んでいても、まとめて表示・非表示にはなりません。

対処方針: 複数行を選択した状態でどちらかのチェックを操作したら、選択中の行すべてに
同じ状態を適用する。**「すべて表示」「すべて非表示」ボタンは残してください**
（`AGENTS.md` の「既存機能を消さない」）。一括変更は Undo / Redo の 1 ステップに
してください。

### 9.13 「データ」欄から外すボタンの行き先は既にある（確定）

ホーム画面の「データ」欄は現在 6 つ（読み込み・削除・詳細・定量条件・グラジエント・
選択を同一 Run へ・選択を Run から分離）、「解析」欄は 2 つ（測定・試料条件の入力・
表示設定）です。

外すことになる「詳細・定量条件」と「グラジエント」は、**チ(#214) で
`BatchMetadataDialog` に `edit_details_button`（選択行の詳細設定）と
`edit_gradient_button`（選択行のグラジエント）として既に入っています。**
Run の統合・分離も同ダイアログにあります。

つまり今回の変更は**削除ではなく整理**です。`AGENTS.md` の「既存機能を新しい操作の
実装過程で削除しない」に抵触しません。**どこから使えるかを PR に書いてください。**

### 9.14 「表示設定」の対象トレースは固定ラベル（確定）

`DisplaySettingsDialog` は `dataset`（現在行）と `selected_datasets`（選択行）を
受け取り、対象トレースを**編集不可の `QLabel`** で見せています。

```python
        target = dataset.label or dataset.original_filename
        form.addRow("対象トレース" ..., QtWidgets.QLabel(target))
```

`change_color` は `self.project.datasets` 全体から `resolved_colors` を作って
渡しているので、**全トレースの情報は既にダイアログに届いています。**
プルダウンにするのに追加のデータは要りません。

> **要判断。** プルダウンで対象を切り替えたとき、**単色モードの色と線種だけ**が
> その行に向くようにし、グラデーションモードは従来どおり表の選択順に従う、
> というのが既定案です。切り替え前に触った内容を保持するかどうかを決めて、
> テストで固定してください。

### 9.15 上部バーとマウスモードの対応の現状（確定）

上部バーは、自作アクションを先頭に差し込んだ Matplotlib の
`NavigationToolbar2QT` です（`AxisAwareNavigationToolbar`）。左から順に:

| # | 項目 | 由来 | 対応するマウスモード |
|---|---|---|---|
| 1 | 縦線ポインター（アイコン + 文字） | 自作 `pointer_action` | `pointer` |
| 2 | 区切り | | |
| 3 | Home / Back / Forward | Matplotlib | なし（表示履歴） |
| 4 | 区切り | | |
| 5 | Pan（交差矢印） | Matplotlib | `normal` |
| 6 | Zoom（矩形） | Matplotlib | `normal`（**2 アイコンで 1 モード**） |
| 7 | Subplots（3 本バー） | Matplotlib | なし |
| 8 | Customize（折れ線と矢印） | Matplotlib / Qt バックエンド専用 | なし |
| 9 | 区切り | | |
| 10 | Save | Matplotlib | なし |

`MOUSE_MODE_IDS` は 8 つ（`normal` / `pointer` / `select` / `integrate` /
`edit_peak` / `split_peak` / `move_trace` / `annotation`）ですが、
**上部バーにあるのは `pointer` と `normal` だけ**です。残りの 6 つは
グループ欄の `QPushButton` と、`annotation_action`（編集メニューと
「表示」欄の `annotation_button`）にあります。

`_mouse_mode_changed` は既にモード ID → コントロールの対応表を持っています。

```python
        controls = {
            "pointer": self.pointer_action,
            "integrate": self.integrate_button,
            "edit_peak": self.edit_peak_button,
            "split_peak": self.split_peak_button,
            "move_trace": self.move_trace_button,
            "annotation": self.annotation_action,
        }
```

**この対応表がそのまま上部バーのアイコン一覧になります。** 新しいモード管理を
作る必要はありません。`select` と `normal` がこの表に無い（前者は span selector、
後者は第 9.1 節のとおり何もしない）ので、その 2 つだけ足りません。

### 9.16 軸・ラベル設定のアイコンが 2 つある理由（確定）

Qt バックエンドの `NavigationToolbar2QT` は、Matplotlib 共通の
`toolitems` に **Qt 専用の "Customize" を 1 つ足します**。

```text
('Subplots',  'Configure subplots',                      'subplots')
('Customize', 'Edit axis, curve and image parameters',   'qt4_editor_options')
```

アイコンの実物は次のとおりです（`mpl-data/images/` を確認）。

- `subplots.png` — **横 3 本のバー**（利用者が「3 本バーのアイコン」と呼んでいるもの）
- `qt4_editor_options.png` — **右上向きの矢印を含む折れ線**（「矢印のアイコン」）

`AxisAwareNavigationToolbar` は、PyQtGraph 表示のとき **両方とも
`edit_axis_labels()` に差し替えています**。

```python
    def configure_subplots(self):
        if owner is not None and getattr(owner, "_screen_preview", None) is not None:
            owner.edit_axis_labels()
            return
        return super().configure_subplots()

    def edit_parameters(self):
        # 同じ内容
```

つまり **Windows 11 の既定（PyQtGraph）では完全な重複**です。利用者の指摘は
正確です。

**ただし Matplotlib 描画のときは別物です。** `configure_subplots` は
Matplotlib の余白調整ダイアログ、`edit_parameters` は Matplotlib の
図オプション（軸範囲・曲線の色）ダイアログを開きます。**Customize を消すと、
Matplotlib 経路ではその図オプションが使えなくなります。**

対処方針: **Customize（矢印）を上部バーから外し、3 本バーを残す**（2026-09-02
利用者判断）。そのうえで、3 本バーは**描画方式にかかわらず**アプリの
「軸・ラベル・書式設定」を開くようにしてください。描画方式で開くものが変わる方が
利用者には分かりにくく、マ(#241) がそのダイアログを拡張するので、そちらに
寄せるのが筋です。**失われる Matplotlib 図オプションの中身（軸範囲・曲線の色）は
アプリ側のダイアログと「表示設定」で足りることを確認してから外してください。**

### 9.17 矩形ズームは上部バーの zoom アクションに完全に依存している（確定）

**アイコンを消すだけにすると、矩形ズームの機能ごと失われます。** 経路は 2 つとも
`toolbar._actions["zoom"]` を見ています。

- Matplotlib: `NavigationToolbar2` の標準実装
- PyQtGraph: `screen_preview._handle_zoom_event` の**先頭が門番**

```python
    def _handle_zoom_event(self, name, event):
        owner = self.owner
        if not owner.toolbar._actions["zoom"].isChecked():
            if self._zoom_drag is not None:
                self.cancel_zoom_drag()
            return False
```

PyQtGraph 側はラバーバンド矩形を自前で描いており（`set_zoom_rectangle`）、
`method.zoom_axis` の設定（自動 / X+Y / X / Y）にも従います。**素性の良い実装が
既にあります。**

対処方針: `QToolBar.removeAction()` でアイコンを外しても、
**`toolbar._actions["zoom"]` の `QAction` オブジェクトは辞書に残ります。**
表に出さないまま状態保持だけに使えるので、上の門番も
`_toolbar_navigation_triggered` も書き換えずに済みます。そのうえで、
**「通常」モードの右ドラッグ**に矩形ズームを割り当ててください。Matplotlib の
pan ツールの説明文が元々「左ボタンでパン、右ボタンでズーム」なので、
モード名「通常（パン／ズーム）」とも一致します。

**`AGENTS.md` の「新しい操作の実装過程で既存のマウス機能を削除しない」に
直接あたります。** アイコンを消すのは利用者の明示的な要求ですが、**機能まで
消してよいとは言われていません。** 消す場合は先に Issue へコメントしてください。

なお `_handle_zoom_event` は `event.hit_region in ("plot", "plot_y1", "plot_y2")`
を要求します。**第 9.2 節の当たり判定の不具合により、グリッド表示中は矩形ズームも
効きません。** 9.2 を直すと一緒に直ります。

### 9.18 「移動・ズーム」欄の行順（確定）

`navigation_group`（日本語ラベル「移動・ズーム」）のグリッドは次の順です。

| 行 | 内容 |
|---|---|
| 0 | マウスモード（ラベル + プルダウン） |
| 1 | **スペクトル移動 / 移動方向 / 移動を元に戻す**（`move_controls`） |
| 2 | ズーム方向（ラベル + プルダウン） |
| 3 | 表示モード（ラベル + プルダウン） |
| 4 | 縦線ポインター（`pointer_control_button`） |
| 5 | 全体表示 / X軸全体 / Y軸全体（`reset_buttons`） |
| 6 | 伸縮用の空行 |

要求は「行 1 を行 5 の下へ移す」ことです。`addLayout` の行番号を入れ替え、
`setRowStretch` の対象行を最後尾に付け直すだけで済みます。**ウィジェットの
生成順や接続は変えないでください。**

---

## 10. バッチ定義（ハ行・マ行）

今回は 20 件の要求を 7 バッチにまとめました。要求 1 件だけの Issue を作らないよう、
**同じモジュール・同じダイアログを触るものを 1 つにまとめて**います。

### 10.0 実施順と依存関係

```
ハ(#236) ──► ヒ(#237)          （どちらも pyqtgraph_scene.py を触るため直列）
フ(#238)                      （独立。peak_fitting.py のみ）
ヘ(#239) ──► ホ(#240)          （どちらも dialogs.py を触るため直列）
マ(#241)                      （独立。dialogs.py で ヘ・ホ と重なる）
ミ(#242)                      （独立。plot3d.py と report.py）
```

| 記号 | Issue | 内容 | 優先 | 依存 |
|---|---|---|---|---|
| ハ | #236 | 上部バーの整理、マウスモードとの 1 対 1 同期、グリッド時の当たり判定 | 高 | なし |
| ヒ | #237 | 積分ハイライトの塗り替えと用語の修正 | 中 | #236 |
| フ | #238 | 飽和ピーク補正のフィット精度 | 高 | なし |
| ヘ | #239 | 「測定・試料条件の入力」ダイアログの操作性 | 高 | なし |
| ホ | #240 | ホーム画面の表とボタン配置、対象トレースの選択 | 中 | #239 |
| マ | #241 | 「軸・ラベル・書式設定」への改名、線の太さと目盛間隔の下限 | 中 | なし |
| ミ | #242 | 3D グリッドの表示形式とレポート続きページの余白 | 低 | なし |

**依存と重なりの理由**

- **ハ → ヒ**: どちらも `pyqtgraph_scene.py` を触ります。ハ は `_pointer_region`、
  ヒ は `set_peak_selection` と別の関数ですが、並行させると rebase が要ります。
  当たり判定の不具合の方が利用者への影響が大きいので **ハ を先に**。
- **ヘ → ホ**: どちらも `dialogs.py` を触ります。ヘ は `BatchMetadataDialog`、
  ホ は `DisplaySettingsDialog` と別クラスなので致命的ではありませんが、
  後にマージする方を rebase してください。ホ がホーム画面から外すボタンの行き先は
  ヘ が触るダイアログなので、**ヘ を先に**通す方が確認しやすくなります。
- **マ は独立**ですが `dialogs.py`（`AxisLabelsDialog`）で ヘ・ホ と重なります。
  別クラスなので並行できます。後にマージする方を rebase してください。
- **フ と ミ は独立**。フ は `peak_fitting.py`、ミ は `plot3d.py` と `report.py`
  が中心で、`gui.py` にほとんど触りません。

> **並行数の目安は 2 つまで。** タ行と同じです。`gui.py` は ハ・ヘ・ホ・マ が
> 触ります。3 つ以上を同時に走らせると rebase が連鎖します。先にマージされた方に
> 他方を rebase し、その旨を PR に書いてください。

---

### ハ — 上部バーの整理、マウスモードとの 1 対 1 同期、グリッド時の当たり判定

**Issue: #236** / **依存なし** / バッチ「ヒ」(#237) の前提

方針: **上部バーのアイコンとマウスモードを 1 対 1 にし、同じ 1 つの状態を
指すようにする。あわせて、グリッドが当たり判定を壊している原因を取り除く。**

対象要求
- マウスモードを「通常（パン・ズーム）」にしたときの挙動を、上部バーの交差矢印
  （left button pans, ...）を押した状態と同じにする
- 「通常（パン・ズーム）」を選んでいるのにパン・ズームができない。現状は上部バーの
  ボタンを押している時しかできない
- PyQtGraph 表示かつ「主目盛グリッドを表示」のとき、パンが x 方向しか効かない
- 上部バーのアイコンの説明文（ツールチップ）を、日本語 UI では日本語にする
- 上部バーのアイコンを整理する。**マウスモードの選択肢とアイコンが 1 対 1 に
  対応し、常に同期する**ようにする
- 軸・ラベル設定のアイコンが 2 つあるので、**3 本バーのアイコンに統一し、
  矢印のアイコンは削除する**
- 「移動・ズーム」欄の「スペクトル移動〜移動を元に戻す」の行を、
  **「全体表示〜Y軸全体」の下（一番下）へ移す**

受け入れ条件
- [x] マウスモードで「通常（パン・ズーム）」を選ぶと、上部バーの pan が押された
      状態になる（`_ensure_normal_mode_navigation`、
      `test_toolbar_pan_click_syncs_mouse_mode_and_normal_mode_preserves_zoom`）
- [x] その状態で、クロマトグラム上のドラッグがそのままパンになる（上部バーを
      押し直す必要がない）
- [x] 上部バーの pan / zoom を押すとマウスモードが「通常」に戻る既存の連動が
      退行していない（`_toolbar_navigation_triggered` は変更なし。同テストで
      `.trigger()` を使い実クリックを再現して確認）
- [x] 「通常」を選んだ時点で zoom が有効だった場合の扱いが決まっており、テストで
      固定されている（zoom が有効ならそのまま残す。同テストで固定）
- [x] マウスモードと上部バーが相互に呼び合って無限ループしない
      （`QAction.setChecked()` は `triggered` を発火しないため再入なし。同テストで確認）
- [x] 「主目盛グリッドを表示」を ON にしても、PyQtGraph でプロット面のドラッグが
      x・y 両方向のパンになる（`_pointer_region` でプロット矩形を軸帯より先に判定）
- [x] グリッド ON のとき、プロット面上の点の `hit_region` が `plot`（分割表示では
      `plot_y1` / `plot_y2`）であることをテストで固定する
      （`test_screen_grid_does_not_break_plot_hit_region_in_either_view_mode`）
- [x] 分割表示でも同じであることをテストで固定する（同テスト、`split_y_axes` も検証）
- [x] 軸の帯をドラッグしたときの単軸パン（X だけ、Y だけ）が退行していない
      （同テストでプロット矩形の外側にある軸帯の点が `x` に解決されることを確認）
- [x] グリッド ON のとき、他のマウスモード（積分・選択・縦線など）も従来どおり効く
      （`_pointer_region` の変更は当たり判定の優先順位のみで、モードごとの処理には
      触れていない。既存のモード別テスト群が回帰なしで通過）
- [x] 日本語 UI で上部バーの各アイコンのツールチップが日本語になる
      （`i18n.py` に `toolbar_*_tooltip` を追加し `_retranslate()` で適用）
- [x] Home のツールチップが現在の挙動（全体表示）と一致する文言になっている
      （`toolbar_home_tooltip` = "全体表示"）
- [x] Subplots のツールチップが、実際に開くダイアログと一致している
      （マ #241 後は `toolbar_subplots_tooltip` = "軸・ラベル・書式設定…"。
      ダイアログの実体は
      `AxisLabelsDialog`。`test_subplots_action_opens_axis_dialog_on_both_renderers_and_customize_is_removed`）
- [x] Matplotlib 描画のパン・ズームが退行していない（フルテストスイート 375 件通過）
- [x] 上部バーに、マウスモード 8 個それぞれのアイコンが 1 つずつある
      （`normal`=Pan、`pointer`=既存アイコン、`select`/`integrate`/`edit_peak`/
      `split_peak`/`move_trace`/`annotation` を新規追加）
- [x] Pan と Zoom が「通常（パン／ズーム）」の 1 アイコンに統合されている
      （Zoom アイコンを `_install_zoom_preservation` で非表示化。`QAction` 自体は
      `toolbar._actions["zoom"]` に残す）
- [x] マウスモードのプルダウン・上部バーのアイコン・グループ欄のボタンの
      3 つが常に同じモードを指す（どれを操作しても他の 2 つが追従する）
      （`_bind_mode_control_pair` による汎用の双方向バインド。
      `test_mode_toolbar_icons_three_way_sync_with_group_controls_and_combo`）
- [x] 同時に 2 つ以上のモードアイコンが押された状態にならない（同テストで確認）
- [x] グループ欄のモード用ボタンが残っており、退行していない
- [x] **矩形ズームが引き続き使える**。到達手段が決まっており、テストで固定されている
      （「通常」モードの右ドラッグ。Matplotlib は既存のパンツールがネイティブ対応、
      PyQtGraph は `screen_preview._handle_zoom_event` を拡張。
      `test_preview_right_drag_zooms_while_normal_mode_pan_is_active`）
- [x] 矩形ズームが「ズーム方向」の設定（自動 / X+Y / X / Y）に従う挙動を保っている
      （`_handle_zoom_event` の `configured = owner.project.method.zoom_axis` 決定ロジックは
      変更していない。既存の `test_preview_rectangle_zoom_modes_history_and_cancellation` が
      引き続き通過）
- [x] 軸・ラベル設定を開くアイコンが 3 本バーの 1 つだけになっている
- [x] 矢印のアイコンが上部バーから消えている（Customize を `_install_zoom_preservation` で除去）
- [x] 3 本バーのアイコンが、Matplotlib 描画でも PyQtGraph 描画でも同じダイアログを開く
      （`AxisAwareNavigationToolbar.configure_subplots`/`edit_parameters` はどちらも
      無条件に `owner.edit_axis_labels()` を呼ぶ。同テストで両レンダラを確認）
- [x] 「移動・ズーム」欄で「スペクトル移動」の行が「全体表示〜Y軸全体」の下にある
      （`test_navigation_group_row_order_keeps_widgets_connected`）
- [x] 行の並べ替えでどのウィジェットも失われておらず、接続先も変わっていない（同テスト）
- [x] `REQUIREMENTS_STATUS.md` を更新する

実測（Windows 11 x64 / PySide6, `.venv-win11-x64`）
- `tests/test_gui.py` を含むフルスイート: 375 件成功（既存 369 件 + 新規 6 件）
- `scripts/probe_pyqtgraph_parity.py`: 全機能 `supported`
- `scripts/benchmark_integrated_screen.py`: native/legacy 比 約 1.94 倍（回帰なし）
- Python 3.8.10 x86（Windows 7 固定依存の構文チェック）: 変更ファイルすべて構文OK
- `scripts/ci_validate.py --offline-assets auto`: OK

実装中にフルスイートで見つかった既存テストの退行(4 件、すべて根本原因を特定して修正。
テスト側を回帰なしに合わせるのではなく、いずれもコード側の見落としだった)
- `test_preview_split_panels_native_navigation_and_markers`: 矩形ズームの新しい門番
  (`screen_preview._handle_zoom_event`)が、右ドラッグ用のボタン判定に一致しない
  すべての `button_press_event` を「処理済み」として飲み込んでいたため、
  「通常」モードでパンが有効なときの左ドラッグ本体のパンが起動しなくなっていた。
  一致しないボタンでは `False` を返して後続のハンドラへ委譲するよう修正
- `test_preview_text_label_create_edit_delete_and_cancel` /
  `test_preview_vertical_markers_use_native_events_and_shared_history`: 「通常」モードが
  既定でパン有効になった結果、`gui.py` の `_on_canvas_press` と
  `screen_preview.py` の `_handle_annotation_event` / `handle_event` のクリック選択
  フォールバックがそれぞれ独立に持っていた `not str(toolbar.mode)` という
  「ドラッグ中でない」を表す前提が崩れ、既存の注釈・縦線マーカーを「通常」モードから
  クリックして編集・選択する経路が 3 箇所とも塞がっていた。それぞれ、クリック位置が
  既存の注釈・マーカーそのもの(`hit_kind`)である場合だけ `toolbar.mode` の判定を
  素通りさせるよう個別に直した
- `test_reset_trace_position_restores_exact_display_and_is_one_undo_step`: 「移動・ズーム」欄の
  行番号を `1` に決め打ちしていた(9.18 の並べ替えで `5` に移動)。行番号を更新
- `test_preview_toolbar_configuration_reuses_persisted_axis_dialog`: 「通常表示のときは
  レガシーな Matplotlib の Subplots/Customize を開く」という 9.16 で意図的に置き換えた
  旧挙動を検証していたので、新しい挙動(どちらのレンダラでもアプリの軸ダイアログを開く)
  を検証するようテスト側を書き換えた。ここだけはコード側ではなくテスト側の想定が
  古かったケース
- `scripts/ci_validate.py --offline-assets auto`: OK

対象ファイル: `hplc_app/gui.py`, `hplc_app/pyqtgraph_scene.py`,
`hplc_app/screen_preview.py`, `hplc_app/i18n.py`, `tests/test_gui.py`

注意
- **原因は第 9.1・9.2・9.3 節にあります。すべて確定で、実測も付けてあります。
  再調査は不要です。**
- **第 9.2 は テ(#216) が直したつもりの症状の本当の原因です。** #216 が入れた
  「イベント所有者がグリッド変更をまたいで保たれる」テストは正しいので、
  消さないでください。当たり判定を直したうえで両方が通る状態にします。
- マウスモードは カ(#180) で入れた仕組みです。**新しいモード管理を作らないこと。**
  `_mouse_mode_changed` のモード ID → コントロール対応表（第 9.15 節）が、
  そのまま上部バーのアイコン一覧になります。足りないのは `select` と `normal`
  の 2 つだけです。
- **アイコンは 3 つ目の操作経路であって、置き換えではありません**（2026-09-02
  利用者判断）。プルダウン・アイコン・グループ欄のボタンの 3 つが同じ状態を
  指すようにしてください。グループ欄のボタンは残します。
- **Pan と Zoom は 1 アイコンに統合します**（2026-09-02 利用者判断）。ただし
  **矩形ズームの機能まで消さないでください。** 第 9.17 節のとおり、両方の描画経路が
  `toolbar._actions["zoom"]` を見ています。`removeAction()` してもその `QAction` は
  辞書に残るので、表に出さないまま状態保持に使えます。**既定案は「通常」モードの
  右ドラッグに割り当てること**です。`AGENTS.md` の「既存のマウス機能を削除しない」に
  直接あたるので、消す判断をする場合は先に Issue へコメントしてください。
- 削除するのは **Customize（矢印）**、残すのは **Subplots（3 本バー）** です
  （第 9.16 節にアイコンの実物を確認した結果があります）。3 本バーは描画方式に
  かかわらずアプリの「軸・ラベル・書式設定」を開くようにしてください。
  **Matplotlib 経路で失われる図オプションの中身が、アプリ側のダイアログと
  「表示設定」で足りることを先に確認すること。**
- 「移動・ズーム」欄の並べ替えは**行番号の入れ替えだけ**です（第 9.18 節）。
  ウィジェットの生成順や接続を変えないでください。
- ツールチップの文言は `i18n.py` に置き、英語 UI では従来の Matplotlib の文言を
  そのまま使うか、意味の合った英語に直すかを Issue で決めてください。

---

### ヒ — 積分ハイライトの塗り替えと用語の修正

**Issue: #237** / **依存: #236（ハ）**

対象要求
- 積分リストを選ぶと、線の付近だけハイライトが切り替わり、背景の塗りが切り替わらない
  （PyQtGraph のみ）
- 「すべての行を選択」を「すべてのピークを選択」に変更
- 「範囲を数値入力・編集」を「フラクション範囲を数値入力・編集」に変更

受け入れ条件
- [x] PyQtGraph で積分リストの選択を変えると、積分範囲の背景の塗りもその場で
      切り替わる（`LinearRegionItem.setBrush()` の直後に item 自身の `update()` を呼ぶ。
      `test_preview_peak_selection_restyles_without_scene_rebuild` で brush と repaint を固定）
- [x] 選択の切り替えでシーンが作り直されない（同テストで `consumer.render` と
      `MainWindow._plot` が主表・別ウィンドウ・選択解除のすべてで 0 回、view も不変）
- [x] 主表と別ウィンドウの積分リストのどちらから選んでも同じ結果になる（同テスト）
- [x] 選択を外したときに、塗りが非選択の見え方に戻る（同テストで両 region の
      alpha が 0.08、選択色でないことと repaint 3 回目を確認）
- [x] Matplotlib 描画のハイライトが退行していない
      （`test_selected_integration_is_highlighted_and_overlays_can_be_hidden` と
      `test_lightweight_overview_is_coarser_and_peak_selection_reuses_patch`）
- [x] 「すべてのピークを選択」に変わっている（日本語・英語の両方。
      `test_fraction_mouse_mode_and_inline_interval_are_not_exposed`）
- [x] 「フラクション範囲を数値入力・編集…」に変わっている（日本語・英語の両方。同テスト）
- [x] `REQUIREMENTS_STATUS.md` を更新する

実測（Windows 11 x64 / Python 3.11.9 / PySide6 6.8.3 / PyQtGraph 0.13.7）
- フルテストスイート: 375 件成功（611.874 秒）
- `scripts/probe_pyqtgraph_parity.py`: 全 11 機能 `supported`
- `scripts/benchmark_integrated_screen.py`: native 0.132 秒 / legacy 0.136 秒、
  native が約 1.03 倍高速、raw 配列 SHA-256 不変
- Python 3.8.10 x86: 変更 3 ファイルの構文チェック OK
- `scripts/ci_validate.py --offline-assets auto`: Level 2 source contracts と
  Windows 7 offline manifest / wheelhouse を含めて OK

対象ファイル: `hplc_app/pyqtgraph_scene.py`, `hplc_app/i18n.py`,
`tests/test_gui.py`

注意
- **原因は第 9.4 節にあります。確定です。** `LinearRegionItem.setBrush()` が
  `update()` を呼ばないだけなので、直すのはアイテム側の再描画です。
- **`_plot()` を呼ぶ解決にしないでください。** セ(#203) が「作り直さずに restyle
  する」設計を入れています。それを壊すと、#203 と テ(#216) のちらつき対策が
  同時に退行します。
- ラベルは `i18n.py` の `select_all_peaks` と `fraction_numeric` です。
  英語側（`Select all rows` / `Add / edit range numerically...`）も揃えてください。

---

### フ — 飽和ピーク補正のフィット精度

**Issue: #238** / **依存なし**

方針: **フィット曲線の高さを、切り取られた本来のピークの高さに戻す。**

対象要求
- 「飽和ピーク補正」のフィッティングが、添付画像の赤い曲線のように、グレーの
  クロマトグラムの本来の形を再現するようにする。現状は青い曲線のように、頭打ちの
  高さ付近で止まってしまう

受け入れ条件
- [x] EMG モデルでの飽和ピーク補正が、既知の合成ピークの高さと面積を復元する
- [x] 頭打ち率を変えた複数の水準（例 90% / 70% / 50% / 35%）でテストされている
- [x] 復元の許容差が明示され、テストで固定されている（**既定案は高さ・面積とも
      ±5%** — 実測では 70% 水準で高さ誤差 -5.71% と既定案をわずかに超えたため、
      高さ ±7% / 面積 ±4% を実測に基づく余裕を持たせた値としてテストに固定した。
      根拠は PR に実測値とともに記載）
- [x] Gaussian 経路の既存の復元精度が退行していない（既存テストがそのまま通る。
      補正は `tau is not None` のときだけ働くため Gaussian 経路は無変更）
- [x] 飽和していない通常のフィットの面積・高さ・保持時間が、この変更でどれだけ
      動くかを測り、PR に記載している（高さ約 -1.5%、面積約 -0.03%）
- [x] `amplitude_uv` が何を基準に正規化された高さなのかが、コードのコメントで
      明示されている（`_emg_true_amplitude` の docstring）
- [x] `fitted_area_uv_min` / `fitted_apex_min` / `fitted_fwhm_min` と当てはめ側の
      正規化基準が一致している（同じ `_model_grid` を再利用）
- [x] フィットの所要時間が実用的な範囲にとどまる（テスト実行時間で確認し、PR に記載。
      新規回帰テストは Windows 11 開発機で約 40 秒。**修正前後で有意差なし**——
      コストは既存の EMG グリッドサーチ自体に由来し、今回の補正が追加したのは
      1 回だけの後処理評価）
- [x] 推定値と測定値の区別（ナ #218 で入れた表・凡例・エクスポート・レポートの
      表示）が退行していない（表示ロジックは無変更。既存テストで確認）
- [x] 元の積分ピークの数値が、この変更で変化しない（テストで固定）
- [x] 修正前に保存されたプロジェクトの飽和補正行をどう扱うかが決まっており、
      Issue と PR に書かれている（**既定案どおり移行しない**。再計算で更新される）
- [x] `REQUIREMENTS_STATUS.md` を更新する

対象ファイル: `hplc_app/peak_fitting.py`, `tests/test_core.py`

注意
- **原因は第 9.5 節にあります。確定で、現状と修正後の両方の実測値が載せてあります。**
- **探索の中で密なグリッドを毎回作らないでください。実測で 2 分を超えました。**
  当てはめ後に 1 回だけ振幅を直す方法が、探索コストを増やさずに同じ結果になります。
- **これは `AGENTS.md` の科学的挙動に当たる変更です。**
  - 生の取り込み値は変更しない
  - 推定であることが利用者から見える状態を保つ
  - 数値が変わるので、テストを先に書いて固定する
- 既存テスト `test_saturated_peak_correction_recovers_the_clipped_area` は
  Gaussian を明示して呼んでいます。**EMG 版を足してください。** これが無かった
  ことが、この不具合が残った理由です。
- **飽和の判定条件（`SATURATION_MIN_RUN` ほか）は今回の対象ではありません。**
  実機で「飽和として検出されない」場合は、それは別の症状です。Issue にコメント
  してから範囲を広げてください。

---

### ヘ — 「測定・試料条件の入力」ダイアログの操作性

**Issue: #239** / **依存なし** / バッチ「ホ」(#240) の前提

対象要求
- 「元ファイル」が `G:...` としか表示されない。パスが読める形にする
- Run ID のセルをダブルクリックしたときに編集できるようにする
- 「選択行から条件をコピー」の「コピーする条件」ダイアログで、既定でチェックされて
  いる要素を **AU/V・流量・カラム** にする
- 選択を Shift / Ctrl で複数選択できるようにする
- 上部ボタンのレイアウトを整理する
  - 最上部に「選択」欄: 全データを選択・同じグループを選択・選択解除
  - その下に「操作」欄: 選択を同一 Run へ・選択を Run から分離・選択行の詳細設定・
    選択行のグラジエント・選択行から条件をコピー・ペースト（この順）
  - その下に、条件プリセット・グラジエントプリセットをこの順で配置

受け入れ条件
- [x] 元ファイル列で、末尾（ファイル名側）が読める形になっている
- [x] 列幅を変えても省略のされ方が破綻しない
- [x] Run ID セルをダブルクリックすると編集できる
- [x] 空の Run ID と、既存の Run ID との重複が拒否される
- [x] 改名が Run 全体に効き、同じ Run の他の行の表示も更新される
- [x] 改名が Undo / Redo の 1 ステップとして扱われる
- [x] 「コピーする条件」ダイアログの既定チェックが AU/V・流量・カラムの 3 つだけに
      なっている
- [x] 既定以外のチェックを変える操作が従来どおりできる
- [x] Shift / Ctrl で行をまとめて選択でき、「選択」列のチェックがそれに追従する
- [x] 「全データを選択」「同じグループを選択」「選択解除」が退行していない
- [x] 上部が「選択」「操作」「プリセット」の順に並び、各欄のボタンが指定の順序で
      並んでいる
- [x] 並べ替えでどのボタンも失われていない
- [x] Run 統合・分離、詳細設定、グラジエント、コピー、ペーストの動作が退行していない
- [x] `REQUIREMENTS_STATUS.md` を更新する

実装・実測（2026-09-04）:
- `LeftElideDelegate` を `dialogs.py` へ移してホーム表と条件表で共有し、現在の列幅を
  使う左省略と完全パスのツールチップを
  `test_batch_dialog_source_path_elides_left_at_resized_widths` で固定した。
- Run ID は同一 Run の全行へ表上で同期するが、OK までは Project を変更しない。
  OK 時は `Project.rename_run` を一時 ID 経由で検証用コピーに適用してから実体へ適用するため、
  空・重複を原子的に拒否し、Run 全体の改名と MainWindow の 1 ステップ Undo / Redo を保つ。
  `test_batch_dialog_run_id_edit_is_staged_shared_and_one_undo_step` と
  `test_batch_dialog_run_id_rejects_empty_and_duplicate_atomically` で確認した。
- コピー既定、Shift / Ctrl 選択とチェック欄の同期、3 群の順序と全ボタン保持は
  `test_condition_copy_defaults_to_auv_flow_and_column_only` と
  `test_batch_dialog_selection_sync_and_control_group_order` で固定した。既存の batch 関連
  19 テスト、条件コピー 4 テスト、Run 統合・分離 2 テストも通過した。
- Windows 11 / Python 3.11.9 x64 / PySide6 6.8.3 で全 380 テストが
  627.260 秒で成功した。Python 3.8.10 x86 の変更 Python 3 ファイルの構文確認、
  現行環境の `compileall`、`ci_validate.py --offline-assets auto`（Level 2 source contracts と
  Windows 7 オフライン manifest / wheelhouse）も成功した。スキーマ、依存 pin、科学計算、
  生データ、オフラインビルド入力は変更していない。実機での列幅変更時の読みやすさ、
  Shift / Ctrl 操作感、3 群の見た目は物理 Windows 11 確認として未確認のまま残す。

対象ファイル: `hplc_app/dialogs.py`, `hplc_app/gui.py`, `tests/test_gui.py`

注意
- **第 9.6 節（元ファイルの省略）と第 9.7 節（Run ID）に確定した内容があります。**
- `LeftElideDelegate` は `gui.py` にあります。**`dialogs.py` へ移して共有し、
  同じものを 2 つ書かないでください。**
- Run ID の改名は `Project.rename_run` を呼びます。**検証の並行実装を作らないこと。**
- ボタンの並べ替えは**見た目の変更です**。接続先のスロットを付け替えないでください。
- 「コピーする条件」の既定は `ConditionCopyDialog` が全項目を `setChecked(True)` に
  している箇所です。既定値の定義を `COPY_FIELDS` 側に持たせると、項目が増えたときに
  既定が崩れません。

---

### ホ — ホーム画面の表とボタン配置、対象トレースの選択

**Issue: #240** / **依存: #239（ヘ）**

対象要求
- 「選択」「表示」を Shift / Ctrl で複数選択できるようにする
- 「データ」欄から「詳細・定量条件」「グラジエント」を削除する。
  「Run の統一と分離」は「解析」欄に移動する
- 「表示設定」ダイアログの「対象トレース」が現在は選択中のものだけになっている。
  プルダウンで全スペクトルから選べるようにする

受け入れ条件
- [x] 複数行を選択した状態で「表示」チェックを操作すると、選択中の行すべてに
      同じ状態が適用される
- [x] 複数行を選択した状態で「選択」チェックを操作したときの挙動が決まっており、
      テストで固定されている
- [x] 一括変更が Undo / Redo の 1 ステップとして扱われる
- [x] 「すべて表示」「すべて非表示」ボタンが残っており、退行していない
- [x] 「データ」欄に「詳細・定量条件」「グラジエント」が出ていない
- [x] 「Run の統一と分離」が「解析」欄にある
- [x] 外した 2 つの機能が、「測定・試料条件の入力」ダイアログから引き続き使える
      ことを PR に明記している
- [x] 「表示設定」ダイアログの対象トレースがプルダウンになり、プロジェクト内の
      全トレースから選べる
- [x] プルダウンで対象を変えると、その行の現在の色と線種が反映される
- [x] グラデーションモードが従来どおり表の並び順で色を割り当てる
- [x] 適用が Undo / Redo の 1 ステップとして扱われる
- [x] `REQUIREMENTS_STATUS.md` を更新する

実装・実測（2026-09-04）:
- オ(#179) の行選択・チェック同期を維持し、チェックを押す直前の複数選択行だけを
  一括対象として保持した。「表示」はクリックした状態を全対象へ適用し、1 回の
  Undo / Redo にまとめる。「選択」もクリックした状態を全対象へ適用するため、既に
  選択済みの複数行でチェックを外すと全行が同時に選択解除される。修飾キーで行を
  追加する従来経路と、選択状態だけでは Project を変更しない性質は維持した。
  `test_selected_visibility_checkbox_updates_all_rows_as_one_undo_step`、
  `test_selected_selection_checkbox_clears_all_rows_without_project_edit`、既存の
  `test_dataset_selection_checkboxes_and_row_selection_stay_synchronized` で固定した。
- ホームの「データ」は追加・削除だけとし、「解析」は測定・試料条件、表示設定、
  Run 統合、Run 分離の順にした。全表示・全非表示はスペクトル表示欄に残した。
  詳細・定量条件とグラジエントは削除ではなく、#239 で整理済みの「測定・試料条件の入力」
  ダイアログの「選択行の詳細設定」「選択行のグラジエント」から引き続き利用できる。
- 表示設定の対象を全 Project トレースのプルダウンにし、切替時は既定案どおり切替前の
  未確定単色編集を保持せず、対象行の明示色（空なら既存の解決済み色）と線種を読み直す。
  単色の適用対象だけが切り替わり、グラデーションは従来どおりホーム表の選択行を表順で
  処理する。新しい対象切替と 1 ステップ Undo / Redo、既存グラデーション回帰を確認した。
- Windows 11 / Python 3.11.9 x64 / PySide6 6.8.3 で全 383 テストが
  652.017 秒で成功した。Python 3.8.10 x86 の変更 Python 3 ファイルの構文確認、
  現行環境の `compileall`、`ci_validate.py --offline-assets auto`（Level 2 source contracts と
  Windows 7 オフライン manifest / wheelhouse）も成功した。列定数、保存済み列順、スキーマ、
  依存 pin、科学計算、生データ、オフラインビルド入力は変更していない。実機での
  Shift / Ctrl マウス操作、ボタン配置、対象トレース切替の操作感は未確認のまま残す。

対象ファイル: `hplc_app/gui.py`, `hplc_app/dialogs.py`, `hplc_app/i18n.py`,
`tests/test_gui.py`

注意
- **第 9.12・9.13・9.14 節に確定した内容があります。**
- **これは削除ではなく移動です**（第 9.13 節）。外す 2 つのボタンの機能は、
  チ(#214) が入れたダイアログに既にあります。**どこから使えるかを PR に
  書いてください。**
- 「選択」列と行選択の同期は オ(#179) の仕組みです。**作り直さないでください。**
- タ(#213) が入れた列順・列 ID の対応表を壊さないこと。列インデックス定数を
  振り直さないでください。

---

### マ — 「軸・ラベル・書式設定」への改名、線の太さと目盛間隔の下限

**Issue: #241** / **依存なし**（`dialogs.py` で ヘ・ホ と重なるので、後にマージする方を rebase）

対象要求
- クロマトグラムの線の太さを選べるようにする。「軸・ラベル設定」を
  「軸・ラベル・書式設定」に改名し、そこに追加する
- 「軸・ラベル設定」の目盛間隔を、**最低 0.1 分**から選べるようにする。
  細かく設定でき過ぎると、誤操作で点数を増やしすぎたときにアプリが落ちるため

受け入れ条件
- [x] ダイアログの名称が「軸・ラベル・書式設定」になっている（タイトル・ボタン・
      履歴ラベル・英語 UI のすべて）
- [x] 線の太さをそのダイアログで変更できる
- [x] 変更が画面（Matplotlib / PyQtGraph）・図の出力（PNG / SVG / PDF）・レポートに
      反映される
- [x] 変更がプロジェクトに保存され、再読込後も保たれる（既存の `Method.line_width`
      をそのまま使い、スキーマを変えていない）
- [x] 変更が Undo / Redo の 1 ステップとして扱われる
- [x] 主目盛・副目盛の間隔の下限が 0.1 分になっている（入力欄の単位は分のまま）
- [x] 下限より小さい値を入力できない
- [x] 生成される目盛の本数に上限があり、超える場合は適用前に警告が出る
- [x] 警告を無視して落ちる経路が残っていない（表示範囲を広げた場合も含む）
- [x] 既存プロジェクトの目盛設定が、読み込み後も同じ値のまま
- [x] 自動モードの目盛が退行していない
- [x] `REQUIREMENTS_STATUS.md` を更新する

実装・実測（2026-09-05）:
- ダイアログタイトル、ホームボタン、上部バーのツールチップ、Undo 履歴を日本語の
  「軸・ラベル・書式設定」と英語の “Axes, labels and formatting” に統一した。
  既存の Project 全体設定 `AnalysisMethod.line_width` を編集する 0.1〜10.0 の線幅欄を
  追加し、個別トレースフィールドやスキーマは追加していない。既存の画面、PNG / SVG /
  PDF、レポート、3D の共有線幅経路をそのまま利用し、保存・再読込と 1 ステップの
  Undo / Redo を回帰テストで確認した。
- 主目盛・副目盛欄は分単位のまま下限 0.1、表示桁 1 にした。現在の X 表示幅から
  主・副目盛の合計本数を保守的に見積もり、5,000 本を超える設定は OK の前に推定本数を
  示して拒否する。共有の描画ガードを `MultipleLocator` と `setTickSpacing` の両方に置き、
  0.1 分未満の旧保存値、無効な主副関係、または後から表示範囲を広げて 5,000 本を超えた
  場合は保存値を変更せず自動目盛へフォールバックする。自動モードの計算は変更していない。
- Windows 11 / Python 3.12.7 x64 / PySide6 6.8.3 で全 386 テストが 718.132 秒で成功した。
  PyQtGraph 0.13.7 parity probe は全 11 機能 supported。Python 3.8.10 x86 の変更 Python
  7 ファイルの構文確認、現行環境の `compileall`、`ci_validate.py --offline-assets auto`
  （Level 2 source contracts と Windows 7 オフライン manifest / wheelhouse）も成功した。
  依存 pin、科学計算、生データ、永続フィールドの単位、オフラインビルド入力は変更していない。
  実機でのダイアログ配置、各出力の見た目、広い表示範囲での操作感は未確認のまま残す。

対象ファイル: `hplc_app/dialogs.py`, `hplc_app/gui.py`,
`hplc_app/screen_preview.py`, `hplc_app/i18n.py`, `tests/test_gui.py`

注意
- **第 9.9 節（目盛の下限と本数）と第 9.11 節（線の太さ）に確定した内容があります。**
- **線の太さはプロジェクト全体の設定です。** `Method.line_width` が既にあり、
  画面・出力・レポート・3D のすべてが読んでいます。**トレースごとにしないで
  ください。** スキーマ変更になります。
- **下限は 0.1 分です**（2026-09-02 利用者判断）。入力欄の単位は分のままにし、
  秒に変えないでください。`setRange(0.1, ...)` と `setDecimals(1)` で足ります。
- 下限だけで落ちる原因はほぼ消えますが、**表示範囲は利用者が広げられる**ので、
  本数の上限と事前警告も入れてください。ト(#217) がフラクションの区切り線で
  同じ形を入れています。**その形に揃えること。**
- 目盛の適用は Matplotlib（`MultipleLocator`）と PyQtGraph（`setTickSpacing`）の
  **2 経路あります。両方に効かせてください。**
- 永続フィールドの単位（分）は変えないこと。

---

### ミ — 3D グリッドの表示形式とレポート続きページの余白

**Issue: #242** / **依存なし**

方針: **どちらも Matplotlib の図の体裁の変更で、スキーマにも数値にも触りません。**

対象要求
- 3D クロマトグラムのグリッド線の表示形式を選べるようにする。既定は
  **保持時間 x 系列 平面のみ**
- 解析レポートの 2 ページ目以降にピークリストがはみ出す場合、上下の余白をかなり
  狭くする

受け入れ条件
- [x] 3D 表示でグリッドの表示形式を選べる（保持時間×系列、保持時間×強度、
      系列×強度の3平面を独立チェックでき、全解除で「なし」。
      `test_3d_dialog_selects_grid_planes_and_resets_only_the_view_angles`）
- [x] 既定が「保持時間 x 系列 平面のみ」になっている
      （`ThreeDPlotOptions.grid_xy=True`、他2平面は `False`。
      `test_3d_grid_planes_reach_the_figure_and_its_image_output`）
- [x] 選んだ形式がプレビューと書き出した画像の両方に反映される
      （5通りのコレクションIDとPNGバイト差、およびダイアログの即時プレビューを同2テストで確認）
- [x] 私用 API（`_axinfo` など）を使っていない
      （公開 `get_*ticks` / `get_*lim` と `Line3DCollection` / `add_collection3d` のみ。
      ソース契約も同コアテストで確認）
- [x] コ(#185) / #204 で入れた他の 3D 設定と視点リセットが退行していない
      （既存 `test_3d_chromatogram_uses_selected_order_existing_styles_and_full_raw_data`、
      上記GUIテストとフルスイート）
- [x] レポートの 2 ページ目以降の上下余白が狭くなり、1 ページに入る行数が増えている
      （上下 0.975 / 0.035、40行から48行へ。
      `test_a4_report_continuation_fits_48_rows_and_keeps_footer_clear`）
- [x] 行が重ならず、フッター（日時）が表に食い込まない
      （48行の各セル境界、表と軸境界、日時との間隔を描画後の実寸で同テストが確認）
- [x] ピーク数がページ境界ちょうどのときに空ページや欠落が出ない
      （先頭20行＋48行×2の116件と、さらに1件の117件を同テストで確認）
- [x] レポート 1 ページ目の体裁が変わっていない
      （既存 top=0.95 / bottom=0.055 と既存混雑時分離テストを確認）
- [x] `REQUIREMENTS_STATUS.md` を更新する

実測（2026-09-05）:
- Windows 11 / Python 3.11.9 x64 / PySide6 6.8.3 / Matplotlib 3.10.1 /
  NumPy 2.2.3 / PyQtGraph 0.13.7 でフルスイート **387件**が
  **646.853秒**で成功した。
- Python 3.8.10 x86 で変更Python 5ファイルの構文チェックに成功した。
  現行環境の `compileall`、`ci_validate.py --offline-assets auto` の Level 2
  ソース契約と Windows 7 オフライン manifest / wheelhouse 検証も成功した。
- スキーマ、依存pin、科学計算、生データ、既存の1ページ目レイアウト、
  オフラインビルド入力は変更していない。3D平面のネイティブ画面での見やすさと、
  48行レポートの物理印刷は既存の実機ゲートに残す。

対象ファイル: `hplc_app/plot3d.py`, `hplc_app/dialogs.py`, `hplc_app/report.py`,
`hplc_app/i18n.py`, `tests/test_core.py`, `tests/test_gui.py`

注意
- **第 9.8 節（3D グリッド）と第 9.10 節（レポート余白）に確定した内容があります。**
- Matplotlib には平面ごとのグリッド API がありません。`Line3DCollection` で
  自分で描いてください。**Plotly を持ち込まないこと**（`CODEX_BRIEF.md`）。
- `ThreeDPlotOptions` はセッション限りです。**プロジェクトに保存しないでください。**
- レポートの 1 ページ目は ニ(#219) と #182 で調整済みです。**触らないこと。**

---

## 11. 未決定事項（ハ行・マ行）

| 項目 | 内容 | 既定案 | 判断者 |
|---|---|---|---|
| 古い目盛値の扱い | マ節。0.1 分未満の間隔を保存した古いプロジェクトを読んだとき | **そのまま描く（下限は入力側だけにかける）** | 実装時に決めてテストで固定 |
| 目盛の本数上限 | マ節。何本を上限にするか | **ト(#217) の 5,000 本に揃える** | 実装時に決めてテストで固定 |
| 「通常」選択時の zoom | ハ節。「通常」を選んだ時点で zoom が有効だった場合、pan に切り替えるか zoom を残すか | **zoom が有効ならそのまま残す** | 実装時に決めてテストで固定 |
| 矩形ズームの到達手段 | ハ節（第 9.17 節）。アイコンを統合した後、矩形ズームをどの操作に割り当てるか | **「通常」モードの右ドラッグ** | 実装時に決めてテストで固定 |
| Matplotlib 図オプションの扱い | ハ節（第 9.16 節）。Customize を消すと Matplotlib 経路の図オプションが使えなくなる | **外す。アプリのダイアログで足りることを確認したうえで** | 実装時に確認して Issue へコメント |
| 上部バーの英語文言 | ハ節。Home の "Reset original view" は現在の挙動と食い違う。英語側も直すか | **英語側も現在の挙動に合わせる** | 実装時に Issue へコメント |
| フィット振幅の許容差 | フ節。復元精度をどこまで要求するか | **高さ・面積とも ±5%、頭打ち率 65% まで** | 実装時に決めてテストで固定 |
| 保存済み飽和補正行の扱い | フ節。修正前に保存された `amplitude_uv` を移行するか | **移行しない。再計算で更新されると明記する** | 実装時に Issue へコメント |
| 「選択」チェックの一括操作 | ホ節。複数行選択中に「選択」チェックを押したときの意味 | **選択中の行すべてに同じ状態を適用** | 実装時に決めてテストで固定 |
| 対象トレース切り替え時の保持 | ホ節。プルダウンで対象を変えたとき、変更途中の色・線種を保持するか | **保持しない（切り替え先の現在値を表示）** | 実装時に決めてテストで固定 |
| 3D グリッドの選択肢の粒度 | ミ節。平面ごとのチェックにするか、いくつかの組み合わせから選ばせるか | **平面ごとのチェック 3 つ、既定は保持時間 x 系列のみ** | 実装時に提案し、実機確認で調整 |
| 元ファイル列の幅 | ヘ節。固定幅のままにするか、内容に合わせるか | **利用者が広げられるようにし、省略は左側にする** | 実装時に決める |

### 決定済み（ハ行・マ行）

| 項目 | 決定 |
|---|---|
| 線の太さの粒度 | **プロジェクト全体（`Method.line_width`）**。トレースごとにはしない。スキーマを触らない |
| 3D グリッドの保存 | **保存しない**。`ThreeDPlotOptions` はセッション限りのまま |
| 目盛間隔の下限と単位 | **入力欄は分のまま、下限 0.1 分**（2026-09-02 利用者判断）。`x_major_tick_min` / `x_minor_tick_min` の意味も変えない |
| ホーム画面から外すボタン | **移動であって削除ではない**。行き先は「測定・試料条件の入力」ダイアログ（既存） |
| 今回のスキーマ変更 | **なし**。7 バッチのいずれも `.hplcproj` の形式を変えない |
| 飽和判定条件の見直し | **今回の対象外**。第 9.5 節は振幅の正規化だけを直す |
| 上部バーと Pan / Zoom | ~~**1 アイコンに統合**（2026-09-02 利用者判断）~~ **2026-09-03 に撤回。ズームは独立した機能として戻す（第 12.0 節）。**1 対 1 対応の要求は残るので、`zoom` モードを足して 9 対 9 にするのが既定案 |
| モード用ボタンの置き場所 | **グループ欄に残して 3 者を同期**（2026-09-02 利用者判断）。プルダウン・アイコン・ボタンのどれを操作しても他が追従する |
| 軸・ラベル設定のアイコン | **3 本バー（Subplots）を残し、矢印（Customize）を削除**（2026-09-02 利用者判断） |

---

# マ行（後半）・ヤ行（2026-09-03 の実機フィードバック）

第 12 節から第 14 節までが今回のバッチです。読む順序（第 0 節）、1 Issue あたりの
手順（第 3 節）、検証コマンド（第 4 節）、完了条件（第 7 節）は変わりません。

---

## 12. 事前調査で判明した技術的事実（ム・メ・モ・ヤ）

`main` = `8ecd0bf` 時点です。**行番号ではなく関数名で探してください。**
計測環境は第 9 節と同じ Windows 11 の `.venv-win11-x64` です。

### 12.0 先に読むこと — ズームの扱いが 2026-09-02 の決定と逆になりました

**「やっぱりズーム機能を復活させたい」という判断が 2026-09-03 に出ました。**
これは第 11 節の決定済み表にある **「上部バーと Pan / Zoom → 1 アイコンに統合」を
覆します。**

- 2026-09-02: Pan と Zoom を 1 アイコンに統合し、矩形ズームは「通常」モードの
  右ドラッグへ逃がす（ハ #236 の受け入れ条件）
- 2026-09-03: **ズームは独立した機能として戻す**

**ハ(#236) はこの判断が出た時点で実装中でした。** #236 の該当部分は
「Zoom アクションを `removeAction()` し、右ドラッグに逃がす」という形で既に
書かれ、その後 PR #254 でマージされました。**撤回された部分は #249 で
作り直します。** 経緯は両 Issue に記録します。

第 11 節の 1 対 1 対応の要求は取り下げられていません。したがって
**マウスモードに `zoom` を足して 9 モード ↔ 9 アイコンにする**のが、両方の要求を
満たす形です（2026-09-02 に提示した選択肢のうち、当時採らなかった案）。
**異なる整理が良ければ進める前に Issue へコメントしてください。**

### 12.1 描画のたびに一度「全体表示」が見える理由（確定）

「表示のチェックを切り替えた時」「積分した時」「分割した時」に、一度全体表示に
なってから元のズームへ戻る、という報告の原因です。

`ExperimentalScreenPreview.refresh()` の順序が次のようになっています。

1. `self.consumer.render(scene)` を呼ぶ
2. `render` の最後の `_finish_render` が **全ビューを自動レンジに戻す**
3. `_finish_render` が **`self.application.processEvents()` を呼ぶ**
4. `refresh()` の末尾で、ようやく `apply_view_state(state, ...)` が保存済みの
   表示範囲を戻す

```python
    def _finish_render(self, scene, counts, *, reused_traces=False, reused_static=False):
        self.primary.enableAutoRange()
        self.secondary.enableAutoRange()
        self.overview.enableAutoRange()
        self.overview_secondary.enableAutoRange()
        ...
        self.application.processEvents()
```

**`processEvents()` があるので、その全体表示は「描かれてしまいます」。**
キューに積まれたまま捨てられるのではなく、実際に 1 フレーム表示されます。
そのあと手順 4 で元の範囲に戻るため、利用者には「一瞬全体表示を経由して同じ
ズーム画面に戻る」ように見えます。

シーンオブジェクトが作り直される経路すべてで起きます（表示チェック、積分、分割、
表示モード変更）。**セ(#203) と テ(#216) が減らしたのは「シーンの作り直し回数」で、
作り直しが必要な場合のこの往復は残っています。** 別の問題です。

対処方針: 自動レンジと `processEvents()` の前に目的の表示範囲を決めるか、
`render` の中で自動レンジを掛けないようにしてください。`_finish_render` が
自動レンジを使っているのは `last_evidence` の `shared_x` 判定と
`overview_state` の初期化のためなので、**そこをデータ由来の境界計算に置き換える
のが素直です**（`owner._full_x_bounds()` が既にあります）。
`processEvents()` を消すだけで済むかは未確認です。**消して評価が壊れないかを
テストで確かめてください。**

### 12.2 表示モードを変えると Matplotlib 表示を経由する理由（確定）

`MainWindow._refresh_screen_preview` が、分割表示の有無が変わったときに
**プレビューを丸ごと作り直します。**

```python
            split = self.project.method.view_mode == "split_y_axes"
            if self._screen_preview.consumer.split_y_axes != split:
                previous, self._screen_preview = self._screen_preview, None
                previous.close()
                from .screen_preview import ExperimentalScreenPreview
                self._screen_preview = ExperimentalScreenPreview(self)
                self.plot_stack.setCurrentWidget(self._screen_preview.consumer.widget)
```

`previous.close()` から `setCurrentWidget` までの間、`plot_stack` は
**Matplotlib のキャンバスを表示しています。** それが「一旦 matplotlib 表示を
経由する」の正体です。

`PyQtGraphSceneConsumer.__init__` が `split_y_axes` をコンストラクタ引数として
受け取り、分割用のプロットと分割ハンドルをその時点で組み立てているため、
作り直しになっています。

対処方針: 分割用の 2 枚目のプロットを**常に作っておき、表示・非表示だけを
切り替える**か、`GraphicsLayoutWidget` の行を足し引きする形にして、
**コンシューマを作り直さない**でください。作り直しが避けられない場合でも、
新しいウィジェットを先に組み立ててから 1 回で差し替えれば、Matplotlib を
経由せずに済みます。

単一表示と「全体+拡大」の切り替えでは作り直しは起きません（`split_y_axes` が
変わらないため）。**それでも全体表示の一瞬は見えます。原因は第 12.1 節です。**

### 12.3 ズームのドラッグがプロット外で解除される理由（確定）

`ExperimentalScreenPreview._handle_zoom_event` は、ドラッグ中でも毎回
「今どこにいるか」を検査し、外れていれば取り消します。

```python
            if not valid or role != drag["role"] or event.button != 1:
                self.cancel_zoom_drag()
                return True
```

`valid` は `event.hit_region in ("plot", "plot_y1", "plot_y2")` を含むので、
**カーソルがプロット面から出た瞬間に選択が消えます。**

**同じ形の不具合を 2 回直しています。** イ(#176) / ス(#201) のパン解放、
そして `PyQtGraphNavigationController.handle_event` の
「Keep ownership until release even when the pointer leaves the plot」です。
**その解決の形をズームにも広げてください。** 押した時点で範囲の役割
（`role`）を確定し、離すまで所有し続け、座標はビューの端で丸めます。

なお `_handle_zoom_event` は `hit_region` を見るので、**第 9.2 節の当たり判定の
不具合により、主目盛グリッド表示中はズームそのものが始まりません。**
ハ(#236) を先に入れてください。

### 12.4 「全体+拡大」のオーバービューは x しか塗っていない（確定）

両方の描画経路とも、オーバービューの強調は **x 方向だけ**です。

- PyQtGraph: `self.overview_region` は `LinearRegionItem`。垂直な帯で、y は常に全域
- Matplotlib: `_overview_view_patch` は axes 座標で y を `0.0`〜`1.0` に固定

```python
                    self._overview_view_patch.set_xy(
                        ((left, 0.0), (left, 1.0), (right, 1.0), (right, 0.0))
                    )
```

対処方針: 矩形に変えてください。PyQtGraph 側は `zoom_rectangle` と同じ
`QGraphicsRectItem` の作りが使えます（既に `primary.vb` で使っています）。
Matplotlib 側は `Rectangle` をデータ座標で置きます。

**`ScreenOverviewState` に y を足す必要はありません。** `apply_view_state` は
`state.y1`（と分割時は `state.y2`）を受け取っているので、そこから矩形の高さを
決められます。**新しい永続フィールドも不要です。**

オーバービュー上でズーム範囲を選べるようにする要求も、この矩形と同じ場所です。
現在 `self.overview.setMouseEnabled(x=False, y=False)` で、
`_pointer_region` はオーバービュー上の点を `("overview_y1", "x")` として返し、
`PyQtGraphNavigationController` がクリックを「その位置へ中心移動」として扱います。
**ドラッグによる範囲選択の経路はありません。** 既存のクリック移動を壊さずに
ドラッグを足してください（例: 動いた距離が数ピクセル未満ならクリック扱い）。

### 12.5 選択した範囲が離した瞬間に消える理由（確定）

`_handle_span_event` は、ボタンを離すと **先に選択表示を消してから** 呼び出しを
行います。

```python
            elif name == "button_release_event":
                self.cancel_span_drag()
                if abs(event.canvas_x - drag["pixel"]) >= 3 and x_value != drag["start"]:
                    callback = (owner._on_span_selected if mode == "integrate"
                                else owner._on_edit_span_selected if mode == "edit"
                                else owner._on_selection_span_selected)
                    callback(drag["start"], x_value)
```

`cancel_span_drag()` は `set_span_selection()` を引数なしで呼び、帯を隠します。
選んだ範囲そのものは `owner._selected_time_range` に残るので、**状態はあるのに
表示だけが消えています。**

対処方針: **選択モード（`select`）のときだけ**、離した後も帯を出したままに
してください。積分（`integrate`）と範囲修正（`edit`）は、確定すると別の図形
（積分範囲）が描かれるので現状のままで構いません。消えるきっかけ（別の場所を
クリック、Escape、モード変更、選択の削除）を決めてテストで固定してください。
`_selected_time_range` が `None` に戻るときに帯も消えるのが素直です。

### 12.6 既定のトレース配色は波長で決まっている（確定）

```python
WAVELENGTH_COLOR_PALETTES = {
    214: ("#d62728", "#ef4444", "#b91c1c", "#f87171"),
    280: ("#1f77b4", "#2563eb", "#1d4ed8", "#60a5fa"),
}
```

`default_trace_color` は **波長が 214 または 280 に 0.5 nm 以内で一致したときだけ**
色を返します。それ以外は `None` を返し、`dataset_display_color` が
`COLORS` の順番パレットへ落ちます。

```python
    return (
        dataset.color
        or default_trace_color(dataset.measurement.wavelength_nm, ordinal)
        or COLORS[ordinal % len(COLORS)]
    )
```

要求は **チャンネル**（ch1 / ch2）を基準にした寒色・暖色です。波長ではありません。
**波長が未入力のデータや 214 / 280 以外のデータには、今は何の既定色も付きません。**
そこが「思ったとおりの色にならない」と感じる原因です。

チャンネルの手掛かりは既にあります。`parser.py` が
`[Chromatogram (Ch1)]` / `(Ch2)` を読んで縦軸を決めており、取り込み時に
`dataset.y_axis` へ入ります。

```python
    has_ch1 = "1" in channel_tokens
    has_ch2 = "2" in channel_tokens
    return 2 if has_ch2 and not has_ch1 else 1
```

対処方針: `default_trace_color` に **チャンネル（`y_axis`）を基準にした既定**を
足し、ch1 を寒色、ch2 を暖色にしてください。**解決順（明示色 → 既定 →
パレット）は変えないでください**（ヌ #221 の注意）。

> **要判断。** 波長既定とチャンネル既定のどちらを優先するかは決めていません。
> **既定案は「チャンネルを優先し、波長既定は廃止する」**です。要求が
> 「改めて」と書かれており、波長 214 = 暖色 / 280 = 寒色 という現在の対応と
> チャンネル指定が食い違ったときに、利用者の期待はチャンネル側にあると
> 読めるためです。**既に明示色を持つデータセットは影響を受けません。**
> 既存プロジェクトの見た目が変わりうる点は Issue に明記してください。

### 12.7 既定の表示モードと、既存プロジェクトへの影響（確定）

`AnalysisMethod.view_mode` の既定は `"single"` です（`models.py`）。
`project_io` は `asdict(project.method)` で保存し、`_method_from_dict` で読み戻すので、
**保存済みプロジェクトは自分の値を持ち続けます。**

したがって既定を `"overview_detail"` に変えても、

- 新規プロジェクトは「全体+拡大」で開く
- 既存プロジェクトは保存された表示モードのまま

となり、**移行もスキーマ変更も不要です。** その代わり、既存プロジェクトを
開いた利用者から見ると「既定が変わっていない」ように見えます。それでよいかを
Issue に書いてください。

### 12.8 レポートの内容は画面の表示設定と無関係（確定）

レポートは `ReportOptions`（セッション限りのデータクラス）だけで決まります。

```python
class ReportOptions:
    integration_range: bool = True
    baseline: bool = True
    retention_time: bool = True
    gradient_b: bool = True
    gradient_conditions: bool = True
    quantitation: bool = True
```

一方、画面は `method.show_integration_areas` / `show_retention_labels` /
`show_gradient_b` を見ます。**両者はどこにも繋がっていません。** そのため
「積分範囲・ベースラインを表示」を外してもレポートには出ます。

対処方針: レポートの選択肢の**初期値を、そのときの画面の表示設定から作って
ください。** `ReportOptionsDialog` を開いた時点で、画面で消している要素の
チェックが外れているのが素直です。**`ReportOptions` を廃止して `Method` に
一本化しないでください。** レポートにだけある選択肢（`gradient_conditions`、
`quantitation`）があり、レポート単体で変えたい場面もあります。

対応関係を決めて表にし、テストで固定してください。**画面に無い選択肢の初期値を
どうするかも決めてください**（既定案は現状どおり `True`）。

### 12.9 レポートの余白と、削除を求められている 2 列（確定）

**余白**

```python
        figure.subplots_adjust(left=0.075, right=0.9, top=0.95, bottom=0.055, hspace=0.38)
```

1 ページ目の左右がこれです。縦軸ラベルが入る余地が `left=0.075`（A4 の 8.27 inch
に対して約 0.62 inch）しかありません。**印刷時に見切れるという報告と整合します。**
続きページは `left=0.06, right=0.94` で、こちらは表だけなので別問題です
（第 9.10 節）。

**「種別／親」列**

```python
        headers = ["#", "種別／親" if language == "ja" else "Type / parent"]
```

普通の積分行には文字列 `"Integration"` が入ります。利用者はこの列を不要と
言っています。

> **重要。** この列は、**レポートの表でフィット由来行が推定値だと分かる唯一の
> 場所**です。
>
> ```python
>                 "Fit %s%s -> #%s (estimated)" % (...)
>                 if peak.is_fitted else "Integration"
> ```
>
> `REQUIREMENTS_STATUS.md` は ナ(#218) の成果として「推定値が測定値と区別できる。
> 表・凡例・エクスポート・レポートで区別が付く」を記録しています。**列をそのまま
> 消すと、その要求が崩れます。**
>
> 対処方針: 列は消してよいが、**推定であることの表示は表のどこかに必ず残して
> ください。** 既定案は「`#` 列のラベルでフィット行と親を示し、推定である旨を
> そこに付ける」です。凡例（`label="F%d Fit %s%s (#%d, estimated)"`）は別途
> 残ります。

**「Method」列**

```python
        if options.baseline:
            headers.append("Method")
```

`Auto` / `Manual` / フィットモデル名が入ります。`options.baseline` に相乗りして
いるので、**列を消すときにベースライン描画まで消さないよう注意してください。**
同じフラグが `_plot_dataset` のベースライン描画も制御しています。

### 12.10 レポートの面積と量の列（確定）

**面積**

```python
        row.extend((_number(peak.area_mau_sec), ...))
        headers.extend(("Area (mAU·sec)", "%Area", "FWHM (min)"))
```

- 値は `peak.area_mau_sec`（mAU·sec）。要求は **µV·sec** なので
  `peak.raw_area_uv_sec` に変えます。**両方とも既に `PeakRegion` にあります。**
- 書式は `_number` が `"%.4g"` を使うため、小さい値が `1.234e-01` になります。

```python
def _number(value, digits=4) -> str:
    if value is None:
        return ""
    return ("%%.%dg" % digits) % value
```

対処方針: 面積の列だけ指数表記にならない書式に変えてください。**µV·sec は
桁が大きくなるので、`%.4g` のままだと今度は `5.123e+05` になります。**
桁区切りを入れた固定小数（例 `f"{value:,.0f}"`）が読みやすいはずです。
**`_number` 全体を変えると保持時間や FWHM の表示まで変わります。** 面積用の
書式関数を分けてください。

`peak.area_percent`（%Area）は `area_mau_sec` ではなく積分側で計算済みなので、
単位を変えても %Area は動きません。**動かないことをテストで固定してください。**

**量**

```python
        if options.quantitation:
            row.append(_number(peak.amount_ug))
            headers.append("Amount (µg)")
```

`PeakRegion` には `amount_nmol` と `amount_ug` の**両方**があり、CSV
エクスポート（`exporters.py`）は既に両方出しています。レポートだけ µg のみです。
列を 1 つ足すだけで済みます。**列幅の合計（`widths`）の調整を忘れないでください。**

---

## 13. バッチ定義（ム・メ・モ・ヤ）

今回は 12 件の要求を 4 バッチにまとめました。

### 13.0 実施順と依存関係

```
ハ(#236) ──┬─► ム(#249)        （当たり判定とズームの土台）
           └─► メ(#250)        （同じ画面系ファイル）

ム(#249) ──► メ(#250)           （どちらも screen_preview.py の描画往復）

モ(#251)                       （独立。既定値の見直し）
ヤ(#252)                       （独立。report.py が中心）
```

| 記号 | Issue | 内容 | 優先 | 依存 |
|---|---|---|---|---|
| ム | #249 | ズームの復活と「全体+拡大」の使い勝手 | 高 | #236 |
| メ | #250 | 描画の往復をなくし、選んだ範囲を残す | 高 | #236, #249 |
| モ | #251 | 既定値の見直し（表示モードとチャンネル配色） | 中 | なし |
| ヤ | #252 | 解析レポートの体裁と内容 | 中 | なし |

**依存の理由**

- **#236 → ム・メ**: #236 がグリッド表示中の当たり判定を直します
  （第 9.2 節）。**その修正が入るまで、ズームも選択もグリッド表示中は始まりません。**
  ム の受け入れ条件を実機で確認できないので、#236 のマージ後に着手してください。
- **ム → メ**: どちらも `screen_preview.py` と `pyqtgraph_scene.py` の描画経路を
  触ります。ム がズームとオーバービューの図形を足し、メ が描画の順序を変えます。
  並行させると衝突します。**先に ム**。ズームは利用者が毎日使う操作で、
  影響が大きいためです。
- **モ と ヤ は独立**。モ は `models.py` と `rendering.py`、ヤ は `report.py` が
  中心で、画面の描画経路にほとんど触りません。

> **並行数の目安は 2 つまで。** モ か ヤ のどちらかを、ム / メ と並行させるのが
> 安全な組み合わせです。**ム と メ を同時に走らせないでください。**

---

### ム — ズームの復活と「全体+拡大」の使い勝手

**Issue: #249** / **依存: #236（ハ）** / バッチ「メ」(#250) の前提

方針: **ズームを独立した機能として戻し、「全体+拡大」の上側でも範囲を選べる
ようにする。**

対象要求
- **ズーム機能を復活させたい**（2026-09-02 の「Pan と Zoom を 1 アイコンに統合」を
  覆す判断。第 12.0 節）
- 「全体+拡大」のとき、**上部の全体表示でもズーム領域を選択できる**ようにする
- **ズーム領域の選択中にカーソルが領域から出ても、選択中モードが解除されない**
  ようにする
- 「全体+拡大」表示のとき、対応する **y 軸範囲もハイライト**する（現在は x のみ）

受け入れ条件
- [ ] ズームが独立した操作として使える（到達手段が決まっており、テストで固定
      されている）
- [ ] マウスモードの選択肢とアイコンの 1 対 1 対応が保たれている
- [ ] ズームが「ズーム方向」の設定（自動 / X+Y / X / Y）に従う挙動を保っている
- [ ] ズーム中にカーソルがプロット面の外へ出ても、離すまで選択が続く
- [ ] プロット面の外で離しても、選択が正しく確定するか取り消されるかが決まっており、
      テストで固定されている
- [ ] 「全体+拡大」の上側でドラッグすると、その範囲へズームできる
- [ ] 上側のクリックによる従来の中心移動が退行していない
- [ ] クリックとドラッグの区別のしきい値が決まっており、テストで固定されている
- [ ] 「全体+拡大」の強調表示が、x 範囲と y 範囲の両方を示す矩形になっている
- [ ] 矩形が Matplotlib 描画と PyQtGraph 描画の両方で出る
- [ ] 分割表示（Y1 / Y2）のときの矩形の扱いが決まっている
- [ ] 表示範囲を変えると矩形が追従する
- [ ] `REQUIREMENTS_STATUS.md` を更新する

対象ファイル: `hplc_app/gui.py`, `hplc_app/screen_preview.py`,
`hplc_app/pyqtgraph_scene.py`, `hplc_app/i18n.py`, `tests/test_gui.py`

注意
- **第 12.0・12.3・12.4 節に確定した内容があります。**
- **ズームの復活は #236 の受け入れ条件を覆します。** #236 が
  「`removeAction()` して右ドラッグへ逃がす」形で実装済みなら、その部分を
  作り直します。**#236 のマージ前なら、#236 の側で直す方が無駄がありません。**
  どちらで直すかを両方の Issue にコメントしてください。
- **1 対 1 対応の要求は取り下げられていません。** マウスモードに `zoom` を足して
  9 モード ↔ 9 アイコンにするのが既定案です。
- ドラッグの所有権は **イ(#176) / ス(#201) と
  `PyQtGraphNavigationController.handle_event` に前例があります。**
  「離すまで所有し続ける」という同じ形に揃えてください。**新しいドラッグ管理を
  作らないこと。**
- オーバービューの矩形は `zoom_rectangle`（`QGraphicsRectItem`）と同じ作りが
  使えます。**`ScreenOverviewState` に y を足す必要はありません**（第 12.4 節）。
- **スキーマは触りません。**

---

### メ — 描画の往復をなくし、選んだ範囲を残す

**Issue: #250** / **依存: #236（ハ）, #249（ム）**

方針: **描画のたびに別の状態が 1 フレーム見えるのをやめる。**

対象要求
- 次のとき、一旦全体表示を経由してから同じズーム画面に戻るのを直す
  - クロマトグラム表示のチェックを切り替えた時
  - 積分した時、分割した時
- 表示モードを切り替えた時、**一旦 Matplotlib 表示を経由するのをやめる**
- **選択モードで選択したら、選択領域がそのままマークされている**ようにする

受け入れ条件
- [ ] 表示チェックの切り替えで、表示範囲が一度も全体表示にならない
- [ ] 積分・分割の後も同じことが言える
- [ ] 表示モードの切り替えでも同じことが言える
- [ ] **全体表示を経由しないことを、描画順序かフレーム内容でテストに固定する**
      （目視だけで判断しない）
- [ ] 表示モードを切り替えても Matplotlib のキャンバスが表示されない
- [ ] 分割表示への切り替え・解除が退行していない
- [ ] 選択モードで範囲を選ぶと、離した後も帯が表示されたままになる
- [ ] 帯が消えるきっかけが決まっており、テストで固定されている
- [ ] 積分・範囲修正のドラッグは従来どおり（確定すると帯が消える）
- [ ] `_selected_time_range` と帯の表示が食い違わない
- [ ] シーンの作り直し回数が セ(#203) / テ(#216) の水準から増えていない
- [ ] Matplotlib 描画が退行していない
- [ ] `REQUIREMENTS_STATUS.md` を更新する

対象ファイル: `hplc_app/gui.py`, `hplc_app/screen_preview.py`,
`hplc_app/pyqtgraph_scene.py`, `tests/test_gui.py`

注意
- **第 12.1・12.2・12.5 節に確定した内容があります。**
- **セ(#203) と テ(#216) が直したのは「シーンの作り直し回数」です。** 今回は
  作り直しが必要な場合の**表示範囲の往復**で、別の問題です。#203 / #216 の
  テストを壊さずに直してください。
- `_finish_render` の `enableAutoRange()` と `processEvents()` が原因です。
  **`processEvents()` を消すだけで `last_evidence` の評価が壊れないかを、
  テストで確かめてから消してください。**
- プレビューの作り直し（第 12.2 節）は、**コンシューマを作り直さない形にするのが
  本筋**です。難しければ、新しいウィジェットを先に組み立ててから 1 回で
  差し替えてください。
- **`AGENTS.md` の実機確認の注意が効きます。** ちらつきは合成テストで真偽が
  確定しにくい項目です。**シーン再構築回数・適用された表示範囲の履歴など、
  状態で固定してください。** イ(#176) の前例を忘れないこと。

---

### モ — 既定値の見直し（表示モードとチャンネル配色）

**Issue: #251** / **依存なし**

方針: **新規プロジェクトの見え方の既定を、利用者の使い方に合わせる。**

対象要求
- デフォルトの表示モードを **「全体+拡大」** にする
- **ch1 系の色は寒色系、ch2 系は暖色系**にする

受け入れ条件
- [x] 新しいプロジェクトの表示モードが「全体+拡大」で始まる
      （`test_new_main_window_starts_in_overview_detail_mode`）
- [x] 保存済みプロジェクトを開いたとき、保存された表示モードが尊重される
      （`test_new_projects_default_to_overview_detail_without_migrating_saved_mode` で
      明示した `single` を保存・復元）
- [x] 表示モードの保存・読み込みが退行していない
      （同テストと既存プロジェクト往復テスト、フルスイート）
- [x] ch1 のデータセットに寒色系、ch2 に暖色系の既定色が付く
      （`test_default_trace_colors_follow_channel_families`、
      `test_trace_colors_prefer_explicit_values_then_channel_defaults`）
- [x] 明示的な色を持つデータセットが、この変更で色を変えない
      （`test_trace_colors_prefer_explicit_values_then_channel_defaults`）
- [x] 配色の解決順（明示色 → 既定 → パレット）が変わっていない
      （同テストと `dataset_display_color` のフォールバック）
- [x] 同じチャンネルのデータセットが複数あるとき、区別できる色になる
      （`test_import_assigns_distinct_defaults_within_the_same_channel`）
- [x] 波長既定との関係が決まっており、テストで固定されている
      （チャンネルを優先し波長既定を廃止。旧214/280を交差させたGUIテストで固定）
- [x] 既存プロジェクトの見た目がどう変わるかを PR に書いている
      （保存済み表示モードと明示色は不変、未設定色だけチャンネル既定になる旨を記載）
- [x] `REQUIREMENTS_STATUS.md` を更新する

実測（2026-09-05）:
- Windows 11 / Python 3.11.9 x64 / PySide6 6.8.3 / Matplotlib 3.10.1 /
  NumPy 2.2.3 / PyQtGraph 0.13.7 でフルスイート **391件**が
  **662.080秒**で成功した。
- Python 3.8.10 x86 で変更Python 6ファイルの構文チェックに成功した。
  現行環境の `compileall`、`ci_validate.py --offline-assets auto` の Level 2
  ソース契約と Windows 7 オフライン manifest / wheelhouse 検証も成功した。
- スキーマ、依存pin、科学計算、生データ、保存済み表示モード、明示色、
  オフラインビルド入力は変更していない。新規プロジェクトと未設定色だけが新しい
  既定を使う。チャンネル配色と全体+拡大のネイティブ画面での見やすさは既存の
  実機ゲートに残す。

対象ファイル: `hplc_app/models.py`, `hplc_app/rendering.py`, `hplc_app/gui.py`,
`hplc_app/plot3d.py`, `tests/test_core.py`, `tests/test_gui.py`

注意
- **第 12.6・12.7 節に確定した内容があります。**
- **スキーマ変更も移行も不要です**（第 12.7 節）。`view_mode` は保存済みの値が
  優先されるので、既定を変えても既存プロジェクトは影響を受けません。
- **チャンネルは `dataset.y_axis` です。** `parser.py` が
  `[Chromatogram (Ch1)]` / `(Ch2)` から決めています。**新しいフィールドを
  足さないでください。**
- **配色の解決順を変えないこと**（ヌ #221 の注意）。既定を足すのであって、
  明示色より前に出さない。
- 波長既定（214 = 暖色 / 280 = 寒色）とチャンネル既定は食い違いえます。
  **既定案は「チャンネルを優先し、波長既定は廃止する」**です。異なる判断が必要なら
  進める前に Issue へコメントしてください。
- ヌ(#221) のグラジエント配色は明示色を設定する機能なので、この変更の影響を
  受けません。**退行していないことを確認してください。**

---

### ヤ — 解析レポートの体裁と内容

**Issue: #252** / **依存なし**

方針: **レポートを、画面で見えているものと同じ内容にし、読める形にする。**

対象要求
- 画面表示と解析レポートで出力される要素をそろえる。例えば「積分範囲・ベースラインを
  表示」のチェックを外していたら、それが反映されるようにする
- クロマトグラムの左端（主に縦軸ラベル）が印刷時に見切れるので、**左右の余白を
  もう少し増やす**
- **積分方法（`Integration` と書いてある列）と `Method` の欄は不要**
- 面積を **`e-01` のような指数表記ではなく、そのままの形式**で表示する。
  単位も **mAU·sec ではなく µV·sec** にする
- **量（nmol）と量（µg）をどちらも表示する**

受け入れ条件
- [ ] レポートの選択肢の初期値が、そのときの画面の表示設定から作られる
- [ ] 「積分範囲・ベースラインを表示」を外した状態でレポートを出すと、積分範囲と
      ベースラインが出ない
- [ ] 保持時間ラベルと %B についても同じことが言える
- [ ] 画面に対応する設定が無い選択肢の初期値が決まっている
- [ ] レポート側だけで選択肢を変えられる従来の動作が残っている
- [ ] 1 ページ目の左右の余白が広がり、縦軸ラベルが切れない
- [ ] 図の描画領域が狭くなりすぎていない
- [ ] 「種別／親」列が表から消えている
- [ ] **フィット由来行が推定値だと、表の中で分かる**（#218 の要求を維持する）
- [ ] `Method` 列が表から消えている
- [ ] `Method` 列を消しても、ベースラインの描画が消えていない
- [ ] 面積の列が µV·sec になっている
- [ ] 面積が指数表記にならず、桁が読める形式で出る
- [ ] 保持時間・FWHM など他の列の書式が変わっていない
- [ ] %Area の値がこの変更で変わらない
- [ ] 量（nmol）と量（µg）の両方の列が出る
- [ ] 列幅の合計が崩れず、表が図からはみ出さない
- [ ] 続きページの表も同じ列構成になる
- [ ] 印刷プレビュー（ニ #219）と PDF 出力が同じ内容になる
- [ ] `REQUIREMENTS_STATUS.md` を更新する

対象ファイル: `hplc_app/report.py`, `hplc_app/dialogs.py`, `hplc_app/gui.py`,
`hplc_app/i18n.py`, `tests/test_core.py`, `tests/test_gui.py`

注意
- **第 12.8・12.9・12.10 節に確定した内容があります。**
- **「種別／親」列は、レポートの表でフィット由来行が推定値だと分かる唯一の
  場所です。** `REQUIREMENTS_STATUS.md` が ナ(#218) の成果として記録している
  「表・凡例・エクスポート・レポートで区別が付く」を崩さないでください。
  **列は消してよいが、推定であることの表示は表のどこかに必ず残すこと。**
- `Method` 列は `options.baseline` に相乗りしています。**同じフラグが
  `_plot_dataset` のベースライン描画も制御しています。** 列を消すときに描画まで
  消さないでください。
- 面積の書式は **`_number` 全体を変えないでください。** 保持時間や FWHM の表示まで
  変わります。面積用の書式を分けること。µV·sec は桁が大きいので、`%.4g` のままだと
  今度は `5.123e+05` になります。
- `ReportOptions` を廃止して `Method` に一本化しないでください（第 12.8 節）。
  **初期値を画面から作るだけ**にとどめます。
- ミ(#242) は続きページの余白と行数を変更してマージ済みです。**1 ページ目の余白
  （このバッチ）と続きページの余白（ミ）は別です。** #252 は最新 `main` から始め、
  48行の続きページを維持してください。
- **スキーマは触りません。** 量の 2 列は `PeakRegion.amount_nmol` /
  `amount_ug` が既にあります。

---

## 14. 未決定事項（ム・メ・モ・ヤ）

| 項目 | 内容 | 既定案 | 判断者 |
|---|---|---|---|
| ズームの戻し方 | ム節（第 12.0 節）。マウスモードに `zoom` を足すか、モードの外に置くか | **`zoom` モードを足して 9 モード ↔ 9 アイコンにする** | 実装時に Issue へコメント |
| プロット外で離したとき | ム節（第 12.3 節）。ズーム中にプロット外で離したら確定するか取り消すか | **ビューの端で丸めて確定する** | 実装時に決めてテストで固定 |
| クリックとドラッグの境目 | ム節。オーバービュー上で、何ピクセル動いたらドラッグ扱いにするか | **既存のズーム／選択と同じ 3 ピクセル** | 実装時に決めてテストで固定 |
| 分割表示のオーバービュー矩形 | ム節（第 12.4 節）。Y1 / Y2 のどちらの範囲を矩形の高さにするか | **Y1。分割時は上側の範囲を示す** | 実装時に決めてテストで固定 |
| `processEvents()` の扱い | メ節（第 12.1 節）。消すだけで `last_evidence` の評価が保てるか | **消す。保てないなら自動レンジの代わりにデータ由来の境界を使う** | 実装時に測って決める |
| プレビューの作り直し | メ節（第 12.2 節）。分割用プロットを常設するか、差し替えを 1 回にするか | **常設して表示・非表示を切り替える** | 実装時に決めて理由を PR に書く |
| 選択の帯が消えるきっかけ | メ節（第 12.5 節）。何をしたら選択表示が消えるか | **`_selected_time_range` が `None` に戻るとき（別の場所をクリック、Escape、モード変更、削除）** | 実装時に決めてテストで固定 |
| 波長既定とチャンネル既定 | モ節（第 12.6 節）。どちらを優先するか | **チャンネルを優先し、波長既定は廃止する** | 実装時に Issue へコメント |
| 既存プロジェクトの既定表示モード | モ節（第 12.7 節）。保存済みの `single` をそのままにするか | **そのままにする。移行しない** | 実装時に Issue へコメント |
| 推定行の残し方 | ヤ節（第 12.9 節）。「種別／親」列を消した後、推定であることをどう示すか | **`#` 列のラベルでフィット行と親を示し、推定である旨を付ける** | 実装時に決めてテストで固定 |
| 画面に無いレポート選択肢 | ヤ節（第 12.8 節）。`gradient_conditions` と `quantitation` の初期値 | **現状どおり `True`** | 実装時に決める |
| 面積の書式 | ヤ節（第 12.10 節）。µV·sec の桁をどう見せるか | **桁区切り付きの固定小数（指数表記にしない）** | 実装時に決めてテストで固定 |

### 決定済み（ム・メ・モ・ヤ）

| 項目 | 決定 |
|---|---|
| ズーム機能 | **復活させる**（2026-09-03 利用者判断）。2026-09-02 の「Pan と Zoom を 1 アイコンに統合」を覆す |
| ズーム復活を直す Issue | **#249**。#236 は PR #254 でマージ済みのため、撤回された部分を独立した次の Issue で戻す |
| 上部バーとマウスモードの 1 対 1 | **取り下げられていない**。ズームを戻したうえで 1 対 1 を保つ |
| 既定の表示モード | **「全体+拡大」**（2026-09-03 利用者判断） |
| チャンネル配色 | **ch1 は寒色系、ch2 は暖色系**（2026-09-03 利用者判断） |
| レポートの面積の単位 | **µV·sec**（2026-09-03 利用者判断）。mAU·sec から変更 |
| レポートの量の列 | **nmol と µg の両方**（2026-09-03 利用者判断） |
| 推定値の区別 | **維持する**。ナ(#218) の要求であり、列を消す判断がこれを覆すことはない |
| 今回のスキーマ変更 | **なし**。4 バッチのいずれも `.hplcproj` の形式を変えない |
